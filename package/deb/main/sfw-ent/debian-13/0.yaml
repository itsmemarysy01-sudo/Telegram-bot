# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-sfw-ent
variant: runtime
tags:
  - 1.15.1-0
  - 1.15.1-0-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  ARCH: '#{ target.arch == "amd64" ? "x86_64" : "arm64" }'
  DHI_REL: "0"
  DHI_VERSION: 1.15.1-0
  VERSION: 1.15.1
contents:
  packages:
    - '!base-files'
  builds:
    - name: sfw-ent deb
      work-dir: ${source.dir}/sfw-ent
      environment:
        SFW_ARCH: ${ARCH}
        SFW_VERSION: 1.15.1
      contents:
        packages:
          - bash
          - debhelper
          - dpkg-dev
        files:
          - url: https://github.com/SocketDev/firewall-release/releases/download/v1.15.1/sfw-linux-${ARCH}
            path: ${source.dir}/sfw-linux-${ARCH}
            uid: 0
            gid: 0
            mode: "0755"
      pipeline:
        - name: setup
          runs: |
            set -eux -o pipefail

            chmod +x debian/create-aliases.sh
        - uses: dpkg/bump@v1
          with:
            version: 1.15.1-0
        - uses: dpkg/build@v1
          with:
            args: -b -uc -us -d
      paths:
        - type: directory
          path: ${source.dir}/sfw-ent/debian
          uid: 0
          gid: 0
          mode: "0600"
          source: package/deb/main/sfw-ent/debian-13/debian
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
