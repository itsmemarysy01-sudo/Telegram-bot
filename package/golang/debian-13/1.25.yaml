# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-golang
variant: runtime
tags:
  - 1.25.14
  - 1.25.14-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT: 08118a942518cc18db507ec182acffe7364a92a3
  VERSION: 1.25.14
contents:
  packages:
    - '!base-files'
  builds:
    - name: golang
      contents:
        packages:
          - bash
          - coreutils
          - findutils
          - build-essential
          - binutils
          - grep
          - sed
          - golang-1.24
          - git
        files:
          - url: git+https://github.com/golang/go.git#go1.25.14
            path: ${source.dir}/go
            spdx:
              name: golang
              version: 1.25.14
              packages:
                - name: golang
                  purl: pkg:dhi/golang@1.25.14
                  license: BSD-3-Clause
            uid: 0
            gid: 0
      pipeline:
        - name: verify
          uses: git/checkout@v1
          with:
            dir: go
            expected-commit: 08118a942518cc18db507ec182acffe7364a92a3
        - name: install
          runs: |
            set -eux -o pipefail

            cd "${source.dir}/go/src"

            export GOROOT_BOOTSTRAP=/usr/lib/go-1.24

            ./make.bash -v

            cd "${source.dir}/go"

            rm -rf .git .gitattributes .github .gitignore
            rm -rf pkg/obj
            rm -rf pkg/bootstrap
            rm -rf pkg/tool/*/api
            rm -rf pkg/tool/*/dist*
            rm -rf pkg/*/cmd
            rm -rf pkg/tool/*/go_bootstrap
            rm -rf src/cmd/dist
      outputs:
        - source: /opt/docker/sbom
          target: /opt/docker/sbom
          uid: 0
          gid: 0
        - source: /src/go
          target: /usr/local/go
          uid: 0
          gid: 0
accounts:
  root: true
  run-as: root
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  GOLANG_VERSION: 1.25.14
  GOPATH: /go
  GOTOOLCHAIN: local
  PATH: /go/bin:/usr/local/go/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
paths:
  - type: directory
    path: /tmp
    uid: 0
    gid: 0
