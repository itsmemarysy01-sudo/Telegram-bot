# syntax=dhi.io/build:2-alpine3.24

image: dhi.io/pkg-dapr-operator
variant: runtime
tags:
  - 1.18.3-alpine3.24
  - 1.18.3-r2-alpine3.24
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-04-02"
vars:
  COMMIT_SHA: 2dcf2e3548faf3c740b3d29ee300e2132db65cff
  GOLANG_MAJOR_MINOR_VERSION: "1.26"
  GOLANG_VERSION: 1.26.8-r0
  PACKAGE_NAME: dapr-operator
  REL: "2"
  SEMVER_MAJOR_MINOR_VERSION: "1.18"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.18.3
  VERSION: 1.18.3
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.24/main
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: dapr-operator
      work-dir: /go/src/github.com/dapr/dapr
      caches:
        - path: /go/pkg/mod
      contents:
        packages:
          - busybox
          - bash
          - ca-certificates-bundle
          - coreutils
          - findutils
          - mawk
          - sed
          - golang-1.26=1.26.8-r0
          - golang-1.26-oci-config=1.26.8-r0
        files:
          - url: git+https://github.com/dapr/dapr.git#v1.18.3
            checksum: 2dcf2e3548faf3c740b3d29ee300e2132db65cff
            path: /go/src/github.com/dapr/dapr
            spdx:
              name: dapr-operator
              version: 1.18.3
              packages:
                - name: dapr-operator
                  purl: pkg:golang/github.com/dapr/dapr@1.18.3
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v2
          with:
            deps:
              - github.com/go-jose/go-jose/v4@v4.1.4
              - go.opentelemetry.io/otel@v1.44.0
              - google.golang.org/grpc@v1.82.1
            download: true
        - name: test
          environment:
            GODEBUG: ""
          runs: |
            set -eux -o pipefail

            unset OTEL_EXPORTER_OTLP_TRACES_ENDPOINT || true
            unset OTEL_EXPORTER_OTLP_TRACES_PROTOCOL || true

            export DAPR_HOST_IP="${DAPR_HOST_IP:-127.0.0.1}"

            CGO_ENABLED=0 go test -tags unit \
              $(go list -tags unit ./... | grep -v 'github.com/dapr/dapr/tests/')
        - name: build
          uses: go/build@v1
          work-dir: /go/src/github.com/dapr/dapr/cmd/operator
          environment:
            GOFLAGS: -buildvcs=false
          with:
            fips: false
            ldflags:
              - -X github.com/dapr/dapr/pkg/buildinfo.gitcommit=2dcf2e3548faf3c740b3d29ee300e2132db65cff
              - -X github.com/dapr/dapr/pkg/buildinfo.gitversion=1.18.3
              - -X github.com/dapr/dapr/pkg/buildinfo.version=1.18.3
            output: ${target.dir}/usr/bin/dapr-operator
        - name: build dev
          uses: go/build@v1
          work-dir: /go/src/github.com/dapr/dapr/cmd/operator
          environment:
            GOFLAGS: -buildvcs=false
          with:
            fips: false
            ldflags:
              - -X github.com/dapr/dapr/pkg/buildinfo.gitcommit=2dcf2e3548faf3c740b3d29ee300e2132db65cff
              - -X github.com/dapr/dapr/pkg/buildinfo.gitversion=1.18.3
              - -X github.com/dapr/dapr/pkg/buildinfo.version=1.18.3
            output: ${target.dir}/usr/bin/dapr-operator-dev
            strip: false
        - name: verify binaries
          environment:
            GODEBUG: ""
          runs: |
            set -eux -o pipefail

            for binary in \
              "${target.dir}/usr/bin/dapr-operator" \
              "${target.dir}/usr/bin/dapr-operator-dev"; do
              set +o pipefail
              output="$(strings "${binary}" | grep -F -m 1 '1.18.3')"
              set -o pipefail
              printf '%s\n' "${output}"
              test -n "${output}"
            done
        - uses: dhi/build-pkg:0
          with:
            description: Dapr Kubernetes operator
            files:
              - usr/bin/dapr-operator
            license: Apache-2.0
            name: dapr-operator
            rel: 2
            subpackages:
              - files:
                  - path: usr/bin/dapr-operator
                    source: usr/bin/dapr-operator-dev
                name: dapr-operator-dev
            url: https://github.com/dapr/dapr
            version: 1.18.3
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.24"
  pretty-name: Docker Hardened Images/Alpine Linux v3.24
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
