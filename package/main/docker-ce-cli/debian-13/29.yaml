# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-docker-ce-cli
variant: runtime
tags:
  - 29.8.0
  - 29.8.0-r0
  - 29.8.0-debian13
  - 29.8.0-r0-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-07-16"
vars:
  COMMIT_SHA: 88096ef00576baf72a9cb45caa45c0544c40e0a7
  GOLANG_MAJOR_MINOR_VERSION: "1.26"
  GOLANG_VERSION: 1.26.8-0
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "29.8"
  SEMVER_MAJOR_VERSION: "29"
  SEMVER_VERSION: 29.8.0
  VERSION: 29.8.0
contents:
  packages:
    - base-files
  builds:
    - name: docker-ce-cli deb
      work-dir: /go/src/github.com/docker/cli
      caches:
        - path: /go/pkg/mod
      contents:
        repositories:
          - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
        keyring:
          - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
        packages:
          - bash
          - ca-certificates-bundle
          - coreutils
          - findutils
          - gcc
          - golang-1.26=1.26.8-0
          - golang-1.26-oci-config=1.26.8-0
          - libc6-dev
        files:
          - url: git+https://github.com/docker/cli.git#v29.8.0
            checksum: 88096ef00576baf72a9cb45caa45c0544c40e0a7
            path: /go/src/github.com/docker/cli
            spdx:
              name: docker-cli
              version: 29.8.0
              packages:
                - name: docker-cli
                  purl: pkg:golang/github.com/docker/cli@29.8.0
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: prepare docker cli go module
          runs: |
            set -eux -o pipefail

            ln -sf vendor.mod go.mod
            ln -sf vendor.sum go.sum
        - name: update docker cli go dependencies
          uses: go/bump@v2
          with:
            deps: []
            download: true
        - name: test docker cli after dependency updates
          runs: |
            set -eux -o pipefail

            CGO_ENABLED=1 go test -count=1 -timeout=5m -tags grpcnotrace ./cmd/docker
        - name: build docker cli
          uses: go/build@v1
          with:
            cgo_enabled: 1
            extra-args: -buildmode=pie
            ldflags:
              - -X github.com/docker/cli/cli/version.GitCommit=88096ef00576baf72a9cb45caa45c0544c40e0a7
              - -X github.com/docker/cli/cli/version.BuildTime=$(date -ud @${SOURCE_DATE_EPOCH} +'%Y-%m-%dT%H:%M:%SZ')
              - -X github.com/docker/cli/cli/version.Version=29.8.0
            output: ${target.dir}/usr/bin/docker
            packages: ./cmd/docker
            tags: grpcnotrace
        - uses: dhi/build-pkg:0
          with:
            conflicts: docker-cli, docker.io
            depends:
              - libc6
            description: Docker CLI open-source application container engine
            files:
              - usr/bin/docker
            formats:
              - deb
            license: Apache-2.0
            name: docker-ce-cli
            rel: 0
            url: https://www.docker.com/
            version: 5:29.8.0
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
