# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-apigee-microgateway
variant: runtime
tags:
  - 3.3.11
  - 3.3.11-r3
  - 3.3.11-debian13
  - 3.3.11-r3-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: f2817cbceab4dd0f84df49d9d9bd6d687ca3a233
  NODEJS_VERSION: 24.20.0-0
  REL: "3"
  SEMVER_MAJOR_MINOR_VERSION: "3.3"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.3.11
  VERSION: 3.3.11
contents:
  packages:
    - base-files
  builds:
    - name: apigee-microgateway
      work-dir: ${source.dir}/edgemicro
      contents:
        repositories:
          - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
        keyring:
          - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
        packages:
          - bash
          - ca-certificates
          - coreutils
          - findutils
          - git
          - gzip
          - libc6
          - libstdc++6
          - node-corepack
          - nodejs-24=24.20.0-0
          - npm
          - sed
          - tar
          - yarn-classic
        files:
          - url: git+https://github.com/apigee-internal/microgateway.git#v3.3.11
            checksum: f2817cbceab4dd0f84df49d9d9bd6d687ca3a233
            path: ${source.dir}/edgemicro
            spdx:
              name: edgemicro
              version: 3.3.11
              packages:
                - name: edgemicro
                  purl: pkg:npm/edgemicro@3.3.11
                  license: Apache-2.0
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/qs/-/qs-6.15.2.tgz
            checksum: sha256:e699d4b3ac62181f5bcd29c1d15224054c20d81aab67f7b2e75b7f7a2185b897
            path: ${source.dir}/qs-6.15.2.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/diff/-/diff-8.0.3.tgz
            checksum: sha256:0e13ca2ab510066af64a2fa0998bc913f2fa888c2eaeb4dd10622cc6fba30108
            path: ${source.dir}/diff-8.0.3.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/minimatch/-/minimatch-10.2.3.tgz
            checksum: sha256:48dd0992f33943886142f1cd962bb56f8e85c7044637076fea97bf0677e21d79
            path: ${source.dir}/minimatch-10.2.3.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/lodash/-/lodash-4.18.0.tgz
            checksum: sha256:aef19210308907f32d1b1d54f40e3cf461dbddae5605efe4027d6646c6967fb0
            path: ${source.dir}/lodash-4.18.0.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/underscore/-/underscore-1.13.8.tgz
            checksum: sha256:6547214df2878ae60cc552422ef14af38b8d4e3aa613be5bfe9c52b775d96f7a
            path: ${source.dir}/underscore-1.13.8.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/jsrsasign/-/jsrsasign-11.1.1.tgz
            checksum: sha256:c4c790938e313f28be2ad212897ef47c66e4b606ea8ac67c0e0b1656975d4615
            path: ${source.dir}/jsrsasign-11.1.1.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/brace-expansion/-/brace-expansion-5.0.9.tgz
            checksum: sha256:5d06001fddd25cbee90c96db4dc5b7b57711b984c3141e28d10f143deb52dbaf
            path: ${source.dir}/brace-expansion-5.0.9.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/ip-address/-/ip-address-10.3.1.tgz
            checksum: sha256:ad1790063beea11a312c801df30d58e147de762f4f77787552376eb7424623e5
            path: ${source.dir}/ip-address-10.3.1.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/uuid/-/uuid-11.1.1.tgz
            checksum: sha256:82739fc18cf288807d2314f6f52fe8887c6359751569a8252572f16abb151141
            path: ${source.dir}/uuid-11.1.1.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/tmp/-/tmp-0.2.7.tgz
            checksum: sha256:e51210588ae5056744ebb9706de5424b98b8f992597af1b35a9a8d9405e4f67f
            path: ${source.dir}/tmp-0.2.7.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/js-yaml/-/js-yaml-4.3.1.tgz
            checksum: sha256:08d6282b77a3e7242061f6dd5516c019b25c53041ad267bca3b790d79ddd5f34
            path: ${source.dir}/js-yaml-4.3.1.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/body-parser/-/body-parser-1.20.6.tgz
            checksum: sha256:01df6bf39a0a9b8441b96116112947a413c2e8988b8fe2aff6f1ffd83b819266
            path: ${source.dir}/body-parser-1.20.6.tgz
            uid: 0
            gid: 0
      pipeline:
        - name: patch
          uses: npm/bump@v1
          privileged: true
          with:
            bumps: ${source.dir}/npm-patches.json
            npm-root: ${source.dir}/edgemicro
        - name: install dependencies
          privileged: true
          runs: |
            set -eux -o pipefail

            npm install --omit=dev
        - name: patch nested qs packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/qs-fix
            tar -xzf ${source.dir}/qs-6.15.2.tgz -C /tmp/qs-fix

            find node_modules -type d -name "qs" -print0 | while IFS= read -r -d '' qs_dir; do
              echo "Replacing $qs_dir"
              rm -rf "$qs_dir"
              cp -r /tmp/qs-fix/package "$qs_dir"
            done

            rm -rf /tmp/qs-fix
        - name: patch nested diff packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/diff-fix
            tar -xzf ${source.dir}/diff-8.0.3.tgz -C /tmp/diff-fix

            find node_modules -type d -name "diff" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' diff_dir; do
              echo "Replacing $diff_dir"
              rm -rf "$diff_dir"
              cp -r /tmp/diff-fix/package "$diff_dir"
            done

            rm -rf /tmp/diff-fix
        - name: patch nested minimatch packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/minimatch-fix
            tar -xzf ${source.dir}/minimatch-10.2.3.tgz -C /tmp/minimatch-fix

            find node_modules -type d -name "minimatch" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' mm_dir; do
              echo "Replacing $mm_dir"
              rm -rf "$mm_dir"
              cp -r /tmp/minimatch-fix/package "$mm_dir"
            done

            rm -rf /tmp/minimatch-fix
        - name: patch nested lodash packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/lodash-fix
            tar -xzf ${source.dir}/lodash-4.18.0.tgz -C /tmp/lodash-fix
            find node_modules -type d -name "lodash" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' lodash_dir; do
              echo "Replacing $lodash_dir"
              rm -rf "$lodash_dir"
              cp -r /tmp/lodash-fix/package "$lodash_dir"
            done

            rm -rf /tmp/lodash-fix
        - name: patch nested underscore packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/underscore-fix
            tar -xzf ${source.dir}/underscore-1.13.8.tgz -C /tmp/underscore-fix
            find node_modules -type d -name "underscore" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' underscore_dir; do
              echo "Replacing $underscore_dir"
              rm -rf "$underscore_dir"
              cp -r /tmp/underscore-fix/package "$underscore_dir"
            done

            rm -rf /tmp/underscore-fix
        - name: patch nested jsrsasign packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/jsrsasign-fix
            tar -xzf ${source.dir}/jsrsasign-11.1.1.tgz -C /tmp/jsrsasign-fix
            find node_modules -type d -name "jsrsasign" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' jsrsasign_dir; do
              echo "Replacing $jsrsasign_dir"
              rm -rf "$jsrsasign_dir"
              cp -r /tmp/jsrsasign-fix/package "$jsrsasign_dir"
            done

            rm -rf /tmp/jsrsasign-fix
        - name: patch nested brace-expansion packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/brace-expansion-fix
            tar -xzf ${source.dir}/brace-expansion-5.0.9.tgz -C /tmp/brace-expansion-fix
            find node_modules -type d -name "brace-expansion" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' be_dir; do
              echo "Replacing $be_dir"
              rm -rf "$be_dir"
              cp -r /tmp/brace-expansion-fix/package "$be_dir"
            done

            rm -rf /tmp/brace-expansion-fix
        - name: patch nested ip-address packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/ip-address-fix
            tar -xzf ${source.dir}/ip-address-10.3.1.tgz -C /tmp/ip-address-fix
            find node_modules -type d -name "ip-address" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' ip_dir; do
              echo "Replacing $ip_dir"
              rm -rf "$ip_dir"
              cp -r /tmp/ip-address-fix/package "$ip_dir"
            done

            rm -rf /tmp/ip-address-fix
        - name: patch nested uuid packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/uuid-fix
            tar -xzf ${source.dir}/uuid-11.1.1.tgz -C /tmp/uuid-fix
            find node_modules -type d -name "uuid" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' uuid_dir; do
              echo "Replacing $uuid_dir"
              rm -rf "$uuid_dir"
              cp -r /tmp/uuid-fix/package "$uuid_dir"
            done

            rm -rf /tmp/uuid-fix
        - name: patch nested tmp packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/tmp-fix
            tar -xzf ${source.dir}/tmp-0.2.7.tgz -C /tmp/tmp-fix
            find node_modules -type d -name "tmp" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' tmp_dir; do
              echo "Replacing $tmp_dir"
              rm -rf "$tmp_dir"
              cp -r /tmp/tmp-fix/package "$tmp_dir"
            done

            rm -rf /tmp/tmp-fix
        - name: patch nested js-yaml packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/js-yaml-fix
            tar -xzf ${source.dir}/js-yaml-4.3.1.tgz -C /tmp/js-yaml-fix
            find node_modules -type d -name "js-yaml" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' js_yaml_dir; do
              echo "Replacing $js_yaml_dir"
              rm -rf "$js_yaml_dir"
              cp -r /tmp/js-yaml-fix/package "$js_yaml_dir"
            done

            rm -rf /tmp/js-yaml-fix
        - name: patch nested body-parser packages
          privileged: true
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/body-parser-fix
            tar -xzf ${source.dir}/body-parser-1.20.6.tgz -C /tmp/body-parser-fix
            find node_modules -type d -name "body-parser" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' bp_dir; do
              echo "Replacing $bp_dir"
              rm -rf "$bp_dir"
              cp -r /tmp/body-parser-fix/package "$bp_dir"
            done

            rm -rf /tmp/body-parser-fix
        - name: create default config
          runs: |
            set -eux -o pipefail

            mkdir -p "${target.dir}/usr/share/apigee-microgateway/default-config"
            npx edgemicro init -c "${target.dir}/usr/share/apigee-microgateway/default-config"
        - name: stage payload
          runs: |
            set -eux -o pipefail

            npm pack
            pack_tgz=$(ls edgemicro-*.tgz)
            mkdir -p /tmp/edgemicro-pack
            tar -xzf "$pack_tgz" -C /tmp/edgemicro-pack

            mkdir -p "${target.dir}/usr/lib/apigee-microgateway"
            cp -a /tmp/edgemicro-pack/package/. "${target.dir}/usr/lib/apigee-microgateway/"
            cp -a node_modules "${target.dir}/usr/lib/apigee-microgateway/"

            rm -f "${target.dir}/usr/lib/apigee-microgateway/.env" \
              "${target.dir}/usr/lib/apigee-microgateway/.gitignore" \
              "${target.dir}/usr/lib/apigee-microgateway/.eslintrc" \
              "${target.dir}/usr/lib/apigee-microgateway/.jshintrc"
            rm -rf "${target.dir}/usr/lib/apigee-microgateway/test-functional"

            mkdir -p "${target.dir}/usr/bin"
            ln -sf ../lib/apigee-microgateway/cli/edgemicro "${target.dir}/usr/bin/edgemicro"

            rm -rf /tmp/edgemicro-pack "$pack_tgz"
        - uses: dhi/build-pkg:0
          with:
            description: Apigee Edge Microgateway (edgemicro)
            files:
              - usr/lib/apigee-microgateway
              - usr/bin/edgemicro
              - usr/share/apigee-microgateway
            formats:
              - deb
            license: Apache-2.0
            name: apigee-microgateway
            rel: 3
            url: https://github.com/apigee-internal/microgateway
            version: 3.3.11
      paths:
        - type: file
          path: ${source.dir}/npm-patches.json
          uid: 0
          gid: 0
          source: package/main/apigee-microgateway/patch/npm-patches.json
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
