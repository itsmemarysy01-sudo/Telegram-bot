Source: nginx-stable
Section: httpd
Priority: optional
Maintainer: Docker Hardened Images <dhi@docker.com>
Build-Depends: debhelper-compat (= 13),
 cmake,
 libbrotli-dev,
 libpcre2-dev,
 libedit-dev,
 libgrpc++-dev,
 libssl-dev,
 libprotobuf-dev,
 libxml2-dev,
 libxslt1-dev,
 opentelemetry-cpp-dev,
 pkg-config,
 protobuf-compiler,
 protobuf-compiler-grpc,
 zlib1g-dev
Standards-Version: 4.7.0
Homepage: https://nginx.org

Package: nginx-stable
Architecture: any
Depends: ${shlibs:Depends}, ${misc:Depends}
Provides: nginx, httpd, httpd-cgi
Conflicts: nginx, nginx-mainline
Replaces: nginx, nginx-mainline
Description: high performance web server and reverse proxy (stable)
 nginx ("engine x") is a high performance web server, reverse proxy, and
 IMAP/POP3 proxy server. It is known for its stability, rich feature set,
 simple configuration, and low resource consumption.
 .
 This package is compiled from the nginx stable release branch, which is
 updated about once a year, plus backported critical bug fixes and security
 fixes from the mainline branch. The stable branch always carries an even
 minor version number (for example, 1.30.x).

Package: nginx-stable-module-njs
Architecture: any
Depends: nginx-stable (= ${binary:Version}), ${shlibs:Depends}, ${misc:Depends}
Description: njs scripting dynamic modules for nginx (stable)
 This package provides the njs dynamic modules (ngx_http_js_module and
 ngx_stream_js_module) for nginx. njs is a subset of the JavaScript language
 that allows extending nginx functionality in the http and stream contexts.
 .
 The modules are compiled against the matching nginx-stable build and must be
 loaded with the load_module directive.

Package: nginx-stable-module-otel
Architecture: any
Depends: nginx-stable (= ${binary:Version}), opentelemetry-cpp, ${shlibs:Depends}, ${misc:Depends}
Description: OpenTelemetry dynamic module for nginx (stable)
 This package provides the OpenTelemetry dynamic module (ngx_otel_module) for
 nginx. It enables distributed tracing of requests by exporting spans over
 OTLP/gRPC.
 .
 The module is compiled against the matching nginx-stable build and must be
 loaded with the load_module directive.

Package: nginx-stable-module-xslt
Architecture: any
Depends: nginx-stable (= ${binary:Version}), ${shlibs:Depends}, ${misc:Depends}
Description: XSLT dynamic module for nginx (stable)
 This package provides the XSLT dynamic module (ngx_http_xslt_filter_module)
 for nginx. It transforms XML responses using XSLT stylesheets.
 .
 The module is compiled against the matching nginx-stable build and must be
 loaded with the load_module directive.

Package: nginx-stable-module-image-filter
Architecture: any
Depends: nginx-stable (= ${binary:Version}), ${shlibs:Depends}, ${misc:Depends}
Description: image filter dynamic module for nginx (stable)
 This package provides the image filter dynamic module
 (ngx_http_image_filter_module) for nginx. It transforms JPEG, GIF, PNG, and
 WebP images.
 .
 The module is compiled against the matching nginx-stable build and must be
 loaded with the load_module directive.

Package: nginx-stable-module-brotli
Architecture: any
Depends: nginx-stable (= ${binary:Version}), ${shlibs:Depends}, ${misc:Depends}
Description: Brotli dynamic modules for nginx (stable)
 This package provides the Brotli dynamic modules
 (ngx_http_brotli_filter_module and ngx_http_brotli_static_module) for nginx.
 They compress responses on the fly and serve pre-compressed .br files.
 .
 The modules are compiled against the matching nginx-stable build and must be
 loaded with the load_module directive.

Package: nginx-stable-module-upload-progress
Architecture: any
Depends: nginx-stable (= ${binary:Version}), ${shlibs:Depends}, ${misc:Depends}
Description: upload progress dynamic module for nginx (stable)
 This package provides the upload progress dynamic module
 (ngx_http_uploadprogress_module) for nginx. It tracks the progress of in
 flight uploads and reports it over a JSON endpoint, which PHP applications
 such as Drupal use to render upload progress bars.
 .
 The module is compiled against the matching nginx-stable build and must be
 loaded with the load_module directive.

Package: nginx-stable-module-vts
Architecture: any
Depends: nginx-stable (= ${binary:Version}), ${shlibs:Depends}, ${misc:Depends}
Description: virtual host traffic status dynamic module for nginx (stable)
 This package provides the virtual host traffic status dynamic module
 (ngx_http_vhost_traffic_status_module) for nginx. It exposes per server zone,
 per upstream, and per cache traffic counters in HTML, JSON, and Prometheus
 formats.
 .
 The module is compiled against the matching nginx-stable build and must be
 loaded with the load_module directive.

Package: nginx-stable-module-perl
Architecture: any
Depends: nginx-stable (= ${binary:Version}), ${perl:Depends}, ${shlibs:Depends}, ${misc:Depends}
Description: Perl dynamic module for nginx (stable)
 This package provides the Perl dynamic module (ngx_http_perl_module) and its
 supporting Perl modules for nginx. It implements location and variable
 handlers in Perl and inserts Perl calls into SSI.
 .
 The module is compiled against the matching nginx-stable build and must be
 loaded with the load_module directive.
