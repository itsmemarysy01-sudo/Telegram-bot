# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-stunnel
variant: runtime
tags:
  - "5.80"
  - 5.80-r0
  - 5.80-debian13
  - 5.80-r0-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: 62ab5ccf33485b81bc4eaf0c76fd5558d2458e08
  PACKAGE_VERSION: 5.80.0
  REL: "0"
  VERSION: "5.80"
contents:
  packages:
    - base-files
  builds:
    - name: stunnel
      work-dir: ${source.dir}/stunnel-62ab5ccf33485b81bc4eaf0c76fd5558d2458e08
      contents:
        packages:
          - bash
          - build-essential
          - ca-certificates
          - coreutils
          - gcc
          - git
          - grep
          - libssl-dev
          - make
          - openssl
          - pkg-config
          - sed
        files:
          - url: git+https://github.com/mtrojnar/stunnel.git#stunnel-5.80
            checksum: 62ab5ccf33485b81bc4eaf0c76fd5558d2458e08
            path: ${source.dir}/stunnel-62ab5ccf33485b81bc4eaf0c76fd5558d2458e08
            spdx:
              name: stunnel
              version: "5.80"
              packages:
                - name: stunnel
                  purl: pkg:github/mtrojnar/stunnel@5.80
                  license: GPL-2.0-or-later
            uid: 0
            gid: 0
      pipeline:
        - name: configure and build
          runs: |
            set -eux -o pipefail

            shopt -s globstar nullglob
            touch aclocal.m4 configure **/*.in

            ./configure \
              --prefix=/usr \
              --sysconfdir=/etc \
              --disable-systemd \
              --disable-libwrap \
              --enable-fips

            make -j"$(nproc)"

            make install DESTDIR="${target.dir}"
        - uses: dhi/build-pkg:0
          with:
            depends:
              - libssl3t64
            description: Universal SSL/TLS tunneling proxy
            files:
              - usr/bin/stunnel
            formats:
              - deb
            license: GPL-2.0-or-later
            name: stunnel
            rel: 0
            url: https://www.stunnel.org/
            version: 5.80.0
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
