# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-emissary
variant: runtime
tags:
  - 3.10.0
  - 3.10.0-r6
  - 3.10.0-debian13
  - 3.10.0-r6-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2025-07-29"
vars:
  ARCH: '#{ target.arch == "amd64" ? "x86_64" : "aarch64" }'
  COMMIT_SHA: d25610acbea3cd0e57f924f9f6bd9df99a7e33a3
  GOLANG_VERSION: 1.26.8-0
  PACKAGE_HOME: /usr/lib/emissary
  PACKAGE_NAME: emissary-3.10
  PYTHON_MINOR: "3.11"
  PYTHON_VERSION: 3.11.16-1
  REL: "6"
  SEMVER_MAJOR_MINOR_VERSION: "3.10"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.10.0
  VERSION: 3.10.0
contents:
  packages:
    - base-files
  builds:
    - name: emissary
      work-dir: ${source.dir}/emissary
      environment:
        PATH: /go/bin:/usr/local/go/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
      caches:
        - path: /go/pkg/mod
      contents:
        repositories:
          - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
        keyring:
          - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
        packages:
          - bash
          - binutils
          - binutils-gold
          - build-essential
          - ca-certificates-bundle
          - cargo
          - coreutils
          - diffutils
          - findutils
          - gawk
          - gcc
          - golang-1.26=1.26.8-0
          - golang-1.26-oci-config=1.26.8-0
          - grep
          - jq
          - libcap-dev
          - libcap2-bin
          - libffi-dev
          - libncurses6
          - libpython-3.11-minimal=3.11.16-1
          - libpython-3.11-stdlib=3.11.16-1
          - libpython-3.11=3.11.16-1
          - libstdc++6
          - libyaml-dev
          - make
          - mawk
          - openssl
          - patchelf
          - python-3.11-minimal=3.11.16-1
          - python-3.11=3.11.16-1
          - python-3.11-dev=3.11.16-1
          - python3-pip
          - python3-pip-whl
          - python3-setuptools
          - python3-setuptools-whl
          - python3-wheel
          - rsync
          - sed
        files:
          - url: git+https://github.com/emissary-ingress/emissary.git#v3.10.0
            checksum: d25610acbea3cd0e57f924f9f6bd9df99a7e33a3
            path: ${source.dir}/emissary
            spdx:
              name: emissary
              version: 3.10.0
              packages:
                - name: emissary
                  purl: pkg:golang/github.com/emissary-ingress/emissary@3.10.0
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: set emissary version
          runs: |
            set -eux -o pipefail

            cat > ./python/ambassador.version << EOF
            3.10.0
            d25610acbea3cd0e57f924f9f6bd9df99a7e33a3
            EOF
        - name: update go dependencies (3.10 only)
          uses: go/bump@v1
          with:
            deps:
              - golang.org/x/net@v0.45.0
              - golang.org/x/oauth2@v0.27.0
              - github.com/google/cel-go@v0.30.0
              - github.com/klauspost/compress@v1.18.7
              - github.com/moby/spdystream@v0.5.1
              - go.opentelemetry.io/otel@v1.44.0
              - google.golang.org/grpc@v1.83.1
              - k8s.io/kubernetes@v1.32.10
            download: true
        - name: commit bumped go modules
          runs: |
            set -eux -o pipefail

            git config --global --add safe.directory "$(pwd)"
            git config user.email "dhi@docker.com"
            git config user.name "Docker Hardened Images"
            if ! git diff --quiet go.mod go.sum 2> /dev/null; then
              git add go.mod go.sum
              git commit -m "dhi: bump dependencies" --no-gpg-sign
            fi
        - name: retag for go module version
          runs: |
            set -eux -o pipefail

            git config --global --add safe.directory "$(pwd)"
            if [ "3" = "4" ]; then
              git tag -f "v3.3.10"
            else
              git tag -f "v3.10.0"
            fi
        - name: build apiext
          uses: go/build@v1
          with:
            cgo_enabled: 0
            output: ${target.dir}/usr/bin/apiext
            packages: ./cmd/apiext
        - name: build busyambassador
          uses: go/build@v1
          with:
            cgo_enabled: 0
            output: ${target.dir}/usr/bin/busyambassador
            packages: ./cmd/busyambassador
        - name: build wrapper
          uses: go/build@v1
          with:
            cgo_enabled: 0
            output: ${target.dir}/usr/bin/wrapper
            packages: ./cmd/capabilities_wrapper
        - name: set wrapper capabilities
          runs: |
            set -eux -o pipefail

            setcap cap_net_bind_service=p "${target.dir}/usr/bin/wrapper"
        - name: update python dependencies
          work-dir: ${source.dir}/emissary/python
          runs: |
            set -eux -o pipefail

            sed -i "s/^Version = .*/Version = \"3.10.0\"/" setup.py
            sed -i 's/setuptools==75\.8\.2/setuptools==79.0.1/' requirements.txt
            sed -i 's/python-json-logger==3\.2\.1/python-json-logger==3.3.0/' requirements.txt
            sed -i 's/urllib3==2\.3\.0/urllib3==2.7.0/' requirements.txt
            sed -i 's/requests==2\.32\.3/requests==2.33.0/' requirements.txt
            sed -i 's/orjson==3\.10\.15/orjson==3.11.6/' requirements.txt
            sed -i 's/flask==3\.1\.0/flask==3.1.3/' requirements.txt
            sed -i 's/werkzeug==3\.1\.3/werkzeug==3.1.6/' requirements.txt
            sed -i 's/idna==3\.10/idna==3.15/' requirements.txt
        - name: patch diagd (3.10 only)
          uses: git/patch@v1
          with:
            patch: ${source.dir}/patch/diagd.3.10.patch
        - name: install python requirements
          work-dir: ${source.dir}/emissary/python
          privileged: true
          runs: |
            set -eux -o pipefail

            python3 -m venv "${target.dir}/usr/lib/emissary"

            "${target.dir}/usr/lib/emissary/bin/python3" -m pip install --upgrade 'pip>=25.0'
            "${target.dir}/usr/lib/emissary/bin/python3" -m pip install --no-binary=pyyaml -r requirements.txt
            "${target.dir}/usr/lib/emissary/bin/python3" -m pip install --upgrade 'wheel>=0.46.2'
            "${target.dir}/usr/lib/emissary/bin/python3" -m pip install --upgrade 'msgpack>=1.2.1'
            find "${target.dir}/usr/lib/emissary/lib/python"*/site-packages/setuptools/_vendor -name 'wheel-0.45.*' -type d -exec rm -rf {} + 2> /dev/null || true
            "${target.dir}/usr/lib/emissary/bin/python3" setup.py install

            find "${target.dir}/usr/lib/emissary/bin" -type f -executable -exec sed -i "1s|^#!${target.dir}|#!|" {} +
        - name: clean python venv
          runs: |
            set -eux -o pipefail

            venv="${target.dir}/usr/lib/emissary"

            find "${venv}" \( -type d \( -name test -o -name tests -o -name __pycache__ \) \) -prune -exec rm -rf {} + || true
            find "${venv}" -type f \( -name '*.pyc' -o -name '*.pyo' \) -delete || true

            if [ -x "${venv}/bin/python3" ]; then
              "${venv}/bin/python3" -m pip uninstall -y pip setuptools wheel || true
            fi

            rm -f "${venv}/bin"/activate "${venv}/bin"/activate.csh "${venv}/bin"/activate.fish \
              "${venv}/bin"/Activate.ps1 "${venv}/bin"/deactivate \
              "${venv}/bin"/pip "${venv}/bin"/pip3 "${venv}/bin"/wheel "${venv}/bin"/wheel3
            find "${venv}/bin" -maxdepth 1 \( -name 'pip3.*' -o -name 'wheel3.*' \) -delete
        - name: stage ambassador version metadata
          runs: |
            set -eux -o pipefail

            install -D -m 0644 \
              "${source.dir}/emissary/python/ambassador.version" \
              "${target.dir}/usr/share/emissary/ambassador/python/ambassador.version"
        - name: stage usr bin symlinks for python tools
          runs: |
            set -eux -o pipefail

            ln -sf ../lib/emissary/bin/diagd "${target.dir}/usr/bin/diagd"
        - uses: dhi/build-pkg:0
          with:
            depends:
              - python-3.11
              - libcap2
            description: Emissary Ingress API gateway control plane
            files:
              - usr/lib/emissary
              - usr/bin/apiext
              - usr/bin/busyambassador
              - usr/bin/wrapper
              - usr/bin/diagd
              - usr/share/emissary
            formats:
              - deb
            license: Apache-2.0
            name: emissary-3.10
            rel: 6
            url: https://www.getambassador.io/
            version: 3.10.0
      paths:
        - type: directory
          path: ${source.dir}/patch
          uid: 0
          gid: 0
          source: package/main/emissary/patch
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
