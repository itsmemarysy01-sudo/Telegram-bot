Subject: Disable Spotless for the Query Insights plugin build
Upstream source: https://github.com/opensearch-project/query-insights/blob/3.6.0/build.gradle
DHI context: https://github.com/docker-hardened-images/definitions-external/tree/main/image/opensearch
Description: The DHI build patches plugin dependencies for CVE remediation.
  Disable Spotless formatting enforcement so patched generated dependency
  files do not fail the plugin build.

--- a/build.gradle
+++ b/build.gradle
@@ -50,7 +50,7 @@ buildscript {
 
 plugins {
   id "de.undercouch.download" version "5.3.0"
-  id 'com.diffplug.spotless' version '6.25.0'
+  id 'com.diffplug.spotless' version '6.25.0' // disabled for DHI build
   id 'checkstyle'
 }
 
@@ -152,19 +152,19 @@ checkstyle {
   configDirectory.set(rootProject.file("config/checkstyle/"))
 }
 
-spotless {
-  java {
-    target fileTree('.') {
-      include '**/*.java'
-      exclude '**/build/**', '**/build-*/**'
-    }
-    removeUnusedImports()
-    importOrder()
-    eclipse().configFile rootProject.file('config/formatterConfig.xml')
-    trimTrailingWhitespace()
-    endWithNewline()
-  }
-}
+// spotless {
+//   java {
+//     target fileTree('.') {
+//       include '**/*.java'
+//       exclude '**/build/**', '**/build-*/**'
+//     }
+//     removeUnusedImports()
+//     importOrder()
+//     eclipse().configFile rootProject.file('config/formatterConfig.xml')
+//     trimTrailingWhitespace()
+//     endWithNewline()
+//   }
+// }
 
 opensearchplugin {
     name pluginName
@@ -429,7 +429,7 @@ jacocoTestReport {
   }
 }
 
-check.dependsOn spotlessCheck
+// check.dependsOn spotlessCheck // disabled for DHI build
 check.dependsOn jacocoTestReport
 
 task integTestWithSecurity(type: RestIntegTestTask) {
