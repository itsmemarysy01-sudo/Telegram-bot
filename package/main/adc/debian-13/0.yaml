# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-adc
variant: runtime
tags:
  - 0.30.2
  - 0.30.2-r0
  - 0.30.2-debian13
  - 0.30.2-r0-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-06-29"
vars:
  COMMIT_SHA: 5f03a9634a397d50be1171524557c6025729d3ad
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "0.30"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.30.2
  VERSION: 0.30.2
contents:
  packages:
    - base-files
  builds:
    - name: adc
      work-dir: ${source.dir}/adc
      caches:
        - path: /root/.local/share/pnpm
        - path: /root/.cache
      contents:
        repositories:
          - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
        keyring:
          - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
        packages:
          - bash
          - base-files
          - ca-certificates
          - coreutils
          - findutils
          - git
          - libc6
          - libstdc++6
          - node-corepack
          - nodejs-24
          - npm
          - sed
          - yarn-classic
        files:
          - url: git+https://github.com/api7/adc.git#v0.30.2
            checksum: 5f03a9634a397d50be1171524557c6025729d3ad
            path: ${source.dir}/adc
            spdx:
              name: adc
              version: 0.30.2
              packages:
                - name: adc
                  purl: pkg:dhi/adc@0.30.2
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: build and stage payload
          privileged: true
          runs: |
            set -eux -o pipefail

            corepack enable pnpm

            corepack pnpm install nx -w
            corepack pnpm install
            NX_DAEMON=false NODE_ENV=production npx nx build cli

            install -D -m 0644 ./dist/apps/cli/main.cjs "${target.dir}/usr/share/adc/main.cjs"
        - uses: dhi/build-pkg:0
          with:
            description: API Declarative CLI for API7 Enterprise and Apache APISIX Admin APIs
            files:
              - usr/share/adc/main.cjs
            formats:
              - deb
            license: Apache-2.0
            name: adc
            rel: 0
            url: https://github.com/api7/adc
            version: 0.30.2
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
