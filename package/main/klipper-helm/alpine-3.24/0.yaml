# syntax=dhi.io/build:2-alpine3.24

image: dhi.io/pkg-klipper-helm
variant: runtime
tags:
  - 0.13.3-alpine3.24
  - 0.13.3-r2-alpine3.24
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-04-22"
vars:
  COMMIT_SHA: 09c52422fd0654369e3c24a2a971289d1bffd5cd
  GOLANG_MAJOR_MINOR_VERSION: "1.26"
  GOLANG_VERSION: 1.26.8-r0
  HELM_MAPKUBEAPIS_COMMIT_SHA: afa30453b58395ad8215cfe2c1a1edff50428baa
  HELM_MAPKUBEAPIS_VERSION: 0.6.1
  HELM_SET_STATUS_COMMIT_SHA: aa683c2b38a34bbd7261c5cd59e8d7c02e9d5c6e
  HELM_SET_STATUS_VERSION: 0.4.0
  PACKAGE_NAME: klipper-helm
  REL: "2"
  SEMVER_MAJOR_MINOR_VERSION: "0.13"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.13.3
  VERSION: 0.13.3-build20260820
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.24/main
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: klipper-helm
      work-dir: /go/src/github.com/k3s-io/klipper-helm
      caches:
        - path: /go/pkg/mod
      contents:
        packages:
          - busybox
          - bash
          - ca-certificates-bundle
          - coreutils
          - findutils
          - mawk
          - sed
          - golang-1.26=1.26.8-r0
          - golang-1.26-oci-config=1.26.8-r0
        files:
          - url: git+https://github.com/k3s-io/klipper-helm.git#v0.13.3-build20260820
            checksum: 09c52422fd0654369e3c24a2a971289d1bffd5cd
            path: /go/src/github.com/k3s-io/klipper-helm
            spdx:
              name: klipper-helm
              version: 0.13.3-build20260820
              packages:
                - name: klipper-helm
                  purl: pkg:github/k3s-io/klipper-helm@0.13.3-build20260820
                  license: Apache-2.0
            uid: 0
            gid: 0
          - url: git+https://github.com/k3s-io/helm-set-status.git#aa683c2b38a34bbd7261c5cd59e8d7c02e9d5c6e
            checksum: aa683c2b38a34bbd7261c5cd59e8d7c02e9d5c6e
            path: /go/src/github.com/k3s-io/helm-set-status
            spdx:
              name: helm-set-status
              version: 0.4.0
              packages:
                - name: helm-set-status
                  purl: pkg:golang/github.com/k3s-io/helm-set-status@0.4.0
                  license: Apache-2.0
            uid: 0
            gid: 0
          - url: git+https://github.com/helm/helm-mapkubeapis.git#v0.6.1
            checksum: afa30453b58395ad8215cfe2c1a1edff50428baa
            path: /go/src/github.com/helm/helm-mapkubeapis
            spdx:
              name: helm-mapkubeapis
              version: 0.6.1
              packages:
                - name: helm-mapkubeapis
                  purl: pkg:golang/github.com/helm/helm-mapkubeapis@0.6.1
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: update helm-set-status dependencies
          uses: go/bump@v2
          work-dir: /go/src/github.com/k3s-io/helm-set-status
          with:
            deps:
              - golang.org/x/crypto@v0.56.0
              - golang.org/x/mod@v0.40.0
              - google.golang.org/grpc@v1.79.3
              - oras.land/oras-go/v2@v2.6.2
            download: true
        - name: test helm-set-status
          work-dir: /go/src/github.com/k3s-io/helm-set-status
          privileged: true
          runs: |
            set -eux -o pipefail

            CGO_ENABLED=0 go test ./...
        - name: build helm-set-status
          uses: go/build@v1
          work-dir: /go/src/github.com/k3s-io/helm-set-status
          with:
            fips: false
            ldflags:
              - -X main.version=0.4.0
            output: ${target.dir}/usr/bin/helm-set-status
            packages: ./cmd/helm-set-status
        - name: build helm-set-status dev
          uses: go/build@v1
          work-dir: /go/src/github.com/k3s-io/helm-set-status
          with:
            fips: false
            ldflags:
              - -X main.version=0.4.0
            output: ${target.dir}/usr/bin/helm-set-status-dev
            packages: ./cmd/helm-set-status
            strip: false
        - name: stage helm-set-status plugin dir
          runs: |
            set -eux -o pipefail

            PLUGIN_DIR="${target.dir}/usr/share/klipper-helm/plugins/helm-set-status"
            mkdir -p "${PLUGIN_DIR}"
            install -m0644 "/go/src/github.com/k3s-io/helm-set-status/plugin.yaml" "${PLUGIN_DIR}/plugin.yaml"
            install -m0555 "${target.dir}/usr/bin/helm-set-status" "${PLUGIN_DIR}/helm-set-status"
        - name: update helm-mapkubeapis dependencies
          uses: go/bump@v2
          work-dir: /go/src/github.com/helm/helm-mapkubeapis
          with:
            deps:
              - golang.org/x/crypto@v0.56.0
              - golang.org/x/mod@v0.40.0
              - github.com/containerd/containerd@v1.7.33
              - helm.sh/helm/v3@v3.20.2
              - oras.land/oras-go/v2@v2.6.2
            download: true
        - name: test helm-mapkubeapis
          work-dir: /go/src/github.com/helm/helm-mapkubeapis
          privileged: true
          runs: |
            set -eux -o pipefail

            CGO_ENABLED=0 go test ./...
        - name: build mapkubeapis
          uses: go/build@v1
          work-dir: /go/src/github.com/helm/helm-mapkubeapis
          with:
            fips: false
            ldflags:
              - -X main.version=0.6.1
            output: ${target.dir}/usr/bin/mapkubeapis
            packages: ./cmd/mapkubeapis
        - name: build mapkubeapis dev
          uses: go/build@v1
          work-dir: /go/src/github.com/helm/helm-mapkubeapis
          with:
            fips: false
            ldflags:
              - -X main.version=0.6.1
            output: ${target.dir}/usr/bin/mapkubeapis-dev
            packages: ./cmd/mapkubeapis
            strip: false
        - name: stage helm-mapkubeapis plugin dir
          runs: |
            set -eux -o pipefail

            PLUGIN_DIR="${target.dir}/usr/share/klipper-helm/plugins/helm-mapkubeapis"
            mkdir -p "${PLUGIN_DIR}/config"
            install -m0644 "/go/src/github.com/helm/helm-mapkubeapis/plugin.yaml" "${PLUGIN_DIR}/plugin.yaml"
            mkdir -p "${PLUGIN_DIR}/bin"
            install -m0555 "${target.dir}/usr/bin/mapkubeapis" "${PLUGIN_DIR}/bin/mapkubeapis"
            install -m0644 "/go/src/github.com/helm/helm-mapkubeapis/config/Map.yaml" "${PLUGIN_DIR}/config/Map.yaml"
        - name: stage helm-set-status dev plugin dir
          runs: |
            set -eux -o pipefail

            PLUGIN_DIR="${target.dir}/usr/share/klipper-helm/plugins-dev/helm-set-status"
            mkdir -p "${PLUGIN_DIR}"
            install -m0644 "/go/src/github.com/k3s-io/helm-set-status/plugin.yaml" "${PLUGIN_DIR}/plugin.yaml"
            install -m0555 "${target.dir}/usr/bin/helm-set-status-dev" "${PLUGIN_DIR}/helm-set-status"
        - name: stage helm-mapkubeapis dev plugin dir
          runs: |
            set -eux -o pipefail

            PLUGIN_DIR="${target.dir}/usr/share/klipper-helm/plugins-dev/helm-mapkubeapis"
            mkdir -p "${PLUGIN_DIR}/config"
            install -m0644 "/go/src/github.com/helm/helm-mapkubeapis/plugin.yaml" "${PLUGIN_DIR}/plugin.yaml"
            mkdir -p "${PLUGIN_DIR}/bin"
            install -m0555 "${target.dir}/usr/bin/mapkubeapis-dev" "${PLUGIN_DIR}/bin/mapkubeapis"
            install -m0644 "/go/src/github.com/helm/helm-mapkubeapis/config/Map.yaml" "${PLUGIN_DIR}/config/Map.yaml"
        - name: stage entry
          runs: |
            set -eux -o pipefail

            install -m0555 "/go/src/github.com/k3s-io/klipper-helm/entry" "${target.dir}/usr/bin/entry"
        - uses: dhi/build-pkg:0
          with:
            depends:
              - bash
              - coreutils
              - grep
              - helm-4
              - jq
            description: Klipper Helm controller for k3s
            files:
              - usr/bin/entry
              - usr/bin/helm-set-status
              - usr/bin/mapkubeapis
              - usr/share/klipper-helm/plugins/helm-set-status
              - usr/share/klipper-helm/plugins/helm-mapkubeapis
            license: Apache-2.0
            name: klipper-helm
            rel: 2
            subpackages:
              - files:
                  - path: usr/bin/helm-set-status
                    source: usr/bin/helm-set-status-dev
                  - path: usr/bin/mapkubeapis
                    source: usr/bin/mapkubeapis-dev
                  - path: usr/share/klipper-helm/plugins/helm-set-status
                    source: usr/share/klipper-helm/plugins-dev/helm-set-status
                  - path: usr/share/klipper-helm/plugins/helm-mapkubeapis
                    source: usr/share/klipper-helm/plugins-dev/helm-mapkubeapis
                name: klipper-helm-dev
            url: https://github.com/k3s-io/klipper-helm
            version: 0.13.3-build20260820
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.24"
  pretty-name: Docker Hardened Images/Alpine Linux v3.24
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
