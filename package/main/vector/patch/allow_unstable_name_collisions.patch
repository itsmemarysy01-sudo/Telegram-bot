Allow unstable_name_collisions in vector-config for DHI rustc builds.
Upstream: https://github.com/vectordotdev/vector
Mirrors image/vector/patch/allow_unstable_name_collisions.patch for package-first build.

diff --git a/lib/vector-config/src/lib.rs b/lib/vector-config/src/lib.rs
--- a/lib/vector-config/src/lib.rs
+++ b/lib/vector-config/src/lib.rs
@@ -104,6 +104,7 @@
 // around existing/remote types for the purpose of making them `Configurable`.
 #![deny(warnings)]
+#![allow(unstable_name_collisions)]
 
 // Re-export of the various public dependencies required by the generated code to simplify the import requirements for
 // crates actually using the macros/derives.
