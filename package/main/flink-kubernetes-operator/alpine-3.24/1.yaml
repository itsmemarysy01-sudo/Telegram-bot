# syntax=dhi.io/build:2-alpine3.24

image: dhi.io/pkg-flink-kubernetes-operator
variant: runtime
tags:
  - 1.15.0-alpine3.24
  - 1.15.0-r15-alpine3.24
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-05-07"
vars:
  COMMIT_SHA: 79d730bab4d8403f3a447fb027f879f5cbdc59ad
  FLINK_HOME: /opt/flink
  MAVEN_REFERENCE: dhi/maven:3.9.16-jdk17-debian13-dev@sha256:6bd511e5ba7a082e6a4299bef7b35b57327a5a67dde8bece3b4f1dfc6ec77776
  MAVEN_VERSION: 3.9.16
  OPERATOR_HOME: /flink-kubernetes-operator
  PACKAGE_NAME: flink-kubernetes-operator
  REL: "15"
  SEMVER_MAJOR_MINOR_VERSION: "1.15"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.15.0
  VERSION: 1.15.0
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.24/main
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: flink-kubernetes-operator
      uses: dhi.io/maven:3.9.16-jdk17-debian13-dev@sha256:6bd511e5ba7a082e6a4299bef7b35b57327a5a67dde8bece3b4f1dfc6ec77776
      work-dir: ${source.dir}/flink-kubernetes-operator
      environment:
        JAVA_HOME: /usr/lib/jvm/temurin-17
        MAVEN_ARGS: --batch-mode --no-transfer-progress
        MAVEN_HOME: /opt/java/apache-maven-3.9.16
      caches:
        - path: /root/.m2/repository
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
        files:
          - url: git+https://github.com/apache/flink-kubernetes-operator.git#release-1.15.0
            checksum: 79d730bab4d8403f3a447fb027f879f5cbdc59ad
            path: ${source.dir}/flink-kubernetes-operator
            spdx:
              name: flink-kubernetes-operator
              version: 1.15.0
              packages:
                - name: flink-kubernetes-operator
                  purl: pkg:github/apache/flink-kubernetes-operator@release-1.15.0
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: install xq-python for mvn/patch
          privileged: true
          runs: |
            set -eux -o pipefail

            apt-get update -qq
            apt-get install -y -qq yq
            command -v xq-python
        - uses: mvn/mirror@v1
        - name: use maven central when mirror credentials are absent
          secrets:
            - id: maven_user
              env: MAVEN_USER
              optional: true
            - id: maven_password
              env: MAVEN_PASSWORD
              optional: true
          runs: |
            set -eux -o pipefail

            if [ -z "${MAVEN_USER:-}" ] || [ -z "${MAVEN_PASSWORD:-}" ]; then
              rm -f /root/.m2/settings.xml
            fi
        - name: patch maven dependencies
          uses: mvn/patch@v1
          secrets:
            - id: maven_user
              env: MAVEN_USER
              optional: true
            - id: maven_password
              env: MAVEN_PASSWORD
              optional: true
          with:
            dependencies: ${source.dir}/patch/mvn-dependencies.json
            dependencies-key: dependencyManagement.dependencies
            mvn-root: ${source.dir}/flink-kubernetes-operator
            properties: ${source.dir}/patch/mvn-properties.json
        - name: test
          privileged: true
          secrets:
            - id: maven_user
              env: MAVEN_USER
              optional: true
            - id: maven_password
              env: MAVEN_PASSWORD
              optional: true
          runs: |
            set -eux -o pipefail

            mvn test \
              -pl flink-kubernetes-standalone,flink-kubernetes-operator-api,flink-kubernetes-operator,flink-autoscaler,flink-kubernetes-webhook \
              -Dxml.skip=true \
              -Dfabric8.httpclient.impl=okhttp
        - name: build
          privileged: true
          secrets:
            - id: maven_user
              env: MAVEN_USER
              optional: true
            - id: maven_password
              env: MAVEN_PASSWORD
              optional: true
          runs: |
            set -eux -o pipefail

            mvn package \
              -pl flink-kubernetes-standalone,flink-kubernetes-operator-api,flink-kubernetes-operator,flink-autoscaler,flink-kubernetes-webhook \
              -DskipTests=true \
              -Dxml.skip=true \
              -Dfabric8.httpclient.impl=okhttp
        - name: install
          runs: |
            set -eux -o pipefail

            install -d "${target.dir}/flink-kubernetes-operator"
            install -d "${target.dir}/opt/flink/operator-lib"
            install -d "${target.dir}/opt/flink/log/log4j"
            install -d "${target.dir}/opt/flink/log/logback"
            install -d "${target.dir}/opt/flink/plugins"

            install -m 0644 \
              "flink-kubernetes-operator/target/flink-kubernetes-operator-1.15.0-shaded.jar" \
              "${target.dir}/flink-kubernetes-operator/flink-kubernetes-operator-1.15.0-shaded.jar"
            install -m 0644 \
              "flink-kubernetes-webhook/target/flink-kubernetes-webhook-1.15.0-shaded.jar" \
              "${target.dir}/flink-kubernetes-operator/flink-kubernetes-webhook-1.15.0-shaded.jar"
            install -m 0644 \
              "flink-kubernetes-standalone/target/flink-kubernetes-standalone-1.15.0.jar" \
              "${target.dir}/flink-kubernetes-operator/flink-kubernetes-standalone-1.15.0.jar"

            cp -a flink-kubernetes-operator/target/plugins/. "${target.dir}/opt/flink/plugins/"
            cp -a flink-kubernetes-operator/target/log4j/. "${target.dir}/opt/flink/log/log4j/"
            cp -a flink-kubernetes-operator/target/logback/. "${target.dir}/opt/flink/log/logback/"
        - name: maven sbom
          uses: sbom@v1
          with:
            extra-scanners: java-archive-cataloger
            prefix: flink-kubernetes-operator-maven
            source: ${target.dir}/flink-kubernetes-operator
        - uses: dhi/build-pkg:0
          privileged: true
          with:
            depends:
              - bash
              - ca-certificates
              - coreutils
            description: Apache Flink Kubernetes Operator
            files:
              - flink-kubernetes-operator
              - opt/flink
            license: Apache-2.0
            name: flink-kubernetes-operator
            rel: 15
            url: https://github.com/apache/flink-kubernetes-operator
            version: 1.15.0
      paths:
        - type: file
          path: ${source.dir}/patch/mvn-dependencies.json
          uid: 0
          gid: 0
          source: package/main/flink-kubernetes-operator/patch/mvn-dependencies.json
        - type: file
          path: ${source.dir}/patch/mvn-properties.json
          uid: 0
          gid: 0
          source: package/main/flink-kubernetes-operator/patch/mvn-properties.json
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.24"
  pretty-name: Docker Hardened Images/Alpine Linux v3.24
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
