# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-pgbouncer
variant: runtime
tags:
  - 1.25.2
  - 1.25.2-r0
  - 1.25.2-debian13
  - 1.25.2-r0-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: 515b6284d273bfd62d1b7e3931ccdce41f9bfe7b
  OPENSSL_FIPS_VERSION: 3.5.7-1~deb13u2+fips+dhi0
  PACKAGE_NAME: pgbouncer-1.25
  PGBOUNCER_TAG: pgbouncer_1_25_2
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "1.25"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.25.2
  VERSION: 1.25.2
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  packages:
    - base-files
  builds:
    - name: pgbouncer-1.25
      work-dir: ${source.dir}/pgbouncer
      contents:
        repositories:
          - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
        keyring:
          - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
        packages:
          - autoconf
          - automake
          - bash
          - build-essential
          - coreutils
          - gawk
          - gcc
          - grep
          - libc-ares-dev
          - libc6-dev
          - libevent-dev
          - libssl-dev
          - libtool
          - make
          - pkg-config
          - sed
          - tar
        files:
          - url: git+https://github.com/pgbouncer/pgbouncer.git#pgbouncer_1_25_2
            checksum: 515b6284d273bfd62d1b7e3931ccdce41f9bfe7b
            path: ${source.dir}/pgbouncer
            spdx:
              name: pgbouncer
              version: 1.25.2
              packages:
                - name: pgbouncer
                  purl: pkg:dhi/pgbouncer@1.25.2
                  license: ISC
            uid: 0
            gid: 0
      pipeline:
        - name: autogen
          runs: |
            set -eux -o pipefail

            ./autogen.sh
        - name: configure
          runs: |
            set -eux -o pipefail

            ./configure --prefix=/usr
        - name: build
          runs: |
            set -eux -o pipefail

            nproc="$(nproc)"
            make -j "$nproc" pgbouncer
        - name: install
          runs: |
            set -eux -o pipefail

            install -d "${target.dir}/usr/bin"
            install -m 0755 pgbouncer "${target.dir}/usr/bin/pgbouncer"

            install -d "${target.dir}/etc/pgbouncer"
            install -m 0644 etc/pgbouncer.ini "${target.dir}/etc/pgbouncer/pgbouncer.ini"
            install -m 0644 etc/userlist.txt "${target.dir}/etc/pgbouncer/userlist.txt"
        - uses: dhi/build-pkg:0
          with:
            depends:
              - libc-ares2
              - libcrypt1
              - libevent-2.1-7t64
              - libssl3t64
            description: Lightweight connection pooler for PostgreSQL
            files:
              - usr/bin/pgbouncer
              - etc/pgbouncer/pgbouncer.ini
              - etc/pgbouncer/userlist.txt
            formats:
              - deb
            license: ISC
            name: pgbouncer-1.25
            rel: 0
            url: https://www.pgbouncer.org/
            version: 1.25.2
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
