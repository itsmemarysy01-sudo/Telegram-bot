# syntax=dhi.io/build:2-alpine3.24

image: dhi.io/pkg-fetch-mcp
variant: runtime
tags:
  - 2026.8.31-alpine3.24
  - 2026.8.31-r2-alpine3.24
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: a40bc270fb5ece62673f8a1196f57116d885c5eb
  PYTHON_VERSION: 3.14.7-r1
  REL: "2"
  SEMVER_MAJOR_MINOR_VERSION: "2026.8"
  SEMVER_MAJOR_VERSION: "2026"
  SEMVER_VERSION: 2026.8.31
  UV_ARCH: '#{ target.arch == "amd64" ? "x86_64-unknown-linux-musl" : "aarch64-unknown-linux-musl" }'
  UV_VERSION: 0.12.10
  VERSION: 2026.8.31
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.24/main
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: fetch-mcp
      work-dir: ${source.dir}
      environment:
        UV_COMPILE_BYTECODE: "1"
      caches:
        - path: /root/.cache/uv
      contents:
        packages:
          - bash
          - build-base
          - ca-certificates-bundle
          - coreutils
          - findutils
          - git
          - libffi-dev
          - openssl-dev
          - sed
          - python-3.14
          - python-3.14-dev
        files:
          - url: https://github.com/astral-sh/uv/releases/download/0.12.10/uv-${UV_ARCH}.tar.gz
            checksum: https://github.com/astral-sh/uv/releases/download/0.12.10/uv-${UV_ARCH}.tar.gz.sha256
            path: ${source.dir}/uv.tar.gz
            uid: 0
            gid: 0
          - url: git+https://github.com/modelcontextprotocol/servers.git#2026.8.31
            checksum: a40bc270fb5ece62673f8a1196f57116d885c5eb
            path: ${source.dir}/servers
            spdx:
              name: fetch-mcp
              version: 2026.8.31
              packages:
                - name: fetch-mcp
                  purl: pkg:dhi/fetch-mcp@2026.8.31
                  license: MIT
            uid: 0
            gid: 0
      pipeline:
        - name: setup uv
          runs: |
            set -eux -o pipefail

            mkdir -p /usr/local/bin
            tar -xzf "${source.dir}/uv.tar.gz" -C /usr/local/bin --strip-components=1
            chmod +x /usr/local/bin/uv
        - name: apply CVE pins and stage fetch sources
          runs: |
            set -eux -o pipefail

            FETCH_SRC="${source.dir}/servers/src/fetch"

            sed -i '/"requests>=2\.32\.3",/a\"urllib3==2.7.0",' "${FETCH_SRC}/pyproject.toml"
            sed -i '/"urllib3==2\.7\.0",/a\"starlette>=1.3.1",' "${FETCH_SRC}/pyproject.toml"
            sed -i '/"starlette>=1\.3\.1",/a\"pydantic-settings>=2.14.2",' "${FETCH_SRC}/pyproject.toml"
            sed -i '/"pydantic-settings>=2\.14\.2",/a"mcp<2.0.0",' "${FETCH_SRC}/pyproject.toml"

            mkdir -p "${source.dir}/fetch"
            cp -a "${FETCH_SRC}/." "${source.dir}/fetch/"
        - name: build venv at final path and stage it
          privileged: true
          runs: |
            set -eux -o pipefail

            export SOURCE_DIR="${source.dir}" TARGET_DIR="${target.dir}" PYTHON_BIN=/usr/bin/python3.14
            bash "${source.dir}/build-venv.sh"
        - uses: dhi/build-pkg:0
          with:
            depends:
              - python-3.14
            description: Fetch MCP server for web content retrieval
            files:
              - usr/lib/fetch-mcp
              - usr/bin/mcp-server-fetch
            formats:
              - apk
            license: MIT
            name: fetch-mcp
            rel: 2
            url: https://github.com/modelcontextprotocol/servers/tree/main/src/fetch
            version: 2026.8.31
      paths:
        - type: file
          path: ${source.dir}/build-venv.sh
          uid: 0
          gid: 0
          mode: "0755"
          source: package/main/fetch-mcp/build-venv.sh
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.24"
  pretty-name: Docker Hardened Images/Alpine Linux v3.24
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
