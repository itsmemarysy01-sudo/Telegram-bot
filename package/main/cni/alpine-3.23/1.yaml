# syntax=dhi.io/build:2-alpine3.23

image: dhi.io/pkg-cni
variant: runtime
tags:
  - 1.9.1-alpine3.23
  - 1.9.1-r6-alpine3.23
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: adc3e6b5b581638afbd194cf2e9319ecbb0151a1
  GOLANG_VERSION: 1.26.8-r0
  PACKAGE_NAME: cni
  REL: "6"
  SEMVER_MAJOR_MINOR_VERSION: "1.9"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.9.1
  VERSION: 1.9.1
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: cni
      work-dir: /go/src/github.com/containernetworking/plugins
      contents:
        packages:
          - busybox
          - bash
          - ca-certificates-bundle
          - golang-1.26=1.26.8-r0
          - golang-1.26-oci-config=1.26.8-r0
        files:
          - url: git+https://github.com/containernetworking/plugins.git#v1.9.1
            checksum: adc3e6b5b581638afbd194cf2e9319ecbb0151a1
            path: /go/src/github.com/containernetworking/plugins
            spdx:
              name: cni_plugins
              version: 1.9.1
              packages:
                - name: cni_plugins
                  purl: pkg:golang/github.com/containernetworking/plugins@1.9.1
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: go dependencies
          uses: go/bump@v2
          with:
            deps:
              - go.opentelemetry.io/otel/sdk@v1.43.0
              - golang.org/x/crypto@v0.52.0
              - golang.org/x/net@v0.55.0
            download: true
            tidy: true
        - uses: go/hermetic@v1
        - name: build host-local
          uses: go/build@v1
          with:
            fips: false
            ldflags: []
            output: ${target.dir}/usr/bin/host-local
            packages: ./plugins/ipam/host-local
        - name: build loopback
          uses: go/build@v1
          with:
            fips: false
            ldflags: []
            output: ${target.dir}/usr/bin/loopback
            packages: ./plugins/main/loopback
        - name: build ptp
          uses: go/build@v1
          with:
            fips: false
            ldflags: []
            output: ${target.dir}/usr/bin/ptp
            packages: ./plugins/main/ptp
        - name: build portmap
          uses: go/build@v1
          with:
            fips: false
            ldflags: []
            output: ${target.dir}/usr/bin/portmap
            packages: ./plugins/meta/portmap
        - name: build host-local dev
          uses: go/build@v1
          with:
            fips: false
            ldflags: []
            output: ${target.dir}/usr/bin/host-local-dev
            packages: ./plugins/ipam/host-local
            strip: false
        - name: build loopback dev
          uses: go/build@v1
          with:
            fips: false
            ldflags: []
            output: ${target.dir}/usr/bin/loopback-dev
            packages: ./plugins/main/loopback
            strip: false
        - name: build ptp dev
          uses: go/build@v1
          with:
            fips: false
            ldflags: []
            output: ${target.dir}/usr/bin/ptp-dev
            packages: ./plugins/main/ptp
            strip: false
        - name: build portmap dev
          uses: go/build@v1
          with:
            fips: false
            ldflags: []
            output: ${target.dir}/usr/bin/portmap-dev
            packages: ./plugins/meta/portmap
            strip: false
        - uses: dhi/build-pkg:0
          with:
            description: Container Network Interface - networking for Linux containers
            files:
              - usr/bin/host-local
              - usr/bin/loopback
              - usr/bin/ptp
              - usr/bin/portmap
            license: Apache-2.0
            name: cni
            rel: 6
            subpackages:
              - files:
                  - path: usr/bin/host-local
                    source: usr/bin/host-local-dev
                  - path: usr/bin/loopback
                    source: usr/bin/loopback-dev
                  - path: usr/bin/ptp
                    source: usr/bin/ptp-dev
                  - path: usr/bin/portmap
                    source: usr/bin/portmap-dev
                name: cni-dev
            url: https://github.com/containernetworking/cni
            version: 1.9.1
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.23"
  pretty-name: Docker Hardened Images/Alpine Linux v3.23
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
