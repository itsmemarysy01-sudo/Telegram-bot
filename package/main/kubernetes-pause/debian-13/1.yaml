# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-kubernetes-pause
variant: runtime
tags:
  - 3.10.2
  - 3.10.2-r2
  - 3.10.2-debian13
  - 3.10.2-r2-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2024-05-23"
vars:
  COMMIT_SHA: f54c212e3a2f75d674b717a9b29052b20b60aefc
  KUBERNETES_VERSION: v1.37.0
  PACKAGE_NAME: kubernetes-pause
  PAUSE_SOURCE_SHA256: sha256:14a3fe21ab5f8e62937a2b453e76f3b94b0d3c7c04c3d09bc37195f54c1695ea
  REL: "2"
  SEMVER_MAJOR_MINOR_VERSION: "3.10"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.10.2
  VERSION: 3.10.2
contents:
  packages:
    - base-files
  builds:
    - name: kubernetes-pause
      work-dir: ${source.dir}
      contents:
        packages:
          - bash
          - build-essential
          - coreutils
          - musl-tools
        files:
          - url: https://raw.githubusercontent.com/kubernetes/kubernetes/f54c212e3a2f75d674b717a9b29052b20b60aefc/build/pause/linux/pause.c
            checksum: sha256:14a3fe21ab5f8e62937a2b453e76f3b94b0d3c7c04c3d09bc37195f54c1695ea
            path: ${source.dir}/pause.c
            spdx:
              name: pause
              version: 3.10.2
              packages:
                - name: pause
                  purl: pkg:github/kubernetes/kubernetes@f54c212e3a2f75d674b717a9b29052b20b60aefc
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: build pause
          runs: |
            set -eux -o pipefail

            mkdir -p "${target.dir}/usr/bin"

            musl-gcc \
              -Os \
              -Wall \
              -Werror \
              -static \
              -DVERSION=v3.10.2 \
              -o ${target.dir}/usr/bin/pause \
              ${source.dir}/pause.c

            strip ${target.dir}/usr/bin/pause
        - name: build pause dev
          runs: |
            set -eux -o pipefail

            mkdir -p "${target.dir}/usr/bin"

            musl-gcc \
              -Os \
              -Wall \
              -Werror \
              -static \
              -DVERSION=v3.10.2 \
              -o ${target.dir}/usr/bin/pause-dev \
              ${source.dir}/pause.c
        - uses: dhi/build-pkg:0
          with:
            description: Kubernetes pause container binary
            files:
              - usr/bin/pause
            formats:
              - deb
            license: Apache-2.0
            name: kubernetes-pause
            rel: 2
            subpackages:
              - files:
                  - path: usr/bin/pause
                    source: usr/bin/pause-dev
                name: kubernetes-pause-dev
            url: https://github.com/kubernetes/kubernetes/tree/master/build/pause
            version: 3.10.2
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
