# syntax=dhi.io/build:2-alpine3.24

image: dhi.io/pkg-memcached
variant: runtime
tags:
  - 1.6.45-alpine3.24
  - 1.6.45-r0-alpine3.24
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2020-03-08"
vars:
  CHECKSUM: 45038980ea7045a548b9b5b5125ef7116312a768
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "1.6"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.6.45
  VERSION: 1.6.45
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.24/main
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: memcached
      work-dir: ${source.dir}
      contents:
        packages:
          - busybox
          - ca-certificates-bundle
          - build-base
          - cyrus-sasl-dev
          - dpkg
          - dpkg-dev
          - libevent-dev
          - linux-headers
          - musl-utils
          - openssl-dev
          - perl
          - perl-io-socket-ssl
          - perl-utils
        files:
          - url: https://memcached.org/files/memcached-1.6.45.tar.gz
            path: ${source.dir}/memcached.tar.gz
            spdx:
              name: memcached
              version: 1.6.45
              packages:
                - name: memcached
                  purl: pkg:dhi/memcached@1.6.45
                  license: BSD-3-Clause
            uid: 0
            gid: 0
      pipeline:
        - name: build and install
          runs: |
            set -eux -o pipefail

            echo "45038980ea7045a548b9b5b5125ef7116312a768  memcached.tar.gz" | sha1sum -c -
            mkdir -p memcached
            tar -xzvf memcached.tar.gz -C memcached --strip-components=1
            cd memcached

            gnuArch="$(dpkg-architecture --query DEB_BUILD_GNU_TYPE)"
            ./configure \
              --build="$gnuArch" \
              --host="$gnuArch" \
              --target="$gnuArch" \
              --enable-extstore \
              --enable-sasl \
              --enable-sasl-pwdb \
              --enable-tls \
              --prefix="${target.dir}/usr"

            ncpus=$(getconf _NPROCESSORS_ONLN)
            ncpus=$((ncpus > 6 ? 6 : ncpus))

            make -j"$ncpus"
            make install
        - uses: dhi/build-pkg:0
          with:
            depends:
              - libevent
              - libsasl
              - libssl3
            description: High-performance memory object caching system
            files:
              - usr/bin/memcached
            formats:
              - apk
            license: BSD-3-Clause
            name: memcached
            rel: 0
            url: https://memcached.org/
            version: 1.6.45
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.24"
  pretty-name: Docker Hardened Images/Alpine Linux v3.24
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
