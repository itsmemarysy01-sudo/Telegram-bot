# Full transitive lock for the searxng venv, installed with --no-deps on both
# leaves so deb and apk ship the same dependency set and rebuilds are
# reproducible (upstream pins only its direct deps; the transitive closure
# would otherwise float at build time).
#
# Seeded from the resolved and tested build of upstream commit b023a28ba
# (requirements.txt + requirements-server.txt with all extras). Regenerate
# whenever upstream's requirements*.txt change at the tracked commit:
#
#   uv pip compile requirements.txt requirements-server.txt \
#     --python-version 3.13 -o requirements-lock.txt
#
# A CVE fix in any dependency — direct or transitive — lands here as an
# explicit version bump on the affected line, with the CVE noted next to it.
# Editing this lock changes no tracked variable, so bump REL by hand in both
# leaves whenever the lock changes without a VERSION change.
annotated-doc==0.0.5
anyio==4.14.2
async-timeout==5.0.1
babel==2.18.0
blinker==1.9.0
certifi==2026.7.22
click==8.4.2
flask==3.1.3
flask-babel==4.0.0
granian[pname]==2.7.9
h11==0.16.0
h2==4.4.1
hpack==4.2.0
httpcore==1.0.9
httpx[http2]==0.28.1
httpx-socks[asyncio]==0.10.0
hyperframe==6.1.0
idna==3.18
isodate==0.7.2
itsdangerous==2.2.0
jinja2==3.1.6
lxml==6.1.1
markdown-it-py==4.2.0
markupsafe==3.0.3
mdurl==0.1.2
msgspec==0.21.1
pygments==2.20.0
python-dateutil==2.9.0.post0
python-socks==2.8.2
pytz==2026.3.post1
pyyaml==6.0.3
rich==15.0.0
setproctitle==1.3.7
shellingham==1.5.4
six==1.17.0
sniffio==1.3.1
typer==0.27.0
typing-extensions==4.16.0
valkey==6.1.1
werkzeug==3.1.8
whitenoise==6.12.0
