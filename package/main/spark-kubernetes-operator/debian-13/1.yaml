# syntax=dhi.io/build:2-debian13

image: dhi.io/pkg-spark-kubernetes-operator
variant: runtime
tags:
  - 1.0.0
  - 1.0.0-r10
  - 1.0.0-debian13
  - 1.0.0-r10-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-07-26"
vars:
  COMMIT_SHA: d8b19438bd96c6c2f52ee58425ec192b6cc825fc
  JAVA_REFERENCE: dhi/eclipse-temurin:26.0.2.10-jdk-debian13-dev@sha256:8a31412d1e0c2730074be6e81af789d44baa5963ca1fd706c700836c35b03d04
  OPERATOR_HOME: /usr/share/spark-kubernetes-operator
  PACKAGE_NAME: spark-kubernetes-operator-1.0
  REL: "10"
  SEMVER_MAJOR_MINOR_VERSION: "1.0"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.0.0
  VERSION: 1.0.0
contents:
  packages:
    - base-files
  builds:
    - name: spark-kubernetes-operator-1.0
      uses: dhi.io/eclipse-temurin:26.0.2.10-jdk-debian13-dev@sha256:8a31412d1e0c2730074be6e81af789d44baa5963ca1fd706c700836c35b03d04
      work-dir: ${source.dir}/spark-kubernetes-operator
      environment:
        GRADLE_USER_HOME: /root/.gradle
        JAVA_HOME: /opt/java/openjdk/26
        JDK_JAVA_OPTIONS: -XX:+UseCompactObjectHeaders
      caches:
        - path: /root/.gradle/caches
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - findutils
        files:
          - url: git+https://github.com/apache/spark-kubernetes-operator.git#1.0.0
            checksum: d8b19438bd96c6c2f52ee58425ec192b6cc825fc
            path: ${source.dir}/spark-kubernetes-operator
            spdx:
              name: spark-kubernetes-operator
              version: 1.0.0
              packages:
                - name: spark-kubernetes-operator
                  purl: pkg:github/apache/spark-kubernetes-operator@1.0.0
                  license: Apache-2.0
            uid: 0
            gid: 0
          - url: https://github.com/gradle/gradle/raw/v9.6.1/gradle/wrapper/gradle-wrapper.jar
            checksum: sha256:497c8c2a7e5031f6aa847f88104aa80a93532ec32ee17bdb8d1d2f67a194a9c7
            path: ${source.dir}/spark-kubernetes-operator/gradle/wrapper/gradle-wrapper.jar
            uid: 0
            gid: 0
      pipeline:
        - name: patch buildscript maven mirror
          uses: git/patch@v1
          with:
            patch: ${source.dir}/patch/buildscript-maven-mirror.patch
        - name: build
          privileged: true
          runs: |
            set -eux -o pipefail

            ./gradlew :spark-operator:shadowJar -x check --no-daemon
        - name: install
          runs: |
            set -eux -o pipefail

            install -d "${target.dir}/usr/share/spark-kubernetes-operator"
            install -m 0644 \
              "spark-operator/build/libs/spark-kubernetes-operator-1.0.0-all.jar" \
              "${target.dir}/usr/share/spark-kubernetes-operator/spark-kubernetes-operator.jar"
        - name: java sbom
          uses: sbom@v1
          with:
            extra-scanners: java-archive-cataloger
            prefix: spark-kubernetes-operator
            source: ${target.dir}/usr/share/spark-kubernetes-operator
        - uses: dhi/build-pkg:0
          with:
            description: Apache Spark Kubernetes Operator
            files:
              - usr/share/spark-kubernetes-operator
            formats:
              - deb
            license: Apache-2.0
            name: spark-kubernetes-operator-1.0
            rel: 10
            url: https://github.com/apache/spark-kubernetes-operator
            version: 1.0.0
      paths:
        - type: file
          path: ${source.dir}/patch/buildscript-maven-mirror.patch
          uid: 0
          gid: 0
          mode: "0444"
          source: package/main/spark-kubernetes-operator/patch/buildscript-maven-mirror.patch
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
