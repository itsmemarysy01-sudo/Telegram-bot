# syntax=dhi.io/build:2-alpine3.24

image: dhi.io/pkg-gitaly-backup
variant: runtime
tags:
  - 19.1.7-alpine3.24
  - 19.1.7-r5-alpine3.24
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: 1847bc6d43152b9d7bfebec1034b56f3671a322b
  GOLANG_VERSION: 1.26.8-r0
  PACKAGE_NAME: gitaly-backup-19.1
  REL: "5"
  SEMVER_MAJOR_MINOR_VERSION: "19.1"
  SEMVER_MAJOR_VERSION: "19"
  SEMVER_VERSION: 19.1.7
  VERSION: 19.1.7
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.24/main
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: gitaly-backup-19.1
      work-dir: /go/src/gitlab.com/gitlab-org/gitaly
      contents:
        packages:
          - busybox
          - bash
          - ca-certificates-bundle
          - golang-1.26=1.26.8-r0
          - golang-1.26-oci-config=1.26.8-r0
        files:
          - url: git+https://gitlab.com/gitlab-org/gitaly.git#v19.1.7
            checksum: 1847bc6d43152b9d7bfebec1034b56f3671a322b
            path: /go/src/gitlab.com/gitlab-org/gitaly
            spdx:
              name: gitaly-backup
              version: 19.1.7
              packages:
                - name: gitaly-backup
                  purl: pkg:golang/gitlab.com/gitlab-org/gitaly/v18@19.1.7
                  license: MIT
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v2
          with:
            deps:
              - github.com/cilium/ebpf@v0.22.0
              - github.com/aws/aws-sdk-go-v2/aws/protocol/eventstream@v1.7.11
              - github.com/aws/aws-sdk-go-v2/service/s3@v1.102.2
              - github.com/go-jose/go-jose/v4@v4.1.4
              - github.com/klauspost/compress@v1.18.7
              - go.opentelemetry.io/otel@v1.45.0
              - go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracehttp@v1.45.0
              - golang.org/x/crypto@v0.56.0
              - golang.org/x/net@v0.58.0
              - golang.org/x/sys@v0.47.0
              - golang.org/x/text@v0.41.0
            download: true
        - uses: go/hermetic@v1
        - name: test
          runs: |
            set -eux -o pipefail

            MOD=$(go list -m)

            PKGS="internal/archive
            internal/backoff
            internal/grpc/backchannel
            internal/grpc/metadata
            internal/grpc/protoregistry
            internal/helper/duration
            internal/helper/text
            internal/log
            internal/structerr
            streamio"

            TESTED=""
            for p in ${PKGS}; do
              TESTED="${TESTED} ${MOD}/${p}"
            done

            CGO_ENABLED=0 go test -short -count=1 ${TESTED}

            CLOSURE=$(go list -deps ./cmd/gitaly-backup | grep -c "^${MOD}/")
            echo "DHI-GO-007 coverage: tested $(echo ${PKGS} | wc -w) of ${CLOSURE} in-repo packages in gitaly-backup's dependency closure; the remainder require the bundled Git environment, assert non-root filesystem behaviour, or pin an otel semconv older than the CVE bump"
        - name: build
          uses: go/build@v1
          with:
            cgo_enabled: 0
            fips: false
            ldflags:
              - -X gitlab.com/gitlab-org/gitaly/v18/internal/version.version=19.1.7
            output: ${target.dir}/usr/bin/gitaly-backup
            packages: ./cmd/gitaly-backup
            strip: false
        - name: verify version injection
          runs: |
            set -eux -o pipefail

            BIN=${target.dir}/usr/bin/gitaly-backup
            test -s "${BIN}"

            go version -m "${BIN}" | grep -F -- \
              "-X gitlab.com/gitlab-org/gitaly/v18/internal/version.version=19.1.7"
        - uses: dhi/build-pkg:0
          with:
            description: Gitaly backup and restore client used by GitLab backup tooling
            files:
              - usr/bin/gitaly-backup
            license: MIT
            name: gitaly-backup-19.1
            rel: 5
            url: https://gitlab.com/gitlab-org/gitaly
            version: 19.1.7
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.24"
  pretty-name: Docker Hardened Images/Alpine Linux v3.24
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
