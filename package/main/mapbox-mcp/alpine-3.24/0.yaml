# syntax=dhi.io/build:2-alpine3.24

image: dhi.io/pkg-mapbox-mcp
variant: runtime
tags:
  - 0.14.0-alpine3.24
  - 0.14.0-r3-alpine3.24
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-06-15"
vars:
  COMMIT_SHA: 8d26d7636a30dd0355db18bb2c64f3bc7112b41c
  NODEJS_VERSION: 24.20.0-r0
  REL: "3"
  SEMVER_MAJOR_MINOR_VERSION: "0.14"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.14.0
  VERSION: 0.14.0
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.24/main
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: mapbox-mcp
      work-dir: ${source.dir}/mapbox-mcp
      caches:
        - path: /root/.npm
      contents:
        packages:
          - bash
          - build-base
          - ca-certificates-bundle
          - coreutils
          - findutils
          - git
          - libstdc++
          - mawk
          - nodejs-24=24.20.0-r0
          - npm
          - python3
          - sed
        files:
          - url: git+https://github.com/mapbox/mcp-server.git#v0.14.0
            checksum: 8d26d7636a30dd0355db18bb2c64f3bc7112b41c
            path: ${source.dir}/mapbox-mcp
            spdx:
              name: mapbox-mcp
              version: 0.14.0
              packages:
                - name: mapbox-mcp
                  purl: pkg:npm/%40mapbox/mcp-server@0.14.0
                  license: MIT
            uid: 0
            gid: 0
      pipeline:
        - name: bump dependencies
          uses: npm/bump@v1
          privileged: true
          environment:
            DHI_NO_SFW: "true"
          with:
            bumps: ${source.dir}/patch/npm-bump.json
            npm-root: ${source.dir}/mapbox-mcp
        - name: install and build
          privileged: true
          runs: |
            set -eux -o pipefail

            cd "${source.dir}/mapbox-mcp"

            npm pkg set \
              "overrides.protobufjs@7"="7.6.5" \
              "overrides.protobufjs@8"="8.6.6"
            npm install --package-lock-only --ignore-scripts

            npm ci --ignore-scripts

            ./node_modules/.bin/tshy

            rm -rf node_modules
            npm ci --omit=dev --ignore-scripts
            ./node_modules/.bin/patch-package

            mkdir -p "${target.dir}/usr/lib/mapbox-mcp"
            cp -a dist node_modules package.json "${target.dir}/usr/lib/mapbox-mcp/"
        - uses: dhi/build-pkg:0
          with:
            description: Mapbox MCP server - Model Context Protocol server for Mapbox geospatial APIs
            files:
              - usr/lib/mapbox-mcp
            formats:
              - apk
            license: MIT
            name: mapbox-mcp
            rel: 3
            url: https://github.com/mapbox/mcp-server
            version: 0.14.0
      paths:
        - type: file
          path: ${source.dir}/patch/npm-bump.json
          uid: 0
          gid: 0
          source: package/main/mapbox-mcp/patch/npm-bump.json
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.24"
  pretty-name: Docker Hardened Images/Alpine Linux v3.24
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
