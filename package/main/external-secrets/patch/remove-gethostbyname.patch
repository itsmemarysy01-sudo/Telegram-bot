Backport upstream PR #6164 for CVE-2026-34984.
Remove getHostByName from the Sprig template function map to prevent
DNS-based secret exfiltration through user-controlled templates.
https://github.com/external-secrets/external-secrets/pull/6164
diff --git a/runtime/template/v2/template.go b/runtime/template/v2/template.go
index 1f4d17f7f..c90079f81 100644
--- a/runtime/template/v2/template.go
+++ b/runtime/template/v2/template.go
@@ -87,6 +87,7 @@ func init() {
 	sprigFuncs := sprig.TxtFuncMap()
 	delete(sprigFuncs, "env")
 	delete(sprigFuncs, "expandenv")
+	delete(sprigFuncs, "getHostByName")
 
 	for k, v := range sprigFuncs {
 		tplFuncs[k] = v
diff --git a/runtime/template/v2/template_test.go b/runtime/template/v2/template_test.go
index d7b01c7e5..38efa0a82 100644
--- a/runtime/template/v2/template_test.go
+++ b/runtime/template/v2/template_test.go
@@ -222,6 +222,12 @@ func rsaEncryptOAEP(t testing.TB, publicKeyPEM []byte, hash, plaintext string) [
 	return ciphertext
 }
 
+func TestFuncMapDoesNotExposeGetHostByName(t *testing.T) {
+	if _, ok := FuncMap()["getHostByName"]; ok {
+		t.Fatalf("getHostByName should not be exposed in the template function map")
+	}
+}
+
 func TestExecute(t *testing.T) {
 	tbl := []struct {
 		name                string
