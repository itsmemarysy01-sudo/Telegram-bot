# syntax=dhi.io/build:2-alpine3.23

image: dhi.io/pkg-php
variant: runtime
tags:
  - 8.2.33-apk-alpine3.24
  - 8.2.33-r0-apk-alpine3.24
platforms:
  - linux/amd64
  - linux/arm64
vars:
  CHECKSUM: fbdeace9b38220436a4c8fd79b900df92878151db145e641750743a283b514c1
  OPCACHE_BUILTIN: "false"
  PACKAGE_NAME: php-8.2
  PHP_SYSCONFDIR_REL: etc/php-8.2
  PRIORITY: "82"
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "8.2"
  SEMVER_MAJOR_VERSION: "8"
  SEMVER_VERSION: 8.2.33
  VERSION: 8.2.33
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.24/main
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: php
      work-dir: ${source.dir}/php-8.2.33
      shell: bash
      environment:
        CFLAGS: -fstack-protector-strong -fpic -fpie -O2 -D_LARGEFILE_SOURCE -D_FILE_OFFSET_BITS=64
        CPPFLAGS: -fstack-protector-strong -fpic -fpie -O2 -D_LARGEFILE_SOURCE -D_FILE_OFFSET_BITS=64
        LDFLAGS: -Wl,-O1 -pie
        PHP_BUILD_PROVIDER: https://github.com/docker-hardened-images/definitions
        PHP_UNAME: Linux - Docker Hardened Images
      contents:
        packages:
          - argon2-dev
          - autoconf
          - bash
          - bison
          - curl-dev
          - file
          - g++
          - gcc
          - gnu-libiconv-dev
          - icu-dev
          - libc-dev
          - libsodium-dev
          - libxml2-dev
          - linux-headers
          - make
          - musl-utils
          - oniguruma-dev
          - openssl-dev
          - patch
          - pcre2-dev
          - pkgconf
          - re2c
          - readline-dev
          - sqlite-dev
          - xz
          - zlib-dev
        files:
          - url: https://www.php.net/distributions/php-8.2.33.tar.xz.asc
            path: ${source.dir}/php-8.2.33.tar.xz.asc
            uid: 0
            gid: 0
          - url: https://www.php.net/distributions/php-8.2.33.tar.xz
            checksum: sha256:fbdeace9b38220436a4c8fd79b900df92878151db145e641750743a283b514c1
            path: ${source.dir}/php-8.2.33.tar.xz
            spdx:
              name: php
              version: 8.2.33
              packages:
                - name: php
                  purl: pkg:dhi/php@8.2.33
                  license: PHP-3.01
            uid: 0
            gid: 0
      pipeline:
        - name: verify php-8.2.33.tar.xz
          uses: gpg/verify@v1
          work-dir: ${source.dir}
          with:
            file: ${source.dir}/php-8.2.33.tar.xz
            keys: ${source.dir}/keyring.asc
            signature: ${source.dir}/php-8.2.33.tar.xz.asc
        - name: extract source
          work-dir: ${source.dir}
          runs: |
            set -eux -o pipefail

            tar xf 'php-8.2.33.tar.xz'

            # Copy source to /usr/include/php
            mkdir -p '${target.dir}/usr/include/php/src'
            cp -a 'php-8.2.33/.' '${target.dir}/usr/include/php/src'
        - name: configure
          runs: |
            set -eux -o pipefail

            ./configure \
              --enable-option-checking=fatal \
              --prefix=/usr \
              --sysconfdir='/etc/php-8.2' \
              --datadir='/usr/share/php-8.2' \
              --localstatedir=/var \
              --mandir=/usr/share/man \
              --with-config-file-path='/etc/php-8.2' \
              --with-config-file-scan-dir='/etc/php-8.2/conf.d' \
              --program-suffix='-8.2' \
              --disable-static \
              --disable-cgi \
              --enable-filter \
              --enable-tokenizer \
              --enable-ctype \
              --enable-fileinfo \
              --enable-mbstring --enable-mbregex \
              --enable-intl \
              --enable-mysqlnd \
              --enable-session \
              --enable-xml --enable-simplexml --enable-dom \
              --enable-pdo \
              --with-sqlite3 \
              --with-pdo-sqlite \
              --with-password-argon2 \
              --with-sodium \
              --with-openssl \
              --with-external-pcre=/usr --with-pcre-jit \
              --with-iconv \
              --with-libxml \
              --with-curl \
              --with-readline \
              --with-zlib \
              --with-pear \
              --enable-fpm --with-fpm-user=nonroot --with-fpm-group=nonroot \
              --enable-phpdbg --enable-phpdbg-readline
        - name: build
          runs: |
            set -eux -o pipefail

            make -j"$(nproc)"
        - name: install
          runs: |
            set -eux -o pipefail

            make install INSTALL_ROOT='${target.dir}'

            # Older PHP releases don't apply --program-suffix to php-fpm, but
            # current ones do. Rename only if the unsuffixed binary exists.
            if [ -f '${target.dir}/usr/sbin/php-fpm' ]; then
              mv '${target.dir}/usr/sbin/php-fpm' \
                '${target.dir}/usr/sbin/php-fpm-8.2'
            fi

            # Install php.ini-production as the active php.ini and ship the
            # samples for ops to reference.
            install -d '${target.dir}/etc/php-8.2/conf.d'
            install -m 0644 'php.ini-production' \
              '${target.dir}/etc/php-8.2/php.ini'
            install -m 0644 -t '${target.dir}/etc/php-8.2' \
              php.ini-development php.ini-production

            # OPcache lives in the main package; ship the ini fragment that
            # loads it for versions where it's a separate .so (8.5+ has it
            # built-in and OPCACHE_BUILTIN=true skips this).
            if [ 'false' != 'true' ]; then
              {
                echo '; https://wiki.php.net/rfc/make_opcache_required'
                echo 'zend_extension=opcache'
              } > '${target.dir}/etc/php-8.2/conf.d/opcache.ini'
            fi

            install -Dm 0644 LICENSE \
              '${target.dir}/usr/share/licenses/php-8.2/LICENSE'

            # abuild's doc subpackage postcheck rejects uncompressed man pages.
            find '${target.dir}/usr/share/man' -type f ! -name '*.gz' \
              -exec gzip -9n {} +

            # Create unversioned symlinks
            mkdir -p '${target.dir}/usr/bin' '${target.dir}/usr/sbin'
            ln -s phar-8.2 '${target.dir}/usr/bin/phar'
            ln -s php-8.2 '${target.dir}/usr/bin/php'
            ln -s phpize-8.2 '${target.dir}/usr/bin/phpize'
            ln -s php-config-8.2 '${target.dir}/usr/bin/php-config'
            ln -s phpdbg-8.2 '${target.dir}/usr/bin/phpdbg'
            ln -s php-fpm-8.2 '${target.dir}/usr/sbin/php-fpm'
        - uses: abuild/setup@v1
          work-dir: ${source.dir}/aports/php-8.2
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/php-8.2
          with:
            depends: ca-certificates-bundle
            description: PHP 8.2 interpreter
            functions:
              dev: |
                pkgdesc="Headers and tools to build extensions for PHP 8.2"
                depends="$pkgname=$pkgver-r$pkgrel"
                provides="php-dev=8.2.33-r0"
                provider_priority="82"
                replaces="php-dev=8.2.33-r0"
                replaces_priority="82"

                amove usr/include
                amove usr/lib/php/build
                amove usr/bin/phpize*
                amove usr/bin/php-config*
                amove usr/bin/pear
                amove usr/bin/peardev
                amove usr/bin/pecl
              doc: |
                pkgdesc="Documentation for PHP 8.2"
                depends=""
                provides="php-doc=8.2.33-r0"
                provider_priority="82"
                replaces="php-doc=8.2.33-r0"
                replaces_priority="82"

                amove usr/share/man
                amove usr/share/licenses
              fpm: |
                pkgdesc="FastCGI Process Manager for PHP 8.2"
                depends="$pkgname=$pkgver-r$pkgrel"
                provides="php-fpm=8.2.33-r0"
                provider_priority="82"
                replaces="php-fpm=8.2.33-r0"
                replaces_priority="82"

                amove usr/sbin/php-fpm*

                amove etc/php-8.2/php-fpm.conf.default
                amove etc/php-8.2/php-fpm.d
              phpdbg: |
                pkgdesc="Interactive PHP debugger for PHP 8.2"
                depends="$pkgname=$pkgver-r$pkgrel"
                provides="php-phpdbg=8.2.33-r0"
                provider_priority="82"
                replaces="php-phpdbg=8.2.33-r0"
                replaces_priority="82"

                amove usr/bin/phpdbg*
            license: PHP-3.01
            name: php-8.2
            provider_priority: 82
            provides: php=8.2.33-r0
            rel: 0
            replaces: php
            replaces_priority: 82
            subpackages: $pkgname-fpm $pkgname-phpdbg $pkgname-dev $pkgname-doc::noarch
            url: https://www.php.net/
            version: 8.2.33
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/php-8.2
      paths:
        - type: file
          path: ${source.dir}/keyring.asc
          uid: 0
          gid: 0
          source: package/apk/main/php/key/php8.2.keyring.asc
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.24"
  pretty-name: Docker Hardened Images/Alpine Linux v3.24
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
