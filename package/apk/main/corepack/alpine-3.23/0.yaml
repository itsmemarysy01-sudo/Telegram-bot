# syntax=dhi.io/build:2-alpine3.23

image: dhi.io/pkg-corepack
variant: runtime
tags:
  - 0.36.0-alpine3.23
  - 0.36.0-r0-alpine3.23
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: 7a4c30b1aecece619a3e01db757f0eb947414b1f
  PACKAGE_NAME: corepack
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "0.36"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.36.0
  VERSION: 0.36.0
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: abuild
      contents:
        packages:
          - nodejs
          - npm
          - python3
          - yarn-berry
        files:
          - url: git+https://github.com/nodejs/corepack.git#v0.36.0
            path: ${source.dir}/corepack
            uid: 0
            gid: 0
      pipeline:
        - name: yarn install
          work-dir: ${source.dir}/corepack
          privileged: true
          runs: |
            set -eux -o pipefail

            yarn install
        - name: yarn build
          work-dir: ${source.dir}/corepack
          runs: |
            set -eux -o pipefail

            yarn build
        - name: stage
          work-dir: ${source.dir}/corepack
          runs: |
            set -eux -o pipefail

            mkdir -p "${target.dir}/usr/share/node_modules/corepack"

            # Copy the built directory into the destination directory
            cp -r dist "${target.dir}/usr/share/node_modules/corepack/dist"
            cp -r shims "${target.dir}/usr/share/node_modules/corepack/shims"
            cp package.json "${target.dir}/usr/share/node_modules/corepack/package.json"

            # Remove windows specific files
            rm "${target.dir}"/usr/share/node_modules/corepack/shims/*.cmd
            rm "${target.dir}"/usr/share/node_modules/corepack/shims/*.ps1
            rm -rf "${target.dir}/usr/share/node_modules/corepack/shims/nodewin"

            # Create a symlink for corepack in /usr/bin
            mkdir -p "${target.dir}/usr/bin"
            ln -sf "/usr/share/node_modules/corepack/dist/corepack.js" "${target.dir}/usr/bin/corepack"
        - uses: abuild/setup@v1
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/corepack
          with:
            depends: nodejs
            description: Zero-runtime-dependency package acting as bridge between Node projects and their package managers
            license: MIT
            name: corepack
            rel: 0
            version: 0.36.0
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/corepack
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.23"
  pretty-name: Docker Hardened Images/Alpine Linux v3.23
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
