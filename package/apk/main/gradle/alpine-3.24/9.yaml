# syntax=dhi.io/build:2-alpine3.23

image: dhi.io/pkg-gradle
variant: runtime
tags:
  - 9.7.1-alpine3.24
  - 9.7.1-r0-alpine3.24
platforms:
  - linux/amd64
vars:
  COMMIT_SHA: 92f0512e7f06d84621afba191f75e265363890cf
  PACKAGE_NAME: gradle-9
  REL: "0"
  SEMVER_MAJOR_MINOR_VERSION: "9.7"
  SEMVER_MAJOR_VERSION: "9"
  SEMVER_VERSION: 9.7.1
  VERSION: 9.7.1
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.24/main
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: gradle-9
      work-dir: ${source.dir}/gradle
      contents:
        packages:
          - unzip
          - openjdk17
        files:
          - url: git+https://github.com/gradle/gradle.git#v9.7.1
            checksum: 92f0512e7f06d84621afba191f75e265363890cf
            path: ${source.dir}/gradle
            uid: 0
            gid: 0
      pipeline:
        - name: patch disable deprecation warnings
          uses: git/patch@v1
          with:
            patch: 0004-Disable-deprecation-warnings.patch
        - name: patch jackson-databind 2.22
          uses: git/patch@v1
          with:
            patch: 0008-Update-com.fasterxml.jackson.core-jackson-databind-2.22.1.patch
        - name: build gradle 9
          uses: dhi/gradle:9-jdk21-dev
          privileged: true
          runs: |
            set -eux -o pipefail

            # remove the daemon jvm properties file
            rm gradle/gradle-daemon-jvm.properties

            ./gradlew :distributions-full:binDistributionZip -Pversion="9.7.1"
        - name: install
          runs: |
            set -eux -o pipefail

            unzip "packaging/distributions-full/build/distributions/gradle-9.7.1-bin.zip"

            cd gradle-*

            gradlehome="/usr/share/java/gradle"
            distdir="${target.dir}/${gradlehome}"

            mkdir -p "${distdir}"

            install -dm755 "${distdir}/bin"
            rm bin/*.bat
            install -m755 bin/* "${distdir}/bin"

            install -dm755 "${target.dir}/usr/bin"
            ln -sf "${gradlehome}/bin/gradle" "${target.dir}/usr/bin/gradle"

            install -dm755 "${distdir}/lib"
            cp -a lib "${distdir}/"

            # NOTICE file should be redistributed for derivative works
            for file in LICENSE NOTICE; do
              install -m644 -D ${file} "${target.dir}/usr/share/licenses/gradle-9/${file}"
            done
        - name: test
          runs: |
            set -eux -o pipefail

            "${target.dir}/usr/share/java/gradle/bin/gradle" --version
        - uses: sbom@v1
          privileged: true
          with:
            prefix: gradle-9-deps
            source: ${target.dir}
        - uses: abuild/setup@v1
          work-dir: ${source.dir}/aports/gradle-9
        - uses: abuild/apk@v1
          work-dir: ${source.dir}/aports/gradle-9
          with:
            arch: noarch
            description: A powerful build system for the JVM
            license: Apache-2.0
            name: gradle-9
            provider_priority: 900
            provides: gradle=9.7.1-r0
            rel: 0
            replaces: gradle
            replaces_priority: 900
            url: https://gradle.org/
            version: 9.7.1
        - uses: abuild/build@v1
          work-dir: ${source.dir}/aports/gradle-9
        - name: copy x86_64 to aarch64 packages
          run-as: abuild
          runs: |
            set -eux -o pipefail

            mkdir -p /home/abuild/packages/aports/aarch64
            cp -a /home/abuild/packages/aports/x86_64/* /home/abuild/packages/aports/aarch64
      paths:
        - type: file
          path: ${source.dir}/gradle/0002-Update-org.eclipse.jgit-org.eclipse.jgit-7.2.1.2025.patch
          uid: 0
          gid: 0
          source: package/apk/main/gradle/patch/0002-Update-org.eclipse.jgit-org.eclipse.jgit-7.2.1.2025.patch
        - type: file
          path: ${source.dir}/gradle/0004-Disable-deprecation-warnings.patch
          uid: 0
          gid: 0
          source: package/apk/main/gradle/patch/0004-Disable-deprecation-warnings.patch
        - type: file
          path: ${source.dir}/gradle/0008-Update-com.fasterxml.jackson.core-jackson-databind-2.22.1.patch
          uid: 0
          gid: 0
          source: package/apk/main/gradle/patch/0008-9-Update-com.fasterxml.jackson.core-jackson-databind-2.22.1.patch
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.24"
  pretty-name: Docker Hardened Images/Alpine Linux v3.24
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
