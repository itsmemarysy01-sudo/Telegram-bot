# syntax=dhi.io/build:2-helm3

name: OpenSearch Dashboards Helm chart 3.x
image: dhi.io/opensearch-dashboards-chart
variant: helm
tags:
  - 3.8.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 62688c44c63f60d932f4c2b143c8c2bb321a92da
  CHART_HELM_NAME: opensearch-dashboards-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  OPENSEARCH_DASHBOARDS_REFERENCE: dhi/opensearch-dashboards:3.8.0-debian13@sha256:e9014fff5c21ba8d5d40685a6080ac8b65aafba6089670307ce185381fc69da1
  SEMVER_MAJOR_MINOR_VERSION: "3.8"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.8.0
  VERSION: 3.8.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: opensearch-dashboards
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/opensearch-project/helm-charts.git#opensearch-dashboards-3.8.0
            checksum: 62688c44c63f60d932f4c2b143c8c2bb321a92da
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/opensearch-dashboards
          with:
            chart:
              appRef: opensearch-dashboards
              description: Docker Hardened Images Helm chart for OpenSearch Dashboards
              images:
                - name: opensearch-dashboards
                  ref: dhi/opensearch-dashboards:3.8.0-debian13@sha256:e9014fff5c21ba8d5d40685a6080ac8b65aafba6089670307ce185381fc69da1
              name: opensearch-dashboards-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/opensearch-project/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/opensearch-dashboards
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/charts/opensearch-dashboards
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: opensearch-dashboards
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/opensearch-dashboards
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/opensearch-dashboards/config
