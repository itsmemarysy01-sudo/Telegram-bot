# syntax=dhi.io/build:2-helm3

name: Grafana Operator Helm chart 5.x
image: dhi.io/grafana-operator-chart
variant: helm
tags:
  - 5.25.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 1d8584df2127ad9341567efb2683adaf25894ebe
  CHART_HELM_NAME: grafana-operator-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  GRAFANA_OPERATOR_REFERENCE: dhi/grafana-operator:5.25.0-debian13@sha256:2ceca3a2299bf7a65c5c1084840c96f98175ebe3a0bb0d5fefabfa031b0739b8
  HELM_VERSION: 4.1.4-2
  SEMVER_MAJOR_MINOR_VERSION: "5.25"
  SEMVER_MAJOR_VERSION: "5"
  SEMVER_VERSION: 5.25.0
  VERSION: 5.25.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: grafana-operator-chart
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/grafana/grafana-operator.git#v5.25.0
            checksum: 1d8584df2127ad9341567efb2683adaf25894ebe
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/deploy/helm/grafana-operator
          with:
            chart:
              appRef: grafana-operator
              description: Docker Hardened Images Helm chart for Grafana Operator
              images:
                - name: grafana-operator
                  ref: dhi/grafana-operator:5.25.0-debian13@sha256:2ceca3a2299bf7a65c5c1084840c96f98175ebe3a0bb0d5fefabfa031b0739b8
              name: grafana-operator-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/grafana/grafana-operator
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/deploy/helm/grafana-operator
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/deploy/helm
          privileged: true
          with:
            chart: grafana-operator
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/deploy/helm
          with:
            chart: grafana-operator
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/deploy/helm
          with:
            chart: grafana-operator
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/grafana-operator/config
