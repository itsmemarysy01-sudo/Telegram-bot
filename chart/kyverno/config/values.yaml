crds:
  migration:
    image:
      defaultRegistry: ${kyverno-cli.registry}
      repository: ${kyverno-cli.repository}
      tag: ${kyverno-cli.tag}@${kyverno-cli.digest}

    podSecurityContext:
      runAsNonRoot: true
      runAsGroup: 65532
      runAsUser: 65532

    securityContext:
      runAsUser: 65532
      runAsGroup: 65532
      runAsNonRoot: true
      privileged: false
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL
      seccompProfile:
        type: RuntimeDefault

test:
  image:
    registry: ${kyverno-readiness-checker.registry}
    repository: ${kyverno-readiness-checker.repository}
    tag: ${kyverno-readiness-checker.tag}@${kyverno-readiness-checker.digest}

  securityContext:
    runAsUser: 65532
    runAsGroup: 65532
    runAsNonRoot: true
    privileged: false
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop:
        - ALL
    seccompProfile:
      type: RuntimeDefault

webhooksCleanup:
  # image and repository are specified on values-<VERSION>.yaml
  podSecurityContext:
    runAsNonRoot: true
    runAsGroup: 65532
    runAsUser: 65532

  securityContext:
    runAsUser: 65532
    runAsGroup: 65532
    runAsNonRoot: true
    privileged: false
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop:
        - ALL
    seccompProfile:
      type: RuntimeDefault

admissionController:
  initContainer:
    image:
      defaultRegistry: ${kyverno-init.registry}
      repository: ${kyverno-init.repository}
      tag: ${kyverno-init.tag}@${kyverno-init.digest}

    securityContext:
      runAsNonRoot: true
      runAsGroup: 65532
      runAsUser: 65532
      privileged: false
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL
      seccompProfile:
        type: RuntimeDefault

  container:
    image:
      defaultRegistry: ${kyverno.registry}
      repository: ${kyverno.repository}
      tag: ${kyverno.tag}@${kyverno.digest}

    securityContext:
      runAsNonRoot: true
      runAsGroup: 65532
      runAsUser: 65532
      privileged: false
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL
      seccompProfile:
        type: RuntimeDefault

  podSecurityContext:
    runAsNonRoot: true
    runAsGroup: 65532
    runAsUser: 65532

backgroundController:
  image:
    defaultRegistry: ${kyverno-background-controller.registry}
    repository: ${kyverno-background-controller.repository}
    tag: ${kyverno-background-controller.tag}@${kyverno-background-controller.digest}

  podSecurityContext:
    runAsNonRoot: true
    runAsGroup: 65532
    runAsUser: 65532


  # -- Security context for the containers
  securityContext:
    runAsNonRoot: true
    runAsGroup: 65532
    runAsUser: 65532
    privileged: false
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop:
        - ALL
    seccompProfile:
      type: RuntimeDefault

cleanupController:
  image:
    defaultRegistry: ${kyverno-cleanup-controller.registry}
    repository: ${kyverno-cleanup-controller.repository}
    tag: ${kyverno-cleanup-controller.tag}@${kyverno-cleanup-controller.digest}

  podSecurityContext:
    runAsNonRoot: true
    runAsGroup: 65532
    runAsUser: 65532

  # -- Security context for the containers
  securityContext:
    runAsNonRoot: true
    runAsGroup: 65532
    runAsUser: 65532
    privileged: false
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop:
        - ALL
    seccompProfile:
      type: RuntimeDefault

reportsController:
  image:
    defaultRegistry: ${kyverno-reports-controller.registry}
    repository: ${kyverno-reports-controller.repository}
    tag: ${kyverno-reports-controller.tag}@${kyverno-reports-controller.digest}

  podSecurityContext:
    runAsNonRoot: true
    runAsGroup: 65532
    runAsUser: 65532

  # -- Security context for the containers
  securityContext:
    runAsNonRoot: true
    runAsGroup: 65532
    runAsUser: 65532
    privileged: false
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop:
        - ALL
    seccompProfile:
      type: RuntimeDefault
