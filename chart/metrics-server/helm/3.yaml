# syntax=dhi.io/build:2-helm3

name: Metrics Server Helm chart 3.x
image: dhi.io/metrics-server-chart
variant: helm
tags:
  - 3.14.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 0716b65c02ce17f22be780c5c04af2392aa01b8a
  CHART_HELM_NAME: metrics-server-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  METRICS_SERVER_REFERENCE: dhi/metrics-server:0.9.0-debian13@sha256:4ac634f33d6ba045dc58eaf1798f34993f6df83b4745ac4b54dd021821525f47
  SEMVER_MAJOR_MINOR_VERSION: "3.14"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.14.0
  VERSION: 3.14.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: metrics-server-chart
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/kubernetes-sigs/metrics-server.git#metrics-server-helm-chart-3.14.0
            checksum: 0716b65c02ce17f22be780c5c04af2392aa01b8a
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/metrics-server
          with:
            chart:
              appRef: metrics-server
              description: Docker Hardened Images Helm chart for Metrics Server
              images:
                - name: metrics-server
                  ref: dhi/metrics-server:0.9.0-debian13@sha256:4ac634f33d6ba045dc58eaf1798f34993f6df83b4745ac4b54dd021821525f47
              name: metrics-server-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kubernetes-sigs/metrics-server
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/metrics-server
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          privileged: true
          with:
            chart: metrics-server
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: metrics-server
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: metrics-server
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/metrics-server/config
