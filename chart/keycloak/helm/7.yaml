# syntax=dhi.io/build:2-helm3

name: Keycloak Helm chart 7.x
image: dhi.io/keycloak-chart
variant: helm
tags:
  - 7.3.1
platforms:
  - linux/amd64
vars:
  BUSYBOX_REFERENCE: dhi/busybox:1.38.0-alpine3.24@sha256:59ec745ef4b2e553eafea215cf9550f545daf68edf151bdb88a2abbe392da132
  CHART_COMMIT_SHA: f336cd75baf2f3d0913f44d053e95853305b72ed
  CHART_HELM_NAME: keycloak-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  KEYCLOAK_REFERENCE: dhi/keycloak:26.7.3-debian13@sha256:884d2b2769e7de7b07738a136e6ff66476ef6f275c8e2b65278155ccb510b5cd
  SEMVER_MAJOR_MINOR_VERSION: "7.3"
  SEMVER_MAJOR_VERSION: "7"
  SEMVER_VERSION: 7.3.1
  VERSION: 7.3.1
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: keycloak-chart
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/codecentric/helm-charts.git#keycloakx-7.3.1
            checksum: f336cd75baf2f3d0913f44d053e95853305b72ed
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/keycloakx
          with:
            chart:
              appRef: keycloak
              description: Docker Hardened Images Helm chart for Keycloak
              images:
                - name: keycloak
                  ref: dhi/keycloak:26.7.3-debian13@sha256:884d2b2769e7de7b07738a136e6ff66476ef6f275c8e2b65278155ccb510b5cd
                - name: busybox
                  ref: dhi/busybox:1.38.0-alpine3.24@sha256:59ec745ef4b2e553eafea215cf9550f545daf68edf151bdb88a2abbe392da132
              name: keycloak-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/codecentric/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/keycloakx
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          privileged: true
          with:
            chart: ${source.dir}/helm-charts/charts/keycloakx
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/charts/keycloakx
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/keycloakx
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/keycloak/config
