# syntax=dhi.io/build:2-helm3

name: Crossplane Helm chart 2.x
image: dhi.io/crossplane-chart
variant: helm
tags:
  - 2.4.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 61fa450c0c14a75bf5632872bd4a5b6945ec5a26
  CHART_HELM_NAME: crossplane-chart
  CROSSPLANE_REFERENCE: dhi/crossplane:2.4.0-debian13@sha256:b2092259328979865d8456dfa51ee3d9325af2bb376a65e8248b4cdc84da549e
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  SEMVER_MAJOR_MINOR_VERSION: "2.4"
  SEMVER_MAJOR_VERSION: "2"
  SEMVER_VERSION: 2.4.0
  VERSION: 2.4.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: crossplane
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/crossplane/crossplane.git#v2.4.0
            checksum: 61fa450c0c14a75bf5632872bd4a5b6945ec5a26
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts/crossplane
          with:
            chart:
              appRef: crossplane
              description: Docker Hardened Images Helm chart for Crossplane
              images:
                - name: crossplane
                  ref: dhi/crossplane:2.4.0-debian13@sha256:b2092259328979865d8456dfa51ee3d9325af2bb376a65e8248b4cdc84da549e
              name: crossplane-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/crossplane/crossplane
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts/crossplane
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/cluster/charts/crossplane
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts
          with:
            chart: crossplane
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/cluster/charts/crossplane
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/crossplane/config
