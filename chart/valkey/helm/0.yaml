# syntax=dhi.io/build:2-helm3

name: Valkey Helm chart 0.x
image: dhi.io/valkey-chart
variant: helm
tags:
  - 0.12.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 95de5c1065d87c58fa5ecc3e632351000cbc13ff
  CHART_HELM_NAME: valkey-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  REDIS_EXPORTER_REFERENCE: dhi/redis-exporter:1.91.0-debian13@sha256:a9d2827638a02259465670109966259865d5263d6aa7b49b0f29c1f2c25e195e
  SEMVER_MAJOR_MINOR_VERSION: "0.12"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.12.0
  VALKEY_REFERENCE: dhi/valkey:9.1.2-debian13@sha256:513b68223beda35461e218f9103a71407ab3473d2d9ebafa0d2a1422792a753d
  VERSION: 0.12.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: valkey
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/valkey-io/valkey-helm.git#valkey-0.12.0
            checksum: 95de5c1065d87c58fa5ecc3e632351000cbc13ff
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/valkey
          with:
            chart:
              appRef: valkey
              description: Docker Hardened Images Helm chart for Valkey
              images:
                - name: valkey
                  ref: dhi/valkey:9.1.2-debian13@sha256:513b68223beda35461e218f9103a71407ab3473d2d9ebafa0d2a1422792a753d
                - name: redis-exporter
                  ref: dhi/redis-exporter:1.91.0-debian13@sha256:a9d2827638a02259465670109966259865d5263d6aa7b49b0f29c1f2c25e195e
              name: valkey-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/valkey-io/valkey-helm
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/valkey
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/valkey
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: valkey
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/valkey
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/valkey/config
