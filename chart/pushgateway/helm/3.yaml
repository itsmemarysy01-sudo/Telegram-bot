# syntax=dhi.io/build:2-helm3

name: Prometheus Pushgateway Helm chart 3.x
image: dhi.io/pushgateway-chart
variant: helm
tags:
  - 3.8.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 4fa56e1c264a2a7e8030af068dcccd4400dd620c
  CHART_HELM_NAME: pushgateway-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  PUSHGATEWAY_REFERENCE: dhi/pushgateway:1.11.3-debian13@sha256:2f148e319bcaee1cf68c975b5e34391ee01ab27c65642fa3ee7972f8264ddbee
  SEMVER_MAJOR_MINOR_VERSION: "3.8"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.8.0
  VERSION: 3.8.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: prometheus-pushgateway-chart
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/prometheus-community/helm-charts.git#prometheus-pushgateway-3.8.0
            checksum: 4fa56e1c264a2a7e8030af068dcccd4400dd620c
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/prometheus-pushgateway
          with:
            chart:
              appRef: pushgateway
              description: Docker Hardened Images Helm chart for Prometheus Pushgateway
              images:
                - name: pushgateway
                  ref: dhi/pushgateway:1.11.3-debian13@sha256:2f148e319bcaee1cf68c975b5e34391ee01ab27c65642fa3ee7972f8264ddbee
              name: pushgateway-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/prometheus-community/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/prometheus-pushgateway
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          privileged: true
          with:
            chart: prometheus-pushgateway
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: prometheus-pushgateway
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: prometheus-pushgateway
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/pushgateway/config
