# syntax=dhi.io/build:2-helm3

name: Istio Base Helm chart 1.28.x
image: dhi.io/istio-base-chart
variant: helm
tags:
  - 1.28.10
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: ff49c1889ebad8d444b8f82425a2fd326c039e54
  CHART_HELM_NAME: istio-base-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  SEMVER_MAJOR_MINOR_VERSION: "1.28"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.28.10
  VERSION: 1.28.10
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: istio-base-chart
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/istio/istio.git#ff49c1889ebad8d444b8f82425a2fd326c039e54
            checksum: ff49c1889ebad8d444b8f82425a2fd326c039e54
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/manifests/charts/base
          with:
            chart:
              description: Docker Hardened Images Helm chart for Istio Base
              name: istio-base-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/istio/istio/tree/master/manifests/charts/base
            cmd: prepare
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/manifests/charts
          with:
            chart: base
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/manifests/charts
          with:
            chart: base
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/istio-base/config
