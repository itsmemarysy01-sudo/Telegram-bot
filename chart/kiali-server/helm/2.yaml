# syntax=dhi.io/build:2-helm3

name: Kiali Server Helm chart 2.x
image: dhi.io/kiali-server-chart
variant: helm
tags:
  - 2.31.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 7ae226c659fdfc3465acff351560a5d05f30e02c
  CHART_HELM_NAME: kiali-server-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  KIALI_REFERENCE: dhi/kiali:2.31.0-debian13@sha256:fb03ae6e80ba6732d645d29ab418760cb1721d9333656c1d204396a5a34af344
  SEMVER_MAJOR_MINOR_VERSION: "2.31"
  SEMVER_MAJOR_VERSION: "2"
  SEMVER_VERSION: 2.31.0
  VERSION: 2.31.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: kiali-server-chart
      work-dir: ${source.dir}/helm-charts/kiali-server
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/kiali/helm-charts.git#v2.31.0
            checksum: 7ae226c659fdfc3465acff351560a5d05f30e02c
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          with:
            chart:
              appRef: kiali
              description: Docker Hardened Images Helm chart for Kiali Server
              images:
                - name: kiali
                  ref: dhi/kiali:2.31.0-debian13@sha256:fb03ae6e80ba6732d645d29ab418760cb1721d9333656c1d204396a5a34af344
              name: kiali-server-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kiali/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: .
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: .
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kiali-server/config
