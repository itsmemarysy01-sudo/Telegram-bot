# syntax=dhi.io/build:2-helm3

name: Kured Helm chart 5.x
image: dhi.io/kured-chart
variant: helm
tags:
  - 5.12.1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 73ac791764d2b816af13c8c1c9ce048c021f04b7
  CHART_HELM_NAME: kured-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  KURED_REFERENCE: dhi/kured:1.23.0-debian13@sha256:9f6484a04fe8fdd002b73c317555f42cceda50efec1fdb6044df641bd2d57e29
  SEMVER_MAJOR_MINOR_VERSION: "5.12"
  SEMVER_MAJOR_VERSION: "5"
  SEMVER_VERSION: 5.12.1
  VERSION: 5.12.1
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: kured-chart
      work-dir: ${source.dir}/helm-charts/charts/kured
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/kubereboot/charts.git#kured-5.12.1
            checksum: 73ac791764d2b816af13c8c1c9ce048c021f04b7
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          with:
            chart:
              appRef: kured
              description: Docker Hardened Images Helm chart for Kured
              images:
                - name: kured
                  ref: dhi/kured:1.23.0-debian13@sha256:9f6484a04fe8fdd002b73c317555f42cceda50efec1fdb6044df641bd2d57e29
              name: kured-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kubereboot/kured
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: .
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: .
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kured/config
