===============================================================================
 🐳  {{ .Chart.Name | upper }}  —  A Docker Hardened Helm Chart
===============================================================================

This is a {{ .Chart.Name }} Docker Helm chart, a hardened build of the upstream
Valkey `valkey-resources` Helm chart packaged with **Docker Hardened Images**.

⭐ {{ .Chart.Name | upper }} has been deployed!

Release:      {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}
Chart:        {{ .Chart.Name }} {{ .Chart.Version }}
App version:  {{ .Chart.AppVersion }}

This chart only creates a ValkeyCluster custom resource{{ if .Values.metrics.podMonitor.enabled }}
and a PodMonitor{{ end }}. It requires the valkey-operator to already be
installed and running in the cluster, otherwise the Kubernetes API server will
reject the ValkeyCluster resource.

🔍 Verify the installation:
$ kubectl get valkeycluster {{ include "valkey-resources.fullname" . }} -n {{ .Release.Namespace }} -w
$ kubectl get valkeynodes -n {{ .Release.Namespace }}

🧹 Uninstall
$ helm -n {{ .Release.Namespace }} uninstall {{ .Release.Name }}
===============================================================================
