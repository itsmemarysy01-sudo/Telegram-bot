# syntax=dhi.io/build:2-helm3

name: Kubernetes Cluster Autoscaler Helm chart 9.x
image: dhi.io/kubernetes-cluster-autoscaler-chart
variant: helm
tags:
  - 9.59.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 5a0b98e45f896e98132737240ac131a519d3b33d
  CHART_HELM_NAME: kubernetes-cluster-autoscaler-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  KUBERNETES_CLUSTER_AUTOSCALER_REFERENCE: dhi/kubernetes-cluster-autoscaler:1.34.5-debian13@sha256:e24a315a9ac6dcdfc2852d5dc2cf2579d694e78dbe2f56021ee0aaf725614bd8
  SEMVER_MAJOR_MINOR_VERSION: "9.59"
  SEMVER_MAJOR_VERSION: "9"
  SEMVER_VERSION: 9.59.0
  VERSION: 9.59.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: kubernetes-cluster-autoscaler
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/kubernetes/autoscaler.git#cluster-autoscaler-chart-9.59.0
            checksum: 5a0b98e45f896e98132737240ac131a519d3b33d
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts/cluster-autoscaler
          with:
            chart:
              appRef: kubernetes-cluster-autoscaler
              description: Docker Hardened Images Helm chart for kubernetes-cluster-autoscaler
              images:
                - name: kubernetes-cluster-autoscaler
                  ref: dhi/kubernetes-cluster-autoscaler:1.34.5-debian13@sha256:e24a315a9ac6dcdfc2852d5dc2cf2579d694e78dbe2f56021ee0aaf725614bd8
              name: kubernetes-cluster-autoscaler-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kubernetes/autoscaler
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts/cluster-autoscaler
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts
          with:
            chart: cluster-autoscaler
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/cluster-autoscaler/charts
          with:
            chart: cluster-autoscaler
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kubernetes-cluster-autoscaler/config
