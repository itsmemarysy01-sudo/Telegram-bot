# syntax=dhi.io/build:2-helm3

name: Pyroscope Helm chart 2.x
image: dhi.io/pyroscope-chart
variant: helm
tags:
  - 2.3.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: d969f943947df3a22525418ac3f9f1ae86b5c13f
  CHART_HELM_NAME: pyroscope-chart
  COMMIT_SHA: 6db589af112b7a5c6fa24ec48795bbb0e3f211bf
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  PYROSCOPE_REFERENCE: dhi/pyroscope:2.3.0-debian13@sha256:c4948c61ed8809d4c1acd29a8b3bf709bd2fe6e3dfa61eac630b8a44c8a990ef
  SEMVER_MAJOR_MINOR_VERSION: "2.3"
  SEMVER_MAJOR_VERSION: "2"
  SEMVER_VERSION: 2.3.0
  VERSION: 2.3.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: pyroscope
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/grafana/pyroscope.git#d969f943947df3a22525418ac3f9f1ae86b5c13f
            checksum: d969f943947df3a22525418ac3f9f1ae86b5c13f
            path: ${source.dir}
            uid: 0
            gid: 0
      pipeline:
        - name: prepare
          uses: helm/rudder@v1
          work-dir: ${source.dir}/operations/pyroscope/helm/pyroscope
          with:
            chart:
              appRef: pyroscope
              description: Docker Hardened Images Helm chart for Grafana Pyroscope
              images:
                - name: pyroscope
                  ref: dhi/pyroscope:2.3.0-debian13@sha256:c4948c61ed8809d4c1acd29a8b3bf709bd2fe6e3dfa61eac630b8a44c8a990ef
              name: pyroscope-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/grafana/pyroscope
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/operations/pyroscope/helm/pyroscope
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/operations/pyroscope/helm
          with:
            chart: pyroscope
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/operations/pyroscope/helm/pyroscope
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/pyroscope/config
