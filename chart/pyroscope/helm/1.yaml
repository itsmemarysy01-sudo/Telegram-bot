# syntax=dhi.io/build:2-helm3

name: Pyroscope Helm chart 1.x
image: dhi.io/pyroscope-chart
variant: helm
tags:
  - 1.21.1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: d6cb8c3db58edf6c0ea26daa1dd40d84f11a4ef0
  CHART_HELM_NAME: pyroscope-chart
  COMMIT_SHA: d6cb8c3db58edf6c0ea26daa1dd40d84f11a4ef0
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  PYROSCOPE_REFERENCE: dhi/pyroscope:1.21.1-debian13@sha256:249529c8caf38e877e6d6022af119971890a9dd058fe1b8c5f0c7dc70a354d32
  SEMVER_MAJOR_MINOR_VERSION: "1.21"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.21.1
  VERSION: 1.21.1
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: pyroscope
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/grafana/pyroscope.git#d6cb8c3db58edf6c0ea26daa1dd40d84f11a4ef0
            checksum: d6cb8c3db58edf6c0ea26daa1dd40d84f11a4ef0
            path: ${source.dir}
            uid: 0
            gid: 0
      pipeline:
        - name: prepare
          uses: helm/rudder@v1
          work-dir: ${source.dir}/operations/pyroscope/helm/pyroscope
          with:
            chart:
              appRef: pyroscope
              description: Docker Hardened Images Helm chart for Grafana Pyroscope
              images:
                - name: pyroscope
                  ref: dhi/pyroscope:1.21.1-debian13@sha256:249529c8caf38e877e6d6022af119971890a9dd058fe1b8c5f0c7dc70a354d32
              name: pyroscope-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/grafana/pyroscope
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/operations/pyroscope/helm/pyroscope
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/operations/pyroscope/helm
          with:
            chart: pyroscope
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/operations/pyroscope/helm/pyroscope
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/pyroscope/config
