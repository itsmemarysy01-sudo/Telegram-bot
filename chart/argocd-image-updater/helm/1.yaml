# syntax=dhi.io/build:2-helm3

name: Argo CD Image Updater Helm chart 1.x
image: dhi.io/argocd-image-updater-chart
variant: helm
tags:
  - 1.3.1
platforms:
  - linux/amd64
vars:
  ARGOCD_IMAGE_UPDATER_REFERENCE: dhi/argocd-image-updater:1.3.0-debian13@sha256:a32e81fddf654bd10edaa46a0df4af1df33ed6b3f8fe91490f6fda857ea65582
  CHART_COMMIT_SHA: 21e00e11df1c82f0be7bd06889525105780d43a0
  CHART_HELM_NAME: argocd-image-updater-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  SEMVER_MAJOR_MINOR_VERSION: "1.3"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.3.1
  VERSION: 1.3.1
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: argocd-image-updater
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/argoproj/argo-helm#argocd-image-updater-1.3.1
            checksum: 21e00e11df1c82f0be7bd06889525105780d43a0
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/argocd-image-updater
          with:
            chart:
              appRef: argocd-image-updater
              description: Docker Hardened Images Helm chart for Argo CD Image Updater
              images:
                - name: argocd-image-updater
                  ref: dhi/argocd-image-updater:1.3.0-debian13@sha256:a32e81fddf654bd10edaa46a0df4af1df33ed6b3f8fe91490f6fda857ea65582
              name: argocd-image-updater-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/argoproj/argo-helm
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/argocd-image-updater
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          privileged: true
          with:
            chart: argocd-image-updater
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: argocd-image-updater
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: argocd-image-updater
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/argocd-image-updater/config
