# syntax=dhi.io/build:2-helm3

name: Kyverno Policies Helm chart 3.x
image: dhi.io/kyverno-policies-chart
variant: helm
tags:
  - 3.9.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: fb98fe3bd6ab4ac7dec2e198145b0f5017350325
  CHART_HELM_NAME: kyverno-policies-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  SEMVER_MAJOR_MINOR_VERSION: "3.9"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.9.0
  VERSION: 3.9.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: kyverno-policies-chart
      work-dir: ${source.dir}/helm-charts/charts/kyverno-policies
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/kyverno/kyverno.git#kyverno-policies-chart-3.9.0
            checksum: fb98fe3bd6ab4ac7dec2e198145b0f5017350325
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          with:
            chart:
              description: Docker Hardened Images Helm chart for Kyverno Policies
              name: kyverno-policies-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kyverno/kyverno/tree/main/charts/kyverno-policies
            cmd: prepare
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: .
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: .
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/kyverno-policies/config
