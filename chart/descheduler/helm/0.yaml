# syntax=dhi.io/build:2-helm3

name: Descheduler Helm chart 0.x
image: dhi.io/descheduler-chart
variant: helm
tags:
  - 0.36.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 3588dacd57d72d8bb18b68bb8fa7b0438e3659cc
  CHART_HELM_NAME: descheduler-chart
  DESCHEDULER_REFERENCE: dhi/descheduler:0.36.0-debian13@sha256:50e335465c6f27362b8d1570aaa09532db3bd86d5806d1f617c920afa3148f72
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  SEMVER_MAJOR_MINOR_VERSION: "0.36"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.36.0
  VERSION: 0.36.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: descheduler-chart
      work-dir: ${source.dir}/helm-charts/charts/descheduler
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/kubernetes-sigs/descheduler.git#descheduler-helm-chart-0.36.0
            checksum: 3588dacd57d72d8bb18b68bb8fa7b0438e3659cc
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          with:
            chart:
              appRef: descheduler
              description: Docker Hardened Images Helm chart for Descheduler
              images:
                - name: descheduler
                  ref: dhi/descheduler:0.36.0-debian13@sha256:50e335465c6f27362b8d1570aaa09532db3bd86d5806d1f617c920afa3148f72
              name: descheduler-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/kubernetes-sigs/descheduler
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          with:
            chart: .
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: .
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/descheduler/config
