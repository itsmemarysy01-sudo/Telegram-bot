# syntax=dhi.io/build:2-helm3

name: Valkey Operator Helm chart 0.x
image: dhi.io/valkey-operator-chart
variant: helm
tags:
  - 0.5.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: dd2d78213ae4e7b229074d473612999c7324d9be
  CHART_HELM_NAME: valkey-operator-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  SEMVER_MAJOR_MINOR_VERSION: "0.5"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.5.0
  VALKEY_OPERATOR_REFERENCE: dhi/valkey-operator:0.5.0-debian13@sha256:02df140a20750312d2f4332b7ed120fdea62f769e71b26dea031003e0b41f236
  VALKEY_OPERATOR_VERSION_PREFIX: "0.5"
  VERSION: 0.5.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: valkey-operator
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/valkey-io/valkey-helm.git#valkey-operator-0.5.0
            checksum: dd2d78213ae4e7b229074d473612999c7324d9be
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/valkey-operator
          with:
            chart:
              appRef: valkey-operator
              description: Docker Hardened Images Helm chart for the Valkey Operator
              images:
                - name: valkey-operator
                  ref: dhi/valkey-operator:0.5.0-debian13@sha256:02df140a20750312d2f4332b7ed120fdea62f769e71b26dea031003e0b41f236
              name: valkey-operator-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/valkey-io/valkey-helm
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/valkey-operator
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: valkey-operator
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/valkey-operator
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/valkey-operator/config
