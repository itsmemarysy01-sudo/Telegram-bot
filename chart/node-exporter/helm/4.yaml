# syntax=dhi.io/build:2-helm3

name: Prometheus Node Exporter Helm chart 4.x
image: dhi.io/node-exporter-chart
variant: helm
tags:
  - 4.56.3
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 3387e946e74089b78d2419081d48e389cb2b35b9
  CHART_HELM_NAME: node-exporter-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  KUBE_RBAC_PROXY_REFERENCE: dhi/kube-rbac-proxy:0.22.1-debian13@sha256:60298c42c2d0ad1da97500834192c80498b9266a98ca03796f0ac331a8a2030b
  NODE_EXPORTER_REFERENCE: dhi/node-exporter:1.12.1-debian13@sha256:f1b40aed443c274bdc87d02276a621ace911e2e974ee5552d3c268cab80c676f
  SEMVER_MAJOR_MINOR_VERSION: "4.56"
  SEMVER_MAJOR_VERSION: "4"
  SEMVER_VERSION: 4.56.3
  VERSION: 4.56.3
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: node-exporter-chart
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/prometheus-community/helm-charts.git#prometheus-node-exporter-4.56.3
            checksum: 3387e946e74089b78d2419081d48e389cb2b35b9
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/prometheus-node-exporter
          with:
            chart:
              appRef: node-exporter
              description: Docker Hardened Images Helm chart for Prometheus Node Exporter
              images:
                - name: node-exporter
                  ref: dhi/node-exporter:1.12.1-debian13@sha256:f1b40aed443c274bdc87d02276a621ace911e2e974ee5552d3c268cab80c676f
                - name: kube-rbac-proxy
                  ref: dhi/kube-rbac-proxy:0.22.1-debian13@sha256:60298c42c2d0ad1da97500834192c80498b9266a98ca03796f0ac331a8a2030b
              name: node-exporter-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/prometheus-community/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/prometheus-node-exporter
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          privileged: true
          with:
            chart: prometheus-node-exporter
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: prometheus-node-exporter
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: prometheus-node-exporter
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/node-exporter/config
