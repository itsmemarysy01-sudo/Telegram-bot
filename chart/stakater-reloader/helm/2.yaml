# syntax=dhi.io/build:2-helm3

name: Stakater Reloader Helm chart 2.x
image: dhi.io/stakater-reloader-chart
variant: helm
tags:
  - 2.2.14
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: dd72380cf1334e41691636c84b4f983ef8011368
  CHART_HELM_NAME: stakater-reloader-chart
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_VERSION: 4.1.4-2
  SEMVER_MAJOR_MINOR_VERSION: "2.2"
  SEMVER_MAJOR_VERSION: "2"
  SEMVER_VERSION: 2.2.14
  STAKATER_RELOADER_REFERENCE: dhi/stakater-reloader:1.4.21-debian13@sha256:5ca297da10cbfabc1308c9818e8dc51d5d73ba5715071a4e6858d2cafac6f152
  VERSION: 2.2.14
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  builds:
    - name: stakater-reloader-chart
      contents:
        packages:
          - bash
          - ca-certificates
          - coreutils
          - helm=4.1.4-2
        files:
          - url: git+https://github.com/stakater/Reloader.git#chart-v2.2.14
            checksum: dd72380cf1334e41691636c84b4f983ef8011368
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/deployments/kubernetes/chart/reloader
          with:
            chart:
              appRef: stakater-reloader
              description: Docker Hardened Images Helm chart for Stakater Reloader
              images:
                - name: stakater-reloader
                  ref: dhi/stakater-reloader:1.4.21-debian13@sha256:5ca297da10cbfabc1308c9818e8dc51d5d73ba5715071a4e6858d2cafac6f152
              name: stakater-reloader-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/stakater/Reloader
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/deployments/kubernetes/chart/reloader
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/deployments/kubernetes/chart
          privileged: true
          with:
            chart: reloader
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/deployments/kubernetes/chart
          with:
            chart: reloader
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/deployments/kubernetes/chart
          with:
            chart: reloader
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/stakater-reloader/config
