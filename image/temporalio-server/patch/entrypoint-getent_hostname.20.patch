diff --git i/docker/entrypoint.sh w/docker/entrypoint.sh
index 25a3382..4510fee 100755
--- i/docker/entrypoint.sh
+++ w/docker/entrypoint.sh
@@ -5 +5 @@ set -eu -o pipefail
-: "${BIND_ON_IP:=$(getent hosts "$(hostname)" | awk '{print $1;}')}"
+: "${BIND_ON_IP:=$(cat /etc/hosts | grep -v ^# | grep "$HOSTNAME" | awk '{print $1;}')}"
@@ -9 +9 @@ if [[ "${BIND_ON_IP}" == "0.0.0.0" || "${BIND_ON_IP}" == "::0" ]]; then
-    : "${TEMPORAL_BROADCAST_ADDRESS:=$(getent hosts "$(hostname)" | awk '{print $1;}')}"
+    : "${TEMPORAL_BROADCAST_ADDRESS:=$(cat /etc/hosts | grep -v ^# | grep "$HOSTNAME" | awk '{print $1;}')}"
