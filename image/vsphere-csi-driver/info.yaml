metadata:
  display-name: vSphere CSI Driver
  short-description: |
    Container Storage Interface (CSI) driver for VMware vSphere. Provisions and
    attaches vSphere volumes for Kubernetes workloads, running as the CSI
    controller plugin and as the node plugin that formats and mounts volumes.
  featured: false
  home-url: https://github.com/kubernetes-sigs/vsphere-csi-driver
  categories:
    - databases-and-storage
