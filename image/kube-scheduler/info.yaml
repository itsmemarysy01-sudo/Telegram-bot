metadata:
  display-name: Kubernetes Scheduler (kube-scheduler)
  short-description: |
    Schedules pods onto nodes.

    kube-scheduler watches for newly created pods with no assigned node, and selects
    a node for them to run on.
  featured: false
  home-url: https://kubernetes.io/
  categories:
    - developer-tools
