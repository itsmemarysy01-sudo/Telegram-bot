# syntax=dhi.io/build:2-debian13

name: Apache Kafka 4.3.x (dev)
image: dhi.io/kafka
variant: dev
tags:
  - 4-dev
  - 4.3-dev
  - 4.3.1-dev
  - 4-debian-dev
  - 4-debian13-dev
  - 4.3-debian-dev
  - 4.3-debian13-dev
  - 4.3.1-debian-dev
  - 4.3.1-debian13-dev
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-05-13"
vars:
  COMMIT_SHA: 26b251a451ce941d3d7a55e6487bcb7f16b5ad48
  GRADLE_PACKAGE: gradle-9
  JAVA_FIPS_SUFFIX: ""
  JDK_MAJOR_VERSION: "21"
  KAFKA_MAJOR_MINOR_VERSION: ${KAFKA_VERSION:([0-9]+.[0-9]+).*$}
  KAFKA_MAJOR_VERSION: ${KAFKA_VERSION:([0-9]+).*$}
  SCALA_VERSION: "2.13"
  SEMVER_MAJOR_MINOR_VERSION: "4.3"
  SEMVER_MAJOR_VERSION: "4"
  SEMVER_VERSION: 4.3.1
  VERSION: 4.3.1
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  packages:
    - '!gawk'
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - apt
    - base-files
    - bash
    - ca-certificates
    - coreutils
    - diffutils
    - dpkg
    - eclipse-temurin-21-jre
    - eclipse-temurin-21-jre-oci-config
    - findutils
    - grep
    - libc-bin
    - mawk
    - netbase
    - perl-base
    - sed
    - tzdata
    - util-linux
  builds:
    - name: kafka
      caches:
        - path: /root/.gradle/caches
        - path: /root/.gradle/wrapper
      contents:
        packages:
          - bash
          - coreutils
          - findutils
          - gzip
          - patch
          - sed
          - tar
          - eclipse-temurin-21-jdk
          - gradle-9
        files:
          - url: git+https://github.com/apache/kafka.git#4.3.1
            checksum: 26b251a451ce941d3d7a55e6487bcb7f16b5ad48
            path: ${source.dir}/kafka
            spdx:
              name: kafka
              version: 4.3.1
              packages:
                - name: kafka
                  purl: pkg:github/apache/kafka@4.3.1
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: apply jline remote-telnet cve patch
          uses: git/patch@v1
          work-dir: ${source.dir}/kafka
          with:
            patch: ${source.dir}/patch/jline-remote-telnet-cve.patch
        - name: apply gradle 9.5 projectdependency compatibility patch
          uses: git/patch@v1
          work-dir: ${source.dir}/kafka
          with:
            patch: ${source.dir}/patch/gradle-9.5-projectdependency-4.3.patch
        - name: configure gradle mirror
          runs: |
            set -eux -o pipefail

            mkdir -p /root/.gradle
            cat > /root/.gradle/init.gradle << 'EOF'
            def mavenCentralMirror = uri("https://maven-central.storage-download.googleapis.com/maven2/")

            def mirrorMavenCentral = { repo ->
                if (repo instanceof org.gradle.api.artifacts.repositories.MavenArtifactRepository &&
                    repo.url.toString() == "https://repo.maven.apache.org/maven2/") {
                    repo.setUrl(mavenCentralMirror)
                }
            }

            settingsEvaluated { settings ->
                settings.pluginManagement.repositories.configureEach(mirrorMavenCentral)
                settings.dependencyResolutionManagement.repositories.configureEach(mirrorMavenCentral)
            }

            allprojects {
                buildscript.repositories.configureEach(mirrorMavenCentral)
                repositories.configureEach(mirrorMavenCentral)
            }

            // jackson-databind PolymorphicTypeValidator bypass family and
            // XML datatype parsing DoS
            // (CVE-2026-54512, -54513, -54514, -54516, -54517, -54518,
            // CVE-2026-54515, CVE-2026-59889, CVE-2026-68497,
            // GHSA-mhm7-754m-9p8w) plus GHSA-72hv-8253-57qq and
            // CVE-2026-68498 (jackson-core DoS). Force databind/core to
            // 2.21.6 across all Kafka versions (4.1/4.2/4.3 use a single
            // `jackson` catalog property, so this version-agnostic force avoids
            // a per-version patch). Also force jackson-annotations to 2.21:
            // annotations has no 2.21.6 artifact on Maven Central, but databind
            // 2.21.6 requires JsonSerializeAs from annotations 2.21+ at build
            // time (MessageGenerator / processMessages).
            //
            // Jetty CVEs (CVE-2026-10050, CVE-2026-10051, CVE-2026-6790,
            // CVE-2026-8384): force jetty-* to 12.0.36.
            //
            // log4j CVE-2026-49844: force 2.25.5 across log4j artifacts.
            //
            // lz4-java CVE-2026-59949: JNI XXHash validation bypass.
            allprojects {
                configurations.all {
                    resolutionStrategy.eachDependency { details ->
                        if (details.requested.group == "org.apache.logging.log4j" &&
                                (details.requested.name == "log4j-api" ||
                                 details.requested.name == "log4j-core" ||
                                 details.requested.name == "log4j-1.2-api" ||
                                 details.requested.name == "log4j-slf4j-impl")) {
                            details.useVersion("2.25.5")
                            details.because("CVE remediation: CVE-2026-49844")
                        }
                        if (details.requested.group == "com.fasterxml.jackson.core" &&
                                (details.requested.name == "jackson-databind" ||
                                 details.requested.name == "jackson-core")) {
                            details.useVersion("2.21.6")
                            details.because("CVE remediation: GHSA-72hv-8253-57qq, CVE-2026-54512, CVE-2026-54513, CVE-2026-54514, CVE-2026-54515, CVE-2026-54516, CVE-2026-54517, CVE-2026-54518, CVE-2026-59889, CVE-2026-68497, CVE-2026-68498, GHSA-mhm7-754m-9p8w")
                        }
                        if (details.requested.group == "com.fasterxml.jackson.core" &&
                                details.requested.name == "jackson-annotations") {
                            details.useVersion("2.21")
                            details.because("align jackson-annotations with jackson-databind 2.21.6 for MessageGenerator")
                        }
                        if (details.requested.group.startsWith("org.eclipse.jetty") &&
                                details.requested.name.startsWith("jetty-") &&
                                details.requested.version.startsWith("12.0.")) {
                            details.useVersion("12.0.36")
                            details.because("CVE remediation: CVE-2026-10050, CVE-2026-10051, CVE-2026-6790, CVE-2026-8384")
                        }
                        if (details.requested.group == "at.yawk.lz4" &&
                                details.requested.name == "lz4-java") {
                            details.useVersion("1.11.1")
                            details.because("CVE remediation: CVE-2026-59949")
                        }
                    }
                }
            }
            EOF
        - name: build kafka distribution
          work-dir: ${source.dir}/kafka
          privileged: true
          runs: |
            set -eux -o pipefail

            export GRADLE_HOME="/usr/share/java/gradle"
            export JAVA_HOME="/usr/lib/jvm/temurin-21"
            export PATH="${GRADLE_HOME}/bin:${JAVA_HOME}/bin:${PATH}"

            gradle clean releaseTarGz \
              -PscalaVersion=2.13 \
              --no-daemon \
              --console=plain \
              --warning-mode=summary
        - name: extract distribution
          work-dir: ${source.dir}/kafka
          runs: |
            set -eux -o pipefail

            mkdir -p ${target.dir}/opt/kafka

            tar -xzf "core/build/distributions/kafka_2.13-4.3.1.tgz" \
              --strip-components=1 \
              -C ${target.dir}/opt/kafka
        - name: configure
          work-dir: ${source.dir}/kafka
          runs: |
            set -eux -o pipefail

            cp docker/server.properties \
              ${target.dir}/opt/kafka/config/server.properties

            sed -i \
              -e 's|^advertised\.listeners=PLAINTEXT://localhost:9092|advertised.listeners=PLAINTEXT://:9092|' \
              ${target.dir}/opt/kafka/config/server.properties

            chown -Rh 65532:65532 ${target.dir}/opt/kafka/config
            chmod -R 755 ${target.dir}/opt/kafka/config
      paths:
        - type: file
          path: ${source.dir}/patch/fips-sha256-log-cleaner.patch
          uid: 0
          gid: 0
          source: image/kafka/patch/fips-sha256-log-cleaner.patch
        - type: file
          path: ${source.dir}/patch/jline-remote-telnet-cve.patch
          uid: 0
          gid: 0
          source: image/kafka/patch/jline-remote-telnet-cve.patch
        - type: file
          path: ${source.dir}/patch/gradle-9.5-projectdependency-4.3.patch
          uid: 0
          gid: 0
          source: image/kafka/patch/gradle-9.5-projectdependency-4.3.patch
      outputs:
        - source: ${target.dir}/opt/kafka
          target: /opt/kafka
          uid: 0
          gid: 0
        - source: /opt/docker/sbom/kafka
          target: /opt/docker/sbom/kafka
          uid: 0
          gid: 0
    - name: locale
      contents:
        packages:
          - bash
          - locales
          - coreutils
          - gzip
      pipeline:
        - name: install locales
          runs: |
            set -eux -o pipefail

            ln -s /etc/locale.alias /usr/share/locale/locale.alias

            echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen
            locale-gen en_US.UTF-8
      outputs:
        - source: /usr/share/locale
          target: /usr/share/locale
          uid: 0
          gid: 0
        - source: /usr/lib/locale
          target: /usr/lib/locale
          uid: 0
          gid: 0
accounts:
  root: true
  run-as: root
  users:
    - name: kafka
      uid: 65532
      gid: 65532
    - name: _apt
      uid: 42
      gid: 65532
  groups:
    - name: kafka
      gid: 65532
      members:
        - kafka
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
work-dir: /opt/kafka
environment:
  DEBIAN_FRONTEND: noninteractive
  JAVA_HOME: /usr/lib/jvm/temurin-21
  KAFKA_LOG4J_OPTS: -Dkafka.logs.dir=/opt/kafka/logs/ -Dlog4j2.configurationFile=file:/opt/kafka/config/log4j2.yaml
  LANG: en_US.UTF-8
  LANGUAGE: en_US:en
  LC_ALL: en_US.UTF-8
paths:
  - type: directory
    path: /opt/kafka/logs
    uid: 65532
    gid: 65532
    mode: "0775"
  - type: directory
    path: /tmp/kraft-combined-logs
    uid: 65532
    gid: 65532
    mode: "0775"
  - type: directory
    path: /usr/local/bin
    uid: 0
    gid: 0
  - type: file
    path: /usr/local/bin/docker-entrypoint.sh
    uid: 0
    gid: 0
    mode: "0755"
    source: image/kafka/bin/docker-entrypoint.sh
annotations:
  org.opencontainers.image.description: A minimal Kafka image
  org.opencontainers.image.licenses: Apache-2.0
entrypoint:
  - /usr/local/bin/docker-entrypoint.sh
ports:
  - 9092/tcp
