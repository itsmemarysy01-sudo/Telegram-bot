# syntax=dhi.io/build:2-debian13

name: Apache Kafka 4.1.x (native)
image: dhi.io/kafka
variant: runtime
flavor: native
tags:
  - 4.1-native
  - 4.1.2-native
  - 4.1-debian-native
  - 4.1-debian13-native
  - 4.1.2-debian-native
  - 4.1.2-debian13-native
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2025-09-02"
vars:
  ARCH: '#{ target.arch == "amd64" ? "x64" : "aarch64" }'
  GRAALVM_VERSION: 21.0.2
  KAFKA_MAJOR_MINOR_VERSION: ${KAFKA_VERSION:([0-9]+.[0-9]+).*$}
  KAFKA_MAJOR_VERSION: ${KAFKA_VERSION:([0-9]+).*$}
  MAVEN_DIGEST: sha256:97e4283d662a0a0dd680bc67c7cfa519d9cf6ef21567f0a727d050a760df3ff8
  MAVEN_VERSION: 3.9.11
  SCALA_VERSION: "2.13"
  SEMVER_MAJOR_MINOR_VERSION: "4.1"
  SEMVER_MAJOR_VERSION: "4"
  SEMVER_VERSION: 4.1.2
  VERSION: 4.1.2
contents:
  packages:
    - '!gawk'
    - base-files
    - bash
    - ca-certificates
    - netbase
    - tzdata
  builds:
    - name: kafka
      uses: dhi.io/maven:3.9.11-jdk23-debian13-dev@sha256:97e4283d662a0a0dd680bc67c7cfa519d9cf6ef21567f0a727d050a760df3ff8
      caches:
        - path: /root/.m2/repository
      contents:
        packages:
          - build-essential
          - gzip
          - sed
          - zlib1g-dev
        files:
          - url: https://github.com/graalvm/graalvm-ce-builds/releases/download/jdk-21.0.2/graalvm-community-jdk-21.0.2_linux-${ARCH}_bin.tar.gz
            checksum: https://github.com/graalvm/graalvm-ce-builds/releases/download/jdk-21.0.2/graalvm-community-jdk-21.0.2_linux-${ARCH}_bin.tar.gz.sha256
            path: ${source.dir}/graalvm.tar.gz
            spdx:
              name: graalvm
              version: 4.1.2
              packages:
                - name: graalvm
                  purl: pkg:generic/graalvm@21.0.2
                  license: GPL-2.0-or-later
            uid: 0
            gid: 0
          - url: https://dlcdn.apache.org/kafka/KEYS
            path: ${source.dir}/keys/apache-gpg-key
            uid: 0
            gid: 0
          - url: https://dlcdn.apache.org/kafka/4.1.2/kafka-4.1.2-src.tgz.asc
            path: ${source.dir}/kafka-src.tgz.asc
            uid: 0
            gid: 0
          - url: https://dlcdn.apache.org/kafka/4.1.2/kafka-4.1.2-src.tgz
            path: ${source.dir}/kafka-src.tgz
            uid: 0
            gid: 0
          - url: https://dlcdn.apache.org/kafka/4.1.2/kafka_2.13-4.1.2.tgz.asc
            path: ${source.dir}/kafka-dist.tgz.asc
            uid: 0
            gid: 0
          - url: https://dlcdn.apache.org/kafka/4.1.2/kafka_2.13-4.1.2.tgz
            path: ${source.dir}/kafka-dist.tgz
            spdx:
              name: kafka
              version: 4.1.2
              packages:
                - name: kafka
                  purl: pkg:dhi/kafka@4.1.2
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: verify dist
          uses: gpg/verify@v1
          with:
            file: ${source.dir}/kafka-dist.tgz
            keys: ${source.dir}/keys/apache-gpg-key
            signature: ${source.dir}/kafka-dist.tgz.asc
        - name: verify source
          uses: gpg/verify@v1
          with:
            file: ${source.dir}/kafka-src.tgz
            keys: ${source.dir}/keys/apache-gpg-key
            signature: ${source.dir}/kafka-src.tgz.asc
        - name: extract kafka
          runs: |
            set -eux -o pipefail

            mkdir -p "${source.dir}/kafka/src"
            mkdir -p "${source.dir}/kafka/dist"
            tar -xaf kafka-src.tgz --wildcards --strip-components=1 -C "${source.dir}/kafka/src"
            tar -xaf kafka-dist.tgz --strip-components=1 -C "${source.dir}/kafka/dist"
        - name: extract graalvm
          runs: |
            set -eux -o pipefail

            mkdir -p /usr/local
            tar -xzf graalvm.tar.gz --strip-components=1 -C /usr/local
            export JAVA_HOME=/usr/local
        - name: configure maven mirror
          runs: |
            set -eux -o pipefail

            mkdir -p /root/.m2
            cat > /root/.m2/settings.xml << 'EOF'
            <?xml version="1.0" encoding="UTF-8"?>
            <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0">
              <mirrors>
                <mirror>
                  <id>google-maven-central</id>
                  <name>GCS Maven Central mirror</name>
                  <url>https://maven-central.storage-download.googleapis.com/maven2/</url>
                  <mirrorOf>central</mirrorOf>
                </mirror>
              </mirrors>
            </settings>
            EOF
        - name: fix CVEs
          privileged: true
          runs: |
            set -eux -o pipefail

            cd "${source.dir}/kafka/dist"

            # Patching manually because we are using the release distribution and not compiling from source.
            # Using dependency:copy because dependency:get -Ddest does not copy artifacts
            # to the destination directory.

            replace_maven_jar() {
              group="$1"
              artifact="$2"
              version="$3"
              jar="$(find libs -maxdepth 1 -type f -name "${artifact}-[0-9]*.jar")"
              if [ -z "$jar" ]; then
                echo "Expected ${artifact} JAR was not found"
                exit 1
              fi
              rm "$jar"
              mvn org.apache.maven.plugins:maven-dependency-plugin:3.7.0:copy \
                -Dartifact="${group}:${artifact}:${version}" \
                -DoutputDirectory=./libs
            }

            # Fixes CVE-2025-48734 (commons-beanutils)
            cb="$(find libs -maxdepth 1 \( -name 'commons-beanutils-1.9.*.jar' -o -name 'commons-beanutils-1.10.*.jar' \))"
            if [ -n "$cb" ]; then
              rm "$cb"
              mvn org.apache.maven.plugins:maven-dependency-plugin:3.7.0:copy -Dartifact=commons-beanutils:commons-beanutils:1.11.0 -DoutputDirectory=./libs
            fi

            # Fixes CVE-2025-48924 (commons-lang3)
            cl="$(find libs -maxdepth 1 -type f -name 'commons-lang3-3.12.*.jar')"
            if [ -n "$cl" ]; then
              rm "$cl"
              mvn org.apache.maven.plugins:maven-dependency-plugin:3.7.0:copy -Dartifact=org.apache.commons:commons-lang3:3.18.0 -DoutputDirectory=./libs
            fi

            # Fixes CVE-2026-1605, CVE-2026-10050, CVE-2026-10051,
            # CVE-2026-6790, and CVE-2026-8384 (jetty)
            replace_maven_jar org.eclipse.jetty.ee10 jetty-ee10-servlet 12.0.36
            replace_maven_jar org.eclipse.jetty.ee10 jetty-ee10-servlets 12.0.36
            replace_maven_jar org.eclipse.jetty jetty-alpn-client 12.0.36
            replace_maven_jar org.eclipse.jetty jetty-client 12.0.36
            replace_maven_jar org.eclipse.jetty jetty-http 12.0.36
            replace_maven_jar org.eclipse.jetty jetty-io 12.0.36
            replace_maven_jar org.eclipse.jetty jetty-security 12.0.36
            replace_maven_jar org.eclipse.jetty jetty-server 12.0.36
            replace_maven_jar org.eclipse.jetty jetty-session 12.0.36
            replace_maven_jar org.eclipse.jetty jetty-util 12.0.36

            # Fixes GHSA-2r2c-cx56-8933 and GHSA-47qp-hqvx-6r3f (jline-remote-telnet)
            jl="$(find libs -maxdepth 1 -type f -name 'jline-3.*.jar')"
            if [ -n "$jl" ]; then
              rm "$jl"
              mvn org.apache.maven.plugins:maven-dependency-plugin:3.7.0:copy -Dartifact=org.jline:jline:4.2.1 -DoutputDirectory=./libs
            fi

            # Fixes CVE-2026-34477, CVE-2026-34478, CVE-2026-34479, CVE-2026-34480,
            # and CVE-2026-49844 (log4j)
            l4j="$(find libs -maxdepth 1 -type f -name 'log4j-core-2.25.*.jar' ! -name 'log4j-core-2.25.5.jar')"
            if [ -n "$l4j" ]; then
              rm $l4j
              mvn org.apache.maven.plugins:maven-dependency-plugin:3.7.0:copy -Dartifact=org.apache.logging.log4j:log4j-core:2.25.5 -DoutputDirectory=./libs
            fi
            l4j="$(find libs -maxdepth 1 -type f -name 'log4j-api-2.25.*.jar' ! -name 'log4j-api-2.25.5.jar')"
            if [ -n "$l4j" ]; then
              rm $l4j
              mvn org.apache.maven.plugins:maven-dependency-plugin:3.7.0:copy -Dartifact=org.apache.logging.log4j:log4j-api:2.25.5 -DoutputDirectory=./libs
            fi
            l4j="$(find libs -maxdepth 1 -type f -name 'log4j-1.2-api-2.25.*.jar' ! -name 'log4j-1.2-api-2.25.5.jar')"
            if [ -n "$l4j" ]; then
              rm $l4j
              mvn org.apache.maven.plugins:maven-dependency-plugin:3.7.0:copy -Dartifact=org.apache.logging.log4j:log4j-1.2-api:2.25.5 -DoutputDirectory=./libs
            fi
            l4j="$(find libs -maxdepth 1 -type f -name 'log4j-slf4j-impl-2.25.*.jar' ! -name 'log4j-slf4j-impl-2.25.5.jar')"
            if [ -n "$l4j" ]; then
              rm $l4j
              mvn org.apache.maven.plugins:maven-dependency-plugin:3.7.0:copy -Dartifact=org.apache.logging.log4j:log4j-slf4j-impl:2.25.5 -DoutputDirectory=./libs
            fi

            # Fixes CVE-2026-54512, CVE-2026-54513, CVE-2026-54514, CVE-2026-54515,
            # CVE-2026-54516, CVE-2026-54517, CVE-2026-54518, CVE-2026-59889,
            # CVE-2026-68497, CVE-2026-68498, GHSA-72hv-8253-57qq, and
            # GHSA-mhm7-754m-9p8w (jackson-databind/core)
            replace_maven_jar com.fasterxml.jackson.core jackson-databind 2.21.6
            replace_maven_jar com.fasterxml.jackson.core jackson-core 2.21.6
            replace_maven_jar com.fasterxml.jackson.core jackson-annotations 2.21

            # Fixes CVE-2026-59949 (lz4-java)
            replace_maven_jar at.yawk.lz4 lz4-java 1.11.1
        - name: build
          runs: |
            set -eux -o pipefail

            "${source.dir}/kafka/src/docker/native/native_command.sh" \
              native-image \
              "${source.dir}/kafka/src/docker/native/native-image-configs" \
              "${source.dir}/kafka/dist/libs" \
              "${source.dir}/kafka.Kafka"
        - name: install
          runs: |
            set -eux -o pipefail

            chmod 755 ${source.dir}/kafka.Kafka

            mkdir -p ${target.dir}/kafka
            cp ${source.dir}/kafka.Kafka ${target.dir}/kafka

            mkdir ${target.dir}/config
            cp ${source.dir}/kafka/src/config/* ${target.dir}/config
            cp ${source.dir}/kafka/src/docker/server.properties ${target.dir}/config/server.properties
            sed -i \
              -e 's|^advertised\.listeners=PLAINTEXT:/\localhost:9092|advertised.listeners=PLAINTEXT://:9092|' \
              ${target.dir}/config/server.properties
            chown -R 65532:65532 ${target.dir}/config
        - name: sbom
          uses: sbom@v1
          with:
            prefix: kafka
            source: ${source.dir}/kafka/dist
      outputs:
        - source: ${target.dir}/kafka
          target: /opt/kafka/
          uid: 0
          gid: 0
        - source: ${target.dir}/config
          target: /etc/kafka/config
          uid: 0
          gid: 0
        - source: /opt/docker/sbom/kafka
          target: /opt/docker/sbom/kafka
          uid: 0
          gid: 0
    - name: locale
      contents:
        packages:
          - bash
          - locales
          - coreutils
          - gzip
      pipeline:
        - name: install locales
          runs: |
            set -eux -o pipefail

            ln -s /etc/locale.alias /usr/share/locale/locale.alias

            echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen
            locale-gen en_US.UTF-8
      outputs:
        - source: /usr/share/locale
          target: /usr/share/locale
          uid: 0
          gid: 0
        - source: /usr/lib/locale
          target: /usr/lib/locale
          uid: 0
          gid: 0
accounts:
  run-as: kafka
  users:
    - name: kafka
      uid: 65532
      gid: 65532
  groups:
    - name: kafka
      gid: 65532
      members:
        - kafka
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  CLUSTER_ID: 5L6g3nShT-eMCtK--X86sw
  KAFKA_LOG4J_CMD_OPTS: -Dkafka.logs.dir=/opt/kafka/logs -Dlog4j2.configurationFile=/opt/kafka/config/log4j2.yaml
  LANG: en_US.UTF-8
  LANGUAGE: en_US:en
  LC_ALL: en_US.UTF-8
paths:
  - type: directory
    path: /opt/kafka/config
    uid: 65532
    gid: 65532
    mode: "0755"
  - type: directory
    path: /opt/kafka/logs
    uid: 65532
    gid: 65532
    mode: "0755"
  - type: directory
    path: /tmp/kraft-combined-logs
    uid: 65532
    gid: 65532
    mode: "0755"
  - type: file
    path: /usr/local/bin/docker-entrypoint.sh
    uid: 0
    gid: 0
    mode: "0755"
    source: image/kafka/bin/docker-entrypoint-native.sh
annotations:
  org.opencontainers.image.description: A minimal Kafka image
  org.opencontainers.image.licenses: Apache-2.0
entrypoint:
  - /usr/local/bin/docker-entrypoint.sh
cmd:
  - start
  - --config=/opt/kafka/config/server.properties
ports:
  - 9092/tcp
