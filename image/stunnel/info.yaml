metadata:
  display-name: Stunnel
  short-description: stunnel is a TLS proxy that adds SSL/TLS encryption to TCP services with no native TLS support, forwarding plaintext traffic between clients and backends without modifying the underlying application.
  featured: false
  home-url: https://www.stunnel.org/
  categories:
    - networking
    - security
