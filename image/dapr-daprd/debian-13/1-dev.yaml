# syntax=dhi.io/build:2-debian13

name: dapr daprd 1.x (dev)
image: dhi.io/dapr-daprd
variant: dev
tags:
  - 1-dev
  - 1.18-dev
  - 1.18.3-dev
  - 1-debian-dev
  - 1-debian13-dev
  - 1.18-debian-dev
  - 1.18-debian13-dev
  - 1.18.3-debian-dev
  - 1.18.3-debian13-dev
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2025-09-16"
vars:
  COMMIT_SHA: 2dcf2e3548faf3c740b3d29ee300e2132db65cff
  FIPS_SUFFIX: ""
  SEMVER_MAJOR_MINOR_VERSION: "1.18"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.18.3
  VERSION: 1.18.3
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  packages:
    - '!gawk'
    - '!libelogind0'
    - '!original-awk'
    - apt
    - base-files
    - bash
    - ca-certificates
    - ca-certificates-bundle
    - coreutils
    - diffutils
    - dpkg
    - findutils
    - grep
    - libc-bin
    - mawk
    - perl-base
    - sed
    - tzdata
    - util-linux
  builds:
    - name: dapr-daprd
      work-dir: /go/src/github.com/dapr/dapr
      caches:
        - path: /go/pkg/mod
      contents:
        packages:
          - bash
          - ca-certificates-bundle
          - coreutils
          - golang-1.26
          - findutils
          - mawk
          - sed
        files:
          - url: git+https://github.com/dapr/dapr#v1.18.3
            checksum: 2dcf2e3548faf3c740b3d29ee300e2132db65cff
            path: /go/src/github.com/dapr/dapr
            spdx:
              name: dapr
              version: 1.18.3
              packages:
                - name: dapr
                  purl: pkg:golang/github.com/dapr/dapr@1.18.3
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v1
          with:
            deps:
              - golang.org/x/crypto@v0.55.0
              - golang.org/x/mod@v0.40.0
              - filippo.io/edwards25519@v1.1.1
              - github.com/aws/aws-sdk-go-v2/service/bedrockruntime@v1.50.4
              - github.com/aws/aws-sdk-go-v2/service/kinesis@v1.43.5
              - github.com/go-chi/chi/v5@v5.3.0
              - github.com/go-jose/go-jose/v4@v4.1.4
              - github.com/jackc/pgx/v5@v5.9.2
              - github.com/klauspost/compress@v1.18.7
              - github.com/valyala/fasthttp@v1.70.0
              - go.etcd.io/etcd/client/pkg/v3@v3.5.33
              - go.mongodb.org/mongo-driver@v1.17.7
              - google.golang.org/grpc@v1.82.1
              - github.com/valyala/fasthttp@v1.70.0
              - go.etcd.io/etcd/client/pkg/v3@v3.5.33
            download: true
            replaces:
              - go.opentelemetry.io/contrib/instrumentation/google.golang.org/grpc/otelgrpc=go.opentelemetry.io/contrib/instrumentation/google.golang.org/grpc/otelgrpc@v0.60.0
        - name: test go dependencies
          runs: |
            set -eux -o pipefail

            # Build system injects OTEL_EXPORTER_OTLP_TRACES_* vars which take priority
            # over OTEL_EXPORTER_OTLP_* and break unit tests that set the latter
            unset OTEL_EXPORTER_OTLP_TRACES_ENDPOINT || true
            unset OTEL_EXPORTER_OTLP_TRACES_PROTOCOL || true

            # Build containers have no outbound network or non-loopback interfaces;
            # provide a fallback so GetHostAddress() doesn't fail
            export DAPR_HOST_IP="${DAPR_HOST_IP:-127.0.0.1}"

            # CGO_ENABLED=0: build container has no linker (binutils not installed)
            # -tags unit: required to compile unit test helpers (WithAppChannel, mock, diag utils etc.)
            # Exclude tests/: those are integration/e2e tests, not unit tests.
            # Also exclude the dnslookup package because upstream added an
            # integration-style reconnect test there that depends on mixed
            # IPv4/IPv6 loopback behavior and is flaky in the DHI build sandbox.
            CGO_ENABLED=0 go test -tags unit \
              $(go list -tags unit ./... \
                | grep -v 'github.com/dapr/dapr/tests/' \
                | grep -v 'github.com/dapr/dapr/pkg/actors/internal/placement/connector/dnslookup$' \
                | grep -v 'github.com/dapr/dapr/pkg/api/grpc/proxy$')
        - name: build
          uses: go/build@v1
          work-dir: /go/src/github.com/dapr/dapr/cmd/daprd
          with:
            ldflags:
              - -X github.com/dapr/dapr/pkg/buildinfo.gitcommit=2dcf2e3548faf3c740b3d29ee300e2132db65cff
              - -X github.com/dapr/dapr/pkg/buildinfo.gitversion=1.18.3
              - -X github.com/dapr/dapr/pkg/buildinfo.version=1.18.3
            output: ${target.dir}/usr/local/bin/daprd
            tags: allcomponents
      outputs:
        - source: ${target.dir}/usr/local/bin/daprd
          target: /usr/local/bin/daprd
          uid: 0
          gid: 0
        - source: /opt/docker/sbom/dapr
          target: /opt/docker/sbom/dapr
          uid: 0
          gid: 0
accounts:
  root: true
  run-as: root
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
    - name: _apt
      uid: 42
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  DEBIAN_FRONTEND: noninteractive
paths:
  - type: symlink
    path: /daprd
    uid: 0
    gid: 0
    source: /usr/local/bin/daprd
  - type: directory
    path: /app/cmd/daprd
    uid: 0
    gid: 0
  - type: symlink
    path: /app/cmd/daprd/daprd
    uid: 0
    gid: 0
    source: /usr/local/bin/daprd
annotations:
  org.opencontainers.image.description: A minimal Dapr daprd runtime image
  org.opencontainers.image.licenses: Apache-2.0
entrypoint:
  - /usr/local/bin/daprd
ports:
  - "3500"
  - "50001"
  - "9090"
