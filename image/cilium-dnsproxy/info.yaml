metadata:
  display-name: Cilium DNS Proxy
  short-description: Cilium Standalone DNS Proxy is an alpha component that provides independent DNS proxying capabilities, receiving policy rules from the Cilium agent via gRPC.
  featured: false
  home-url: https://docs.cilium.io/en/stable/security/standalone-dns-proxy/
  categories:
    - networking
    - security
