# syntax=dhi.io/build:2-debian13

name: Docker Hub MCP 0.x
image: dhi.io/hub-mcp
variant: runtime
tags:
  - "0"
  - "0.18"
  - 0.18.0
  - 0-debian
  - 0-debian13
  - 0.18-debian
  - 0.18-debian13
  - 0.18.0-debian
  - 0.18.0-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: 98cf1b9cbec64316ea2b465462468a2d2204a406
  SEMVER_MAJOR_MINOR_VERSION: "0.18"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.18.0
  VERSION: 0.18.0
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  packages:
    - base-files
    - ca-certificates-bundle
    - libc6
    - libgomp1
    - libstdc++6
    - netbase
    - nodejs-24
    - tzdata
  builds:
    - name: hub-mcp
      contents:
        packages:
          - gzip
          - base-files
          - bash
          - ca-certificates
          - coreutils
          - findutils
          - git
          - libc6
          - libstdc++6
          - mawk
          - nodejs-24
          - node-corepack
          - npm
          - sed
          - yarn-classic
          - tar
        files:
          - url: git+https://github.com/docker/hub-mcp.git#dockerhub-mcp/v0.18.0
            checksum: 98cf1b9cbec64316ea2b465462468a2d2204a406
            path: ${source.dir}/hub-mcp-98cf1b9cbec64316ea2b465462468a2d2204a406
            spdx:
              name: hub-mcp
              version: 0.18.0
              packages:
                - name: hub-mcp
                  purl: pkg:dhi/hub-mcp@0.18.0
                  license: Apache-2.0
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/minimatch/-/minimatch-10.2.3.tgz
            path: ${source.dir}/minimatch-10.2.3.tgz
            uid: 0
            gid: 0
          - url: https://registry.npmjs.org/@hono/node-server/-/node-server-2.0.10.tgz
            path: ${source.dir}/node-server-2.0.10.tgz
            uid: 0
            gid: 0
      pipeline:
        - name: bump
          uses: npm/bump@v1
          privileged: true
          environment:
            DHI_NO_SFW: "true"
          with:
            bumps: ${source.dir}/patch/npm-bumps.json
            npm-root: ${source.dir}/hub-mcp-98cf1b9cbec64316ea2b465462468a2d2204a406
        - name: install and build
          work-dir: ${source.dir}/hub-mcp-98cf1b9cbec64316ea2b465462468a2d2204a406
          privileged: true
          runs: |
            set -eux -o pipefail

            npm install --package-lock-only
            npm ci
            npm run build

            # prune to production deps only
            rm -rf node_modules
            npm ci --omit=dev --ignore-scripts
        - name: patch nested minimatch packages
          work-dir: ${source.dir}/hub-mcp-98cf1b9cbec64316ea2b465462468a2d2204a406
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/minimatch-fix
            tar -xzf ${source.dir}/minimatch-10.2.3.tgz -C /tmp/minimatch-fix

            find node_modules -type d -name "minimatch" -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' minimatch_dir; do
              echo "Replacing ${minimatch_dir}"
              rm -rf "${minimatch_dir}"
              cp -r /tmp/minimatch-fix/package "${minimatch_dir}"
            done

            rm -rf /tmp/minimatch-fix
        - name: patch nested @hono/node-server packages
          work-dir: ${source.dir}/hub-mcp-98cf1b9cbec64316ea2b465462468a2d2204a406
          runs: |
            set -eux -o pipefail

            mkdir -p /tmp/hono-node-server-fix
            tar -xzf ${source.dir}/node-server-2.0.10.tgz -C /tmp/hono-node-server-fix

            find node_modules -type d -path '*/@hono/node-server' -exec test -f {}/package.json \; -print0 | while IFS= read -r -d '' hono_dir; do
              echo "Replacing ${hono_dir}"
              rm -rf "${hono_dir}"
              cp -r /tmp/hono-node-server-fix/package "${hono_dir}"
            done

            rm -rf /tmp/hono-node-server-fix
      paths:
        - type: file
          path: ${source.dir}/patch/npm-bumps.json
          uid: 0
          gid: 0
          source: image/hub-mcp/patch/npm-bumps.json
      outputs:
        - source: ${source.dir}/hub-mcp-98cf1b9cbec64316ea2b465462468a2d2204a406/node_modules
          target: /app/node_modules
          uid: 0
          gid: 0
        - source: ${source.dir}/hub-mcp-98cf1b9cbec64316ea2b465462468a2d2204a406/dist
          target: /app/dist
          uid: 0
          gid: 0
        - source: /opt/docker/sbom/hub-mcp
          target: /opt/docker/sbom/hub-mcp
          uid: 0
          gid: 0
accounts:
  run-as: node
  users:
    - name: node
      uid: 1000
      gid: 1000
  groups:
    - name: node
      gid: 1000
      members:
        - node
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  NODE_ENV: production
paths:
  - type: directory
    path: /app/logs
    uid: 1000
    gid: 1000
    mode: "0755"
annotations:
  org.opencontainers.image.description: Docker Hub MCP Server
  org.opencontainers.image.licenses: Apache-2.0
labels:
  io.modelcontextprotocol.server.name: io.github.docker/hub-mcp
entrypoint:
  - node
  - /app/dist/index.js
