# syntax=dhi.io/build:2-debian13

name: Neo4j 2026.x
image: dhi.io/neo4j
variant: runtime
tags:
  - "2026"
  - "2026.07"
  - 2026.07.1
  - 2026-debian
  - 2026-debian13
  - 2026.07-debian
  - 2026.07-debian13
  - 2026.07.1-debian
  - 2026.07.1-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  ARCH: '#{ target.arch == "amd64" ? "x64" : "aarch64" }'
  COMMIT_SHA: f213380f812b820a1b312e2ea52cb3d8f1931ccc
  GOSU_REFERENCE: dhi/pkg-gosu:1.19-debian13@sha256:c8d3f464708426e928beabdf198ddcbea1bfdcc305c58f3707dfb9e4aac9e590
  MAVEN_REFERENCE: dhi/maven:3.9.16-jdk21-debian13-dev@sha256:645612ac05bca83249c2a1c532128d6e5d397bdf1e0de52abc4d164034a784d2
  MVN_PROPS_VARIANT: stable
  NEO4J_HOME: /var/lib/neo4j
  SEMVER_MAJOR_MINOR_VERSION: "2026.07"
  SEMVER_MAJOR_VERSION: "2026"
  SEMVER_VERSION: 2026.07.1
  VERSION: 2026.07.1
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  packages:
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - base-files
    - bash
    - coreutils
    - eclipse-temurin-21-jre
    - eclipse-temurin-21-jre-oci-config
    - grep
    - sed
    - tini
  builds:
    - name: neo4j
      uses: dhi.io/maven:3.9.16-jdk21-debian13-dev@sha256:645612ac05bca83249c2a1c532128d6e5d397bdf1e0de52abc4d164034a784d2
      contents:
        packages:
          - coreutils
          - tar
          - gzip
          - xmlstarlet
        files:
          - url: git+https://github.com/neo4j/neo4j.git#2026.07.1
            checksum: f213380f812b820a1b312e2ea52cb3d8f1931ccc
            path: ${source.dir}/neo4j
            spdx:
              name: neo4j
              version: 2026.07.1
              packages:
                - name: neo4j
                  purl: pkg:dhi/neo4j@2026.07.1
                  license: GPL-3.0
            uid: 0
            gid: 0
          - url: git+https://github.com/neo4j/docker-neo4j-publish#master
            path: ${source.dir}/docker-neo4j-publish
            spdx:
              name: docker-neo4j-publish
              version: 2026.07.1
              packages:
                - name: docker-neo4j-publish
                  purl: pkg:dhi/docker-neo4j-publish@2026.07.1
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - uses: mvn/mirror@v1
        - name: configure maven mirror
          runs: |
            set -eux -o pipefail

            # Overwrite the Cloudsmith mirror with GCS (public Maven Central
            # read replica). Used when MAVEN_USER/MAVEN_PASSWORD are unavailable
            # locally; mirrors the keycloak pattern for the same reason.
            mkdir -p /root/.m2
            cat > /root/.m2/settings.xml << 'EOF'
            <?xml version="1.0" encoding="UTF-8"?>
            <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0">
              <mirrors>
                <mirror>
                  <id>google-maven-central</id>
                  <name>GCS Maven Central mirror</name>
                  <url>https://maven-central.storage-download.googleapis.com/maven2/</url>
                  <mirrorOf>central</mirrorOf>
                </mirror>
              </mirrors>
            </settings>
            EOF
        - name: copy startup scripts
          work-dir: ${source.dir}/docker-neo4j-publish
          runs: |
            set -eux -o pipefail

            cp -R 2026.07.1/trixie/community/local-package ${target.dir}/startup
        - name: patch entrypoint to use gosu instead of su-exec
          work-dir: ${target.dir}/startup
          runs: |
            set -eux -o pipefail

            # Replace su-exec with gosu in docker-entrypoint.sh
            # su-exec syntax: su-exec user command args
            # gosu syntax: gosu user command args (same syntax, so simple replacement works)
            sed -i 's/su-exec/gosu/g' docker-entrypoint.sh
        - name: patch properties
          uses: mvn/patch@v1
          with:
            dependencies: ${source.dir}/patch/mvn-deps-stable.json
            dependencies-key: dependencyManagement.dependencies
            mvn-root: ${source.dir}/neo4j
            properties: ${source.dir}/patch/mvn-properties-stable.json
        - name: manually edit pom.xml to allow for additional non-standard field
          work-dir: ${source.dir}/neo4j
          runs: |
            set -eux -o pipefail

            xmlstarlet ed -L \
              -N m="http://maven.apache.org/POM/4.0.0" \
              -s "/m:project/m:dependencyManagement/m:dependencies" -t elem -n "dependencyTMP" -v "" \
              -s "/m:project/m:dependencyManagement/m:dependencies/dependencyTMP" -t elem -n "groupId" -v "io.netty" \
              -s "/m:project/m:dependencyManagement/m:dependencies/dependencyTMP" -t elem -n "artifactId" -v "netty-transport-native-epoll" \
              -s "/m:project/m:dependencyManagement/m:dependencies/dependencyTMP" -t elem -n "version" -v "4.2.17.Final" \
              -s "/m:project/m:dependencyManagement/m:dependencies/dependencyTMP" -t elem -n "classifier" -v "linux-x86_64" \
              -r "/m:project/m:dependencyManagement/m:dependencies/dependencyTMP" -v "dependency" \
              pom.xml

            xmlstarlet ed -L \
              -N m="http://maven.apache.org/POM/4.0.0" \
              -s "/m:project/m:dependencyManagement/m:dependencies" -t elem -n "dependencyTMP" -v "" \
              -s "/m:project/m:dependencyManagement/m:dependencies/dependencyTMP" -t elem -n "groupId" -v "io.netty" \
              -s "/m:project/m:dependencyManagement/m:dependencies/dependencyTMP" -t elem -n "artifactId" -v "netty-transport-native-epoll" \
              -s "/m:project/m:dependencyManagement/m:dependencies/dependencyTMP" -t elem -n "version" -v "4.2.17.Final" \
              -s "/m:project/m:dependencyManagement/m:dependencies/dependencyTMP" -t elem -n "classifier" -v "linux-aarch_64" \
              -r "/m:project/m:dependencyManagement/m:dependencies/dependencyTMP" -v "dependency" \
              pom.xml
        - name: remove maven profiler extension
          work-dir: ${source.dir}/neo4j
          runs: |
            set -eux -o pipefail

            # maven-profiler is optional build analytics tooling and is not
            # available in the DHI Maven mirror, which breaks mvn versions:set.
            ext_xml=".mvn/extensions.xml"
            if [ -f "${ext_xml}" ]; then
              xmlstarlet ed -L \
                -d "//*[local-name()='extension'][*[local-name()='artifactId']='maven-profiler']" \
                "${ext_xml}"
            fi
        - name: build
          work-dir: ${source.dir}/neo4j
          privileged: true
          secrets:
            - id: maven_user
              env: MAVEN_USER
              optional: true
            - id: maven_password
              env: MAVEN_PASSWORD
              optional: true
          runs: |
            set -eux -o pipefail

            # Set platform-specific Maven properties
            if [ "${ARCH}" = "aarch64" ]; then
              export MAVEN_OPTS="-Dos.arch=aarch64 -Dos.name=Linux -Dos.version=arm64"
              export JAVA_OPTS="-Dos.arch=aarch64"
            fi

            # Spotless / lint steps expect build-resources under public/ for the stable line.
            # Apply on all architectures (previously only aarch64, which broke linux/amd64 CI).
            # Only copy if build-resources exists (removed in upstream 2026.07.0+).
            if [ "stable" = "stable" ] && [ -d ./build-resources ]; then
              mkdir -p public && cp -r ./build-resources ./public
            fi

            # Set release version (source pom.xml uses SNAPSHOT versioning)
            mvn versions:set -DnewVersion=2026.07.1 -DgenerateBackupPoms=false -q

            # After updated dependencies, the pom.xml file may need linting;
            mvn spotless:apply

            # Drop the test-only com.neo4j:enterprise-it-test-support dependency from the
            # genai-plugin module (added in 2026.05.0). It is an Enterprise artifact not
            # published to the public Maven mirror, and Maven resolves test-scoped deps
            # even under maven.test.skip, so the build cannot fetch it. We skip tests, so
            # removing it does not affect the runtime jar. Guarded so older lines that
            # lack the module (e.g. 5.x) are unaffected.
            if [ -f community/genai-plugin/pom.xml ]; then
              xmlstarlet ed -L \
                -N m=http://maven.apache.org/POM/4.0.0 \
                -d "/m:project/m:dependencies/m:dependency[m:groupId='com.neo4j' and m:artifactId='enterprise-it-test-support']" \
                community/genai-plugin/pom.xml
            fi

            # Drop the test-jar neo4j-values dependency from random-values. Maven resolves
            # test-scoped deps even under maven.test.skip; until Neo4j publishes the tests
            # classifier to Central the build falls back to the mirror and fails on fresh
            # scout bumps. We skip tests, so removing it does not affect the runtime jar.
            if [ -f community/testing/random-values/pom.xml ]; then
              xmlstarlet ed -L \
                -N m=http://maven.apache.org/POM/4.0.0 \
                -d "/m:project/m:dependencies/m:dependency[m:groupId='org.neo4j' and m:artifactId='neo4j-values' and m:type='test-jar']" \
                community/testing/random-values/pom.xml
            fi

            # Use maven.test.skip (not skipTests) so Maven skips test compilation; this
            # also avoids resolving the remaining test-scoped dependencies of the test
            # sources we are not building.
            # dependency.analyze.skip is neo4j's own property wired into the
            # maven-dependency-plugin <skip> in the parent pom. With test compilation
            # skipped, test-scoped deps (e.g. guava, compile-testing) look unused and
            # modules that hardcode failOnWarning=true (e.g. annotations) would fail
            # analyze-only. The mdep.analyze.skip property is ignored because neo4j binds
            # <skip> to its own property, so we set dependency.analyze.skip instead.
            mvn clean install -Dmaven.test.skip=true -Ddependency.analyze.skip=true -Doverwrite=true
        - name: install
          work-dir: ${source.dir}/neo4j
          runs: |
            set -eux -o pipefail

            mkdir -p ${target.dir}/var/lib/neo4j
            TARFILE=$(find packaging/standalone/target -name "neo4j-community-*-unix.tar.gz")
            tar -xzf ${TARFILE} -C ${target.dir}/var/lib/neo4j --strip-components=1
      paths:
        - type: file
          path: /src/patch/mvn-properties-stable.json
          uid: 0
          gid: 0
          source: image/neo4j/patch/mvn-properties-stable.json
        - type: file
          path: /src/patch/mvn-deps-stable.json
          uid: 0
          gid: 0
          source: image/neo4j/patch/mvn-deps-stable.json
      outputs:
        - source: ${target.dir}/var/lib/neo4j
          target: /var/lib/neo4j
          uid: 7474
          gid: 7474
          mode: "0744"
        - source: ${target.dir}/startup
          target: /startup
          uid: 0
          gid: 0
        - source: ${target.dir}/startup/docker-entrypoint.sh
          target: /usr/local/bin/docker-entrypoint.sh
          uid: 0
          gid: 0
        - source: /opt/docker/sbom/neo4j
          target: /opt/docker/sbom/neo4j
          uid: 0
          gid: 0
    - name: locale
      contents:
        packages:
          - bash
          - locales
          - coreutils
          - gzip
      pipeline:
        - name: install locales
          runs: |
            set -eux -o pipefail

            ln -s /etc/locale.alias /usr/share/locale/locale.alias

            echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen
            locale-gen en_US.UTF-8
      outputs:
        - source: /usr/share/locale
          target: /usr/share/locale
          uid: 0
          gid: 0
        - source: /usr/lib/locale
          target: /usr/lib/locale
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/pkg-gosu:1.19-debian13@sha256:c8d3f464708426e928beabdf198ddcbea1bfdcc305c58f3707dfb9e4aac9e590
      includes:
        - opt/docker/sbom/gosu
        - usr/local/sbin/gosu
      uid: 0
      gid: 0
accounts:
  run-as: neo4j
  users:
    - name: neo4j
      uid: 7474
      gid: 7474
  groups:
    - name: neo4j
      gid: 7474
      members:
        - neo4j
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  LANG: en_US.UTF-8
  LANGUAGE: en_US:en
  LC_ALL: en_US.UTF-8
  NEO4J_EDITION: community
  NEO4J_HOME: /var/lib/neo4j
  PATH: /usr/local/sbin:/var/lib/neo4j/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
paths:
  - type: symlink
    path: /data
    uid: 7474
    gid: 7474
    source: /var/lib/neo4j/data
  - type: symlink
    path: /logs
    uid: 7474
    gid: 7474
    source: /var/lib/neo4j/logs
annotations:
  org.opencontainers.image.description: A minimal Neo4j image
  org.opencontainers.image.licenses: GPL-3.0
entrypoint:
  - /usr/bin/tini
  - -g
  - --
  - /startup/docker-entrypoint.sh
cmd:
  - neo4j
ports:
  - 7473/tcp
  - 7474/tcp
  - 7687/tcp
