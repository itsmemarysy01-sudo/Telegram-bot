# Pins mirror netbox-community/netbox-docker's requirements-container.txt.
django-auth-ldap==5.3.0
# CVE-2026-42305, CVE-2026-42563
dulwich==1.2.14
granian[uvloop]==2.8.2
python3-saml==1.16.0
# Compile lxml + xmlsec against the system libxmlsec1 so SAML signing works
# under FIPS; the PyPI xmlsec wheel links a different libxmlsec1.
--no-binary lxml
--no-binary xmlsec
sentry-sdk[django]==2.68.1
