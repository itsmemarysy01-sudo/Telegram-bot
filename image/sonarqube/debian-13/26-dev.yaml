# syntax=dhi.io/build:2-debian13

name: SonarQube 26.x (dev)
image: dhi.io/sonarqube
variant: dev
tags:
  - 26-dev
  - 26.8-dev
  - 26.8.0-dev
  - 26-debian-dev
  - 26-debian13-dev
  - 26.8-debian-dev
  - 26.8-debian13-dev
  - 26.8.0-debian-dev
  - 26.8.0-debian13-dev
platforms:
  - linux/amd64
  - linux/arm64
vars:
  COMMIT_SHA: b33183b846cd47c7a811e280f5755f13d8fa8cc7
  ELASTICSEARCH_AMD64_SHA512: d53f8d3efeb830b73704873d082b940158deb9387937ed817eea89d2bf542ece24dae877548e4b48b0e134d5123704f04c86be23dcd9cb732a2489868a8dd0c5
  ELASTICSEARCH_ARM64_SHA512: c41f4d33c7ce8fdd0754017755d05a9ba4d6326b4099525f1939cdc12a5c5e7afd1879ee45d56a90c665bdb877da4b553fda13796a60030e214ae1bd2da6fbaa
  ELASTICSEARCH_LINUX_ARCH: '#{ target.arch == "arm64" ? "aarch64" : "x86_64" }'
  ELASTICSEARCH_LINUX_SHA512: '#{ target.arch == "arm64" ? "c41f4d33c7ce8fdd0754017755d05a9ba4d6326b4099525f1939cdc12a5c5e7afd1879ee45d56a90c665bdb877da4b553fda13796a60030e214ae1bd2da6fbaa" : "d53f8d3efeb830b73704873d082b940158deb9387937ed817eea89d2bf542ece24dae877548e4b48b0e134d5123704f04c86be23dcd9cb732a2489868a8dd0c5" }'
  ELASTICSEARCH_VERSION: 9.4.3
  HTTPCLIENT5_VERSION: 5.6.3
  HTTPCORE5_VERSION: 5.4.3
  JACKSON_CORE_2_VERSION: 2.21.4
  JACKSON_CORE_3_VERSION: 3.1.6
  JACKSON_DATABIND_2_VERSION: 2.21.6
  JACKSON_DATABIND_3_VERSION: 3.1.6
  JACKSON_DATABIND_ES_VERSION: 2.18.10
  JAVA_HOME_PATH: /usr/lib/jvm/temurin-21
  JDK_MAJOR_VERSION: "21"
  LOG4J_API_25_VERSION: 2.25.5
  LOG4J_API_26_VERSION: 2.26.1
  MAVEN_REF: dhi/maven:3.9.16-jdk21-debian13-dev@sha256:55ddfc1748b1daf376c6a079062fef83379ff2b21b9e0379e375f467bf1f84b2
  NETTY_VERSION: 4.1.137.Final
  REACTOR_NETTY_VERSION: 1.2.8
  SEMVER_MAJOR_MINOR_VERSION: "26.8"
  SEMVER_MAJOR_VERSION: "26"
  SEMVER_VERSION: 26.8.0
  SONARQUBE_DOCKER_SHA: 886f9480f4a9caf83de23784e11aa054cc3ad7f5
  SONARQUBE_DOCKER_VERSION: 26.8.0.126808
  SONARQUBE_HOME: /opt/sonarqube
  SSHD_VERSION: 2.12.0
  VERSION: 26.8.0.126808
contents:
  repositories:
    - deb [signed-by=/usr/share/keyrings/dhi-deb-main.gpg] http://dhi.io/deb/debian/main trixie main
  keyring:
    - https://dhi.io/keyring/dhi-deb-gpg.D46852F6925E9F71.key
  packages:
    - '!gawk'
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - apt
    - base-files
    - bash
    - coreutils
    - diffutils
    - dpkg
    - eclipse-temurin-21-jdk
    - eclipse-temurin-21-jdk-oci-config
    - findutils
    - gosu
    - grep
    - libc-bin
    - libstdc++6
    - mawk
    - perl-base
    - sed
    - util-linux
  builds:
    - name: sonarqube
      uses: dhi.io/maven:3.9.16-jdk21-debian13-dev@sha256:55ddfc1748b1daf376c6a079062fef83379ff2b21b9e0379e375f467bf1f84b2
      contents:
        packages:
          - bash
          - ca-certificates
          - libudev-dev
          - unzip
          - zip
          - jq
          - grep
        files:
          - url: git+https://github.com/SonarSource/sonarqube.git#26.8.0.126808
            checksum: b33183b846cd47c7a811e280f5755f13d8fa8cc7
            path: ${source.dir}/sonarqube
            spdx:
              name: sonarqube
              version: 26.8.0.126808
              packages:
                - name: sonarqube
                  purl: pkg:dhi/sonarqube@26.8.0.126808
                  license: LGPL-3.0-only
            uid: 0
            gid: 0
          - url: https://raw.githubusercontent.com/SonarSource/docker-sonarqube/26.8.0.126808/community-build/entrypoint.sh
            path: ${source.dir}/entrypoint.sh
            spdx:
              name: docker-sonarqube-entrypoint
              version: 26.8.0.126808
              packages:
                - name: docker-sonarqube-entrypoint
                  purl: pkg:dhi/docker-sonarqube@26.8.0.126808
                  license: LGPL-3.0-only
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/netty/netty-handler/4.1.137.Final/netty-handler-4.1.137.Final.jar
            path: ${source.dir}/patch/deps/netty-handler-4.1.137.Final.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/netty/netty-codec-http2/4.1.137.Final/netty-codec-http2-4.1.137.Final.jar
            path: ${source.dir}/patch/deps/netty-codec-http2-4.1.137.Final.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/netty/netty-common/4.1.137.Final/netty-common-4.1.137.Final.jar
            path: ${source.dir}/patch/deps/netty-common-4.1.137.Final.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/netty/netty-codec/4.1.137.Final/netty-codec-4.1.137.Final.jar
            path: ${source.dir}/patch/deps/netty-codec-4.1.137.Final.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/netty/netty-codec-dns/4.1.137.Final/netty-codec-dns-4.1.137.Final.jar
            path: ${source.dir}/patch/deps/netty-codec-dns-4.1.137.Final.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/netty/netty-codec-http/4.1.137.Final/netty-codec-http-4.1.137.Final.jar
            path: ${source.dir}/patch/deps/netty-codec-http-4.1.137.Final.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/netty/netty-resolver-dns/4.1.137.Final/netty-resolver-dns-4.1.137.Final.jar
            path: ${source.dir}/patch/deps/netty-resolver-dns-4.1.137.Final.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/netty/netty-resolver/4.1.137.Final/netty-resolver-4.1.137.Final.jar
            path: ${source.dir}/patch/deps/netty-resolver-4.1.137.Final.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/netty/netty-transport/4.1.137.Final/netty-transport-4.1.137.Final.jar
            path: ${source.dir}/patch/deps/netty-transport-4.1.137.Final.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/netty/netty-buffer/4.1.137.Final/netty-buffer-4.1.137.Final.jar
            path: ${source.dir}/patch/deps/netty-buffer-4.1.137.Final.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/apache/sshd/sshd-common/2.12.0/sshd-common-2.12.0.jar
            path: ${source.dir}/patch/deps/sshd-common-2.12.0.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/apache/sshd/sshd-core/2.12.0/sshd-core-2.12.0.jar
            path: ${source.dir}/patch/deps/sshd-core-2.12.0.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/bouncycastle/bc-fips/2.1.2/bc-fips-2.1.2.jar
            path: ${source.dir}/patch/deps/bc-fips-2.1.2.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/bouncycastle/bcpkix-jdk18on/1.84/bcpkix-jdk18on-1.84.jar
            path: ${source.dir}/patch/deps/bcpkix-jdk18on-1.84.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/bouncycastle/bcprov-jdk18on/1.84/bcprov-jdk18on-1.84.jar
            path: ${source.dir}/patch/deps/bcprov-jdk18on-1.84.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/com/microsoft/sqlserver/mssql-jdbc/13.2.1.jre11/mssql-jdbc-13.2.1.jre11.jar
            path: ${source.dir}/patch/deps/mssql-jdbc-13.2.1.jre11.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/at/yawk/lz4/lz4-java/1.11.1/lz4-java-1.11.1.jar
            path: ${source.dir}/patch/deps/lz4-java-1.11.1.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/apache/logging/log4j/log4j-1.2-api/2.25.4/log4j-1.2-api-2.25.4.jar
            path: ${source.dir}/patch/deps/log4j-1.2-api-2.25.4.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/com/sun/mail/jakarta.mail/1.6.8/jakarta.mail-1.6.8.jar
            path: ${source.dir}/patch/deps/jakarta.mail-1.6.8.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/postgresql/postgresql/42.7.12/postgresql-42.7.12.jar
            path: ${source.dir}/patch/deps/postgresql-42.7.12.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/tools/jackson/core/jackson-core/3.1.6/jackson-core-3.1.6.jar
            path: ${source.dir}/patch/deps/jackson-core-3.1.6.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/com/fasterxml/jackson/core/jackson-databind/2.21.6/jackson-databind-2.21.6.jar
            path: ${source.dir}/patch/deps/jackson-databind-2.21.6.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/tools/jackson/core/jackson-databind/3.1.6/jackson-databind-3.1.6.jar
            path: ${source.dir}/patch/deps/jackson-databind-3.1.6.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/com/fasterxml/jackson/core/jackson-core/2.21.4/jackson-core-2.21.4.jar
            path: ${source.dir}/patch/deps/jackson-core-2.21.4.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/com/fasterxml/jackson/core/jackson-core/2.18.10/jackson-core-2.18.10.jar
            path: ${source.dir}/patch/deps/jackson-core-2.18.10.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/com/fasterxml/jackson/core/jackson-databind/2.18.10/jackson-databind-2.18.10.jar
            path: ${source.dir}/patch/deps/jackson-databind-2.18.10.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/projectreactor/netty/reactor-netty-http/1.2.8/reactor-netty-http-1.2.8.jar
            path: ${source.dir}/patch/deps/reactor-netty-http-1.2.8.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/io/projectreactor/netty/reactor-netty-core/1.2.8/reactor-netty-core-1.2.8.jar
            path: ${source.dir}/patch/deps/reactor-netty-core-1.2.8.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/apache/httpcomponents/core5/httpcore5/5.4.3/httpcore5-5.4.3.jar
            path: ${source.dir}/patch/deps/httpcore5-5.4.3.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/apache/httpcomponents/core5/httpcore5-h2/5.4.3/httpcore5-h2-5.4.3.jar
            path: ${source.dir}/patch/deps/httpcore5-h2-5.4.3.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/apache/httpcomponents/client5/httpclient5/5.6.3/httpclient5-5.6.3.jar
            path: ${source.dir}/patch/deps/httpclient5-5.6.3.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/apache/logging/log4j/log4j-api/2.25.5/log4j-api-2.25.5.jar
            path: ${source.dir}/patch/deps/log4j-api-2.25.5.jar
            uid: 0
            gid: 0
          - url: https://maven-central.storage-download.googleapis.com/maven2/org/apache/logging/log4j/log4j-api/2.26.1/log4j-api-2.26.1.jar
            path: ${source.dir}/patch/deps/log4j-api-2.26.1.jar
            uid: 0
            gid: 0
      pipeline:
        - name: set elasticsearch arch and checksum
          work-dir: ${source.dir}/sonarqube/sonar-application
          runs: |
            set -eux -o pipefail

            sed -i 's/linux-x86_64/linux-${ELASTICSEARCH_LINUX_ARCH}/g' build.gradle
            sed -i 's/^elasticSearchLinuxSha512=.*/elasticSearchLinuxSha512=${ELASTICSEARCH_LINUX_SHA512}/' ../gradle.properties
        - name: build distribution
          work-dir: ${source.dir}/sonarqube
          privileged: true
          runs: |
            set -eux -o pipefail

            ./gradlew :sonar-application:zip -x test --no-daemon --console=plain
        - name: extract and verify
          work-dir: ${source.dir}/sonarqube
          runs: |
            set -eux -o pipefail

            dist_zip=$(find . -name "sonar-application-*.zip" -type f)
            unzip -q "$dist_zip" -d "${target.dir}"
            dist_dir=$(find "${target.dir}" -name "sonarqube-*" -type d)
            mv "$dist_dir" "${target.dir}/sonarqube"
        - name: patch vulnerable JARs
          runs: |
            set -eux -o pipefail

            echo "Apply patches"
            # Apply runtime vulnerability patches using generic DHI patcher
            chmod +x "${source.dir}/patch/dhi-runtime-patch.sh"
            "${source.dir}/patch/dhi-runtime-patch.sh" \
              --app-root "${target.dir}/sonarqube" \
              --patch-dir "${source.dir}/patch/deps" \
              --config "${source.dir}/patch/dhi-patch-config.json"
        - name: patch bundled sshd in scanner engine
          runs: |
            set -eux -o pipefail

            scanner_jar=$(find "${target.dir}/sonarqube/lib/scanner" -name 'sonar-scanner-engine-community-*.jar' -type f | head -1)
            if [[ -z "${scanner_jar}" ]]; then
              echo "ERROR: sonar-scanner-engine-community jar not found" >&2
              exit 1
            fi

            patch_dir="$(mktemp -d)"
            repack_dir="${patch_dir}/repack"
            overlay_dir="${patch_dir}/overlay"
            trap 'rm -rf "${patch_dir}"' EXIT
            mkdir -p "${repack_dir}" "${overlay_dir}"

            (cd "${repack_dir}" && jar xf "${scanner_jar}")
            rm -rf "${repack_dir}/org/apache/sshd" "${repack_dir}/META-INF/maven/org.apache.sshd"

            for dep in sshd-common sshd-core; do
              (cd "${overlay_dir}" && jar xf "${source.dir}/patch/deps/${dep}-2.12.0.jar")
            done
            cp -a "${overlay_dir}/org/apache/sshd" "${repack_dir}/org/apache/sshd"
            mkdir -p "${repack_dir}/META-INF/maven"
            cp -a "${overlay_dir}/META-INF/maven/org.apache.sshd" "${repack_dir}/META-INF/maven/org.apache.sshd"

            scanner_new="${patch_dir}/scanner-repacked.jar"
            (cd "${repack_dir}" && jar --create --file "${scanner_new}" --manifest META-INF/MANIFEST.MF .)
            mv "${scanner_new}" "${scanner_jar}"

            unzip -p "${scanner_jar}" META-INF/maven/org.apache.sshd/sshd-common/pom.properties | grep -q "version=2.12.0"
            unzip -p "${scanner_jar}" META-INF/maven/org.apache.sshd/sshd-core/pom.properties | grep -q "version=2.12.0"
        - name: patch bundled jackson databind
          runs: |
            set -eux -o pipefail

            shopt -s nullglob

            patched_count=0

            patch_databind() {
              local group_id="$1"
              local class_dir="$2"
              local patch_jar="$3"
              local expected_version="$4"
              local metadata_dir="META-INF/maven/${group_id}/jackson-databind"
              local pom_properties="${repack_dir}/${metadata_dir}/pom.properties"

              if [[ ! -f "${pom_properties}" ]]; then
                return
              fi

              if grep -q "version=${expected_version}" "${pom_properties}"; then
                return
              fi

              rm -rf "${repack_dir:?}/${class_dir}" "${repack_dir:?}/${metadata_dir}"
              rm -rf "${overlay_dir:?}"/*
              (cd "${overlay_dir}" && jar xf "${patch_jar}")

              mkdir -p "${repack_dir}/$(dirname "${class_dir}")" "${repack_dir}/META-INF/maven/${group_id}"
              cp -a "${overlay_dir}/${class_dir}" "${repack_dir}/${class_dir}"
              cp -a "${overlay_dir}/${metadata_dir}" "${repack_dir}/${metadata_dir}"
              grep -q "version=${expected_version}" "${repack_dir}/${metadata_dir}/pom.properties"

              patched_count=$((patched_count + 1))
            }

            patch_jackson_core() {
              local group_id="$1"
              local class_dir="$2"
              local patch_jar="$3"
              local expected_version="$4"
              local metadata_dir="META-INF/maven/${group_id}/jackson-core"
              local pom_properties="${repack_dir}/${metadata_dir}/pom.properties"

              if [[ ! -f "${pom_properties}" ]]; then
                return
              fi

              if grep -q "version=${expected_version}" "${pom_properties}"; then
                return
              fi

              rm -rf "${repack_dir:?}/${class_dir}" "${repack_dir:?}/${metadata_dir}"
              for vdir in "${repack_dir}"/META-INF/versions/*/${class_dir}; do
                if [[ -d "${vdir}" ]]; then
                  rm -rf "${vdir}"
                fi
              done

              rm -rf "${overlay_dir:?}"/*
              (cd "${overlay_dir}" && jar xf "${patch_jar}")

              mkdir -p "${repack_dir}/$(dirname "${class_dir}")" "${repack_dir}/META-INF/maven/${group_id}"
              cp -a "${overlay_dir}/${class_dir}" "${repack_dir}/${class_dir}"
              cp -a "${overlay_dir}/${metadata_dir}" "${repack_dir}/${metadata_dir}"
              for vdir in "${overlay_dir}"/META-INF/versions/*/${class_dir}; do
                if [[ -d "${vdir}" ]]; then
                  rel="${vdir#${overlay_dir}/}"
                  mkdir -p "${repack_dir}/$(dirname "${rel}")"
                  cp -a "${vdir}" "${repack_dir}/${rel}"
                fi
              done

              grep -q "version=${expected_version}" "${repack_dir}/${metadata_dir}/pom.properties"

              patched_count=$((patched_count + 1))
            }

            patch_fat_jar() {
              local relative_glob="$1"
              local matches=("${target.dir}/sonarqube"/${relative_glob})
              if [[ "${#matches[@]}" -ne 1 ]]; then
                echo "ERROR: expected exactly one JAR for ${relative_glob}, found ${#matches[@]}" >&2
                exit 1
              fi

              local outer_jar="${matches[0]}"
              local before_count="${patched_count}"
              patch_dir="$(mktemp -d)"
              repack_dir="${patch_dir}/repack"
              overlay_dir="${patch_dir}/overlay"
              mkdir -p "${repack_dir}" "${overlay_dir}"

              (cd "${repack_dir}" && jar xf "${outer_jar}")

              patch_databind \
                "com.fasterxml.jackson.core" \
                "com/fasterxml/jackson/databind" \
                "${source.dir}/patch/deps/jackson-databind-2.21.6.jar" \
                "2.21.6"
              patch_databind \
                "tools.jackson.core" \
                "tools/jackson/databind" \
                "${source.dir}/patch/deps/jackson-databind-3.1.6.jar" \
                "3.1.6"
              patch_jackson_core \
                "com.fasterxml.jackson.core" \
                "com/fasterxml/jackson/core" \
                "${source.dir}/patch/deps/jackson-core-2.21.4.jar" \
                "2.21.4"

              if [[ "${patched_count}" -eq "${before_count}" ]]; then
                echo "ERROR: no jackson metadata patched in ${outer_jar}" >&2
                exit 1
              fi

              patched_jar="${patch_dir}/$(basename "${outer_jar}")"
              (cd "${repack_dir}" && jar --create --file "${patched_jar}" --manifest META-INF/MANIFEST.MF .)
              mv "${patched_jar}" "${outer_jar}"
              rm -rf "${patch_dir}"
            }

            patch_fat_jar "lib/extensions/sonar-python-plugin-*.jar"
            patch_fat_jar "lib/extensions/sonar-text-plugin-*.jar"
            patch_fat_jar "lib/scanner/sonar-scanner-engine-community-*.jar"
            patch_fat_jar "lib/sonar-application-*.jar"

            if [[ "${patched_count}" -lt 5 ]]; then
              echo "ERROR: expected at least 5 bundled jackson patches, got ${patched_count}" >&2
              exit 1
            fi

            echo "Patched ${patched_count} bundled jackson JARs"
        - name: patch bundled jackson core
          runs: |
            set -eux -o pipefail

            shopt -s nullglob

            app_jar=$(find "${target.dir}/sonarqube/lib" -maxdepth 1 -name 'sonar-application-*.jar' -type f | head -1)
            if [[ -z "${app_jar}" ]]; then
              echo "ERROR: sonar-application jar not found" >&2
              exit 1
            fi

            metadata_dir="META-INF/maven/tools.jackson.core/jackson-core"
            patch_dir="$(mktemp -d)"
            repack_dir="${patch_dir}/repack"
            overlay_dir="${patch_dir}/overlay"
            trap 'rm -rf "${patch_dir}"' EXIT
            mkdir -p "${repack_dir}" "${overlay_dir}"

            (cd "${repack_dir}" && jar xf "${app_jar}")

            if [[ ! -f "${repack_dir}/${metadata_dir}/pom.properties" ]]; then
              echo "ERROR: tools.jackson.core:jackson-core not found in $(basename "${app_jar}")" >&2
              exit 1
            fi

            (cd "${overlay_dir}" && jar xf "${source.dir}/patch/deps/jackson-core-3.1.6.jar")

            rm -rf "${repack_dir:?}/tools/jackson/core" "${repack_dir:?}/${metadata_dir}"
            for vdir in "${repack_dir}"/META-INF/versions/*/tools/jackson/core; do
              rm -rf "${vdir}"
            done

            mkdir -p "${repack_dir}/tools/jackson" "${repack_dir}/META-INF/maven/tools.jackson.core"
            cp -a "${overlay_dir}/tools/jackson/core" "${repack_dir}/tools/jackson/core"
            cp -a "${overlay_dir}/${metadata_dir}" "${repack_dir}/${metadata_dir}"
            for vdir in "${overlay_dir}"/META-INF/versions/*/tools/jackson/core; do
              rel="${vdir#${overlay_dir}/}"
              mkdir -p "${repack_dir}/$(dirname "${rel}")"
              cp -a "${vdir}" "${repack_dir}/${rel}"
            done

            grep -q "version=3.1.6" "${repack_dir}/${metadata_dir}/pom.properties"

            app_new="${patch_dir}/app-repacked.jar"
            (cd "${repack_dir}" && jar --create --file "${app_new}" --manifest META-INF/MANIFEST.MF .)
            mv "${app_new}" "${app_jar}"

            unzip -p "${app_jar}" "${metadata_dir}/pom.properties" | grep -q "version=3.1.6"
        - name: patch bundled jackson core 2.x in elasticsearch
          runs: |
            set -eux -o pipefail

            shopt -s nullglob

            patched_count=0

            patch_jackson_core_2x() {
              local jar_path="$1"
              local expected_version="2.18.10"
              local metadata_dir="META-INF/maven/com.fasterxml.jackson.core/jackson-core"
              local patch_jar="${source.dir}/patch/deps/jackson-core-${expected_version}.jar"
              local verify_impl_jars=false

              patch_dir="$(mktemp -d)"
              repack_dir="${patch_dir}/repack"
              overlay_dir="${patch_dir}/overlay"
              mkdir -p "${repack_dir}" "${overlay_dir}"

              (cd "${repack_dir}" && jar xf "${jar_path}")

              if [[ -f "${repack_dir}/${metadata_dir}/pom.properties" ]]; then
                if grep -q "version=${expected_version}" "${repack_dir}/${metadata_dir}/pom.properties"; then
                  rm -rf "${patch_dir}"
                  return
                fi

                (cd "${overlay_dir}" && jar xf "${patch_jar}")

                rm -rf "${repack_dir:?}/com/fasterxml/jackson/core" "${repack_dir:?}/${metadata_dir}"
                for vdir in "${repack_dir}"/META-INF/versions/*/com/fasterxml/jackson/core; do
                  rm -rf "${vdir}"
                done

                mkdir -p "${repack_dir}/com/fasterxml/jackson" "${repack_dir}/META-INF/maven/com.fasterxml.jackson.core"
                cp -a "${overlay_dir}/com/fasterxml/jackson/core" "${repack_dir}/com/fasterxml/jackson/core"
                cp -a "${overlay_dir}/${metadata_dir}" "${repack_dir}/${metadata_dir}"
                for vdir in "${overlay_dir}"/META-INF/versions/*/com/fasterxml/jackson/core; do
                  rel="${vdir#${overlay_dir}/}"
                  mkdir -p "${repack_dir}/$(dirname "${rel}")"
                  cp -a "${vdir}" "${repack_dir}/${rel}"
                done

                grep -q "version=${expected_version}" "${repack_dir}/${metadata_dir}/pom.properties"
              elif [[ -d "${repack_dir}/IMPL-JARS/x-content" ]]; then
                local old_jackson_name
                old_jackson_name=$(find "${repack_dir}/IMPL-JARS/x-content" -maxdepth 1 -type d -name 'jackson-core-*.jar' -printf '%f\n' | head -1)

                if [[ -z "${old_jackson_name}" ]]; then
                  rm -rf "${patch_dir}"
                  return
                fi

                if [[ "${old_jackson_name}" == "jackson-core-${expected_version}.jar" ]]; then
                  rm -rf "${patch_dir}"
                  return
                fi

                rm -rf "${repack_dir}/IMPL-JARS/x-content/${old_jackson_name}"
                mkdir -p "${repack_dir}/IMPL-JARS/x-content/jackson-core-${expected_version}.jar"
                (cd "${repack_dir}/IMPL-JARS/x-content/jackson-core-${expected_version}.jar" && jar xf "${patch_jar}")

                if [[ -f "${repack_dir}/IMPL-JARS/x-content/LISTING.TXT" ]]; then
                  sed -i "s|${old_jackson_name}|jackson-core-${expected_version}.jar|g" "${repack_dir}/IMPL-JARS/x-content/LISTING.TXT"
                fi

                grep -q "version=${expected_version}" "${repack_dir}/IMPL-JARS/x-content/jackson-core-${expected_version}.jar/${metadata_dir}/pom.properties"
                verify_impl_jars=true
              else
                rm -rf "${patch_dir}"
                return
              fi

              patched_jar="${patch_dir}/$(basename "${jar_path}")"
              (cd "${repack_dir}" && jar --create --file "${patched_jar}" --manifest META-INF/MANIFEST.MF .)
              mv "${patched_jar}" "${jar_path}"
              rm -rf "${patch_dir}"

              if [[ "${verify_impl_jars}" == true ]]; then
                unzip -p "${jar_path}" "IMPL-JARS/x-content/jackson-core-${expected_version}.jar/${metadata_dir}/pom.properties" | grep -q "version=${expected_version}"
              else
                unzip -p "${jar_path}" "${metadata_dir}/pom.properties" | grep -q "version=${expected_version}"
              fi
              patched_count=$((patched_count + 1))
            }

            for jar in "${target.dir}/sonarqube/elasticsearch/lib/"*.jar "${target.dir}/sonarqube/elasticsearch/lib/tools/plugin-cli/"*.jar "${target.dir}/sonarqube/elasticsearch/modules/"*/*.jar "${target.dir}/sonarqube/elasticsearch/bin/"*.jar; do
              if [[ -f "${jar}" ]]; then
                patch_jackson_core_2x "${jar}"
              fi
            done

            if [[ "${patched_count}" -lt 1 ]]; then
              echo "ERROR: expected at least one bundled jackson-core 2.x patch in Elasticsearch" >&2
              exit 1
            fi

            echo "Patched ${patched_count} bundled jackson-core 2.x JARs in Elasticsearch"
        - name: patch bundled lz4 in sonar-application
          runs: |
            set -eux -o pipefail

            app_jar=$(find "${target.dir}/sonarqube/lib" -maxdepth 1 -name 'sonar-application-*.jar' -type f | head -1)
            if [[ -z "${app_jar}" ]]; then
              echo "ERROR: sonar-application jar not found" >&2
              exit 1
            fi

            metadata_dir="META-INF/maven/at.yawk.lz4/lz4-java"
            patch_dir="$(mktemp -d)"
            repack_dir="${patch_dir}/repack"
            overlay_dir="${patch_dir}/overlay"
            trap 'rm -rf "${patch_dir}"' EXIT
            mkdir -p "${repack_dir}" "${overlay_dir}"

            (cd "${repack_dir}" && jar xf "${app_jar}")

            if [[ ! -f "${repack_dir}/${metadata_dir}/pom.properties" ]]; then
              echo "ERROR: at.yawk.lz4:lz4-java not found in $(basename "${app_jar}")" >&2
              exit 1
            fi

            if grep -q "version=1.11.1" "${repack_dir}/${metadata_dir}/pom.properties"; then
              echo "lz4-java already at 1.11.1 in $(basename "${app_jar}")"
              exit 0
            fi

            (cd "${overlay_dir}" && jar xf "${source.dir}/patch/deps/lz4-java-1.11.1.jar")

            rm -rf "${repack_dir:?}/net/jpountz" "${repack_dir:?}/${metadata_dir}"
            mkdir -p "${repack_dir}/net" "${repack_dir}/META-INF/maven/at.yawk.lz4"
            cp -a "${overlay_dir}/net/jpountz" "${repack_dir}/net/jpountz"
            cp -a "${overlay_dir}/${metadata_dir}" "${repack_dir}/${metadata_dir}"

            grep -q "version=1.11.1" "${repack_dir}/${metadata_dir}/pom.properties"

            app_new="${patch_dir}/app-repacked.jar"
            (cd "${repack_dir}" && jar --create --file "${app_new}" --manifest META-INF/MANIFEST.MF .)
            mv "${app_new}" "${app_jar}"

            unzip -p "${app_jar}" "${metadata_dir}/pom.properties" | grep -q "version=1.11.1"
        - name: patch bundled httpcore5 in sonar-application
          runs: |
            set -eux -o pipefail

            app_jar=$(find "${target.dir}/sonarqube/lib" -maxdepth 1 -name 'sonar-application-*.jar' -type f | head -1)
            if [[ -z "${app_jar}" ]]; then
              echo "ERROR: sonar-application jar not found" >&2
              exit 1
            fi

            metadata_dir="META-INF/maven/org.apache.httpcomponents.core5/httpcore5"
            h2_metadata_dir="META-INF/maven/org.apache.httpcomponents.core5/httpcore5-h2"
            patch_dir="$(mktemp -d)"
            repack_dir="${patch_dir}/repack"
            overlay_dir="${patch_dir}/overlay"
            trap 'rm -rf "${patch_dir}"' EXIT
            mkdir -p "${repack_dir}" "${overlay_dir}"

            (cd "${repack_dir}" && jar xf "${app_jar}")

            if [[ ! -f "${repack_dir}/${metadata_dir}/pom.properties" ]]; then
              echo "ERROR: org.apache.httpcomponents.core5:httpcore5 not found in $(basename "${app_jar}")" >&2
              exit 1
            fi

            if grep -q "version=5.4.3" "${repack_dir}/${metadata_dir}/pom.properties" \
              && [[ -f "${repack_dir}/${h2_metadata_dir}/pom.properties" ]] \
              && grep -q "version=5.4.3" "${repack_dir}/${h2_metadata_dir}/pom.properties" \
              && [[ -f "${repack_dir}/org/apache/hc/core5/http2/protocol/H2RequestTargetHost.class" ]]; then
              echo "httpcore5 already at 5.4.3 in $(basename "${app_jar}")"
              exit 0
            fi

            rm -rf "${overlay_dir:?}"/*
            (cd "${overlay_dir}" && jar xf "${source.dir}/patch/deps/httpcore5-5.4.3.jar")
            (cd "${overlay_dir}" && jar xf "${source.dir}/patch/deps/httpcore5-h2-5.4.3.jar")

            rm -rf "${repack_dir:?}/org/apache/hc/core5" \
              "${repack_dir:?}/${metadata_dir}" \
              "${repack_dir:?}/${h2_metadata_dir}"
            mkdir -p "${repack_dir}/org/apache/hc" "${repack_dir}/META-INF/maven/org.apache.httpcomponents.core5"
            cp -a "${overlay_dir}/org/apache/hc/core5" "${repack_dir}/org/apache/hc/core5"
            cp -a "${overlay_dir}/${metadata_dir}" "${repack_dir}/${metadata_dir}"
            cp -a "${overlay_dir}/${h2_metadata_dir}" "${repack_dir}/${h2_metadata_dir}"

            grep -q "version=5.4.3" "${repack_dir}/${metadata_dir}/pom.properties"
            grep -q "version=5.4.3" "${repack_dir}/${h2_metadata_dir}/pom.properties"
            [[ -f "${repack_dir}/org/apache/hc/core5/http2/protocol/H2RequestTargetHost.class" ]]

            app_new="${patch_dir}/app-repacked.jar"
            (cd "${repack_dir}" && jar --create --file "${app_new}" --manifest META-INF/MANIFEST.MF .)
            mv "${app_new}" "${app_jar}"

            unzip -p "${app_jar}" "${metadata_dir}/pom.properties" | grep -q "version=5.4.3"
            unzip -p "${app_jar}" "${h2_metadata_dir}/pom.properties" | grep -q "version=5.4.3"
        - name: patch bundled httpclient5 in sonar-application
          runs: |
            set -eux -o pipefail

            app_jar=$(find "${target.dir}/sonarqube/lib" -maxdepth 1 -name 'sonar-application-*.jar' -type f | head -1)
            if [[ -z "${app_jar}" ]]; then
              echo "ERROR: sonar-application jar not found" >&2
              exit 1
            fi

            metadata_dir="META-INF/maven/org.apache.httpcomponents.client5/httpclient5"
            patch_dir="$(mktemp -d)"
            repack_dir="${patch_dir}/repack"
            overlay_dir="${patch_dir}/overlay"
            trap 'rm -rf "${patch_dir}"' EXIT
            mkdir -p "${repack_dir}" "${overlay_dir}"

            (cd "${repack_dir}" && jar xf "${app_jar}")

            if [[ ! -f "${repack_dir}/${metadata_dir}/pom.properties" ]]; then
              echo "ERROR: org.apache.httpcomponents.client5:httpclient5 not found in $(basename "${app_jar}")" >&2
              exit 1
            fi

            if grep -q "version=5.6.3" "${repack_dir}/${metadata_dir}/pom.properties"; then
              echo "httpclient5 already at 5.6.3 in $(basename "${app_jar}")"
              exit 0
            fi

            rm -rf "${overlay_dir:?}"/*
            (cd "${overlay_dir}" && jar xf "${source.dir}/patch/deps/httpclient5-5.6.3.jar")

            rm -rf "${repack_dir:?}/org/apache/hc/client5" "${repack_dir:?}/${metadata_dir}"
            mkdir -p "${repack_dir}/org/apache/hc" "${repack_dir}/META-INF/maven/org.apache.httpcomponents.client5"
            cp -a "${overlay_dir}/org/apache/hc/client5" "${repack_dir}/org/apache/hc/client5"
            cp -a "${overlay_dir}/${metadata_dir}" "${repack_dir}/${metadata_dir}"

            grep -q "version=5.6.3" "${repack_dir}/${metadata_dir}/pom.properties"

            app_new="${patch_dir}/app-repacked.jar"
            (cd "${repack_dir}" && jar --create --file "${app_new}" --manifest META-INF/MANIFEST.MF .)
            mv "${app_new}" "${app_jar}"

            unzip -p "${app_jar}" "${metadata_dir}/pom.properties" | grep -q "version=5.6.3"
        - name: patch bundled log4j-api in sonar-application
          runs: |
            set -eux -o pipefail

            app_jar=$(find "${target.dir}/sonarqube/lib" -maxdepth 1 -name 'sonar-application-*.jar' -type f | head -1)
            if [[ -z "${app_jar}" ]]; then
              echo "ERROR: sonar-application jar not found" >&2
              exit 1
            fi

            metadata_dir="META-INF/maven/org.apache.logging.log4j/log4j-api"
            patch_dir="$(mktemp -d)"
            repack_dir="${patch_dir}/repack"
            overlay_dir="${patch_dir}/overlay"
            trap 'rm -rf "${patch_dir}"' EXIT
            mkdir -p "${repack_dir}" "${overlay_dir}"

            (cd "${repack_dir}" && jar xf "${app_jar}")

            if [[ ! -f "${repack_dir}/${metadata_dir}/pom.properties" ]]; then
              echo "ERROR: org.apache.logging.log4j:log4j-api not found in $(basename "${app_jar}")" >&2
              exit 1
            fi

            if grep -q "version=2.26.1" "${repack_dir}/${metadata_dir}/pom.properties"; then
              echo "log4j-api already at 2.26.1 in $(basename "${app_jar}")"
              exit 0
            fi

            rm -rf "${overlay_dir:?}"/*
            (cd "${overlay_dir}" && jar xf "${source.dir}/patch/deps/log4j-api-2.26.1.jar")

            rm -rf "${repack_dir:?}/org/apache/logging/log4j" "${repack_dir:?}/${metadata_dir}"
            for vdir in "${repack_dir}"/META-INF/versions/*/org/apache/logging/log4j; do
              rm -rf "${vdir}"
            done

            mkdir -p "${repack_dir}/org/apache/logging" "${repack_dir}/META-INF/maven/org.apache.logging.log4j"
            cp -a "${overlay_dir}/org/apache/logging/log4j" "${repack_dir}/org/apache/logging/log4j"
            cp -a "${overlay_dir}/${metadata_dir}" "${repack_dir}/${metadata_dir}"
            for vdir in "${overlay_dir}"/META-INF/versions/*/org/apache/logging/log4j; do
              rel="${vdir#${overlay_dir}/}"
              mkdir -p "${repack_dir}/$(dirname "${rel}")"
              cp -a "${vdir}" "${repack_dir}/${rel}"
            done

            grep -q "version=2.26.1" "${repack_dir}/${metadata_dir}/pom.properties"

            app_new="${patch_dir}/app-repacked.jar"
            (cd "${repack_dir}" && jar --create --file "${app_new}" --manifest META-INF/MANIFEST.MF .)
            mv "${app_new}" "${app_jar}"

            unzip -p "${app_jar}" "${metadata_dir}/pom.properties" | grep -q "version=2.26.1"
        - name: patch bundled log4j-api in elastic apm agent
          runs: |
            set -eux -o pipefail

            apm_jar=$(find "${target.dir}/sonarqube/elasticsearch/modules/apm" -name 'elastic-apm-agent-java8-*.jar' -type f | head -1)
            if [[ -z "${apm_jar}" ]]; then
              echo "ERROR: elastic-apm-agent-java8 jar not found" >&2
              exit 1
            fi

            metadata_dir="agent/META-INF/maven/org.apache.logging.log4j/log4j-api"
            class_dir="agent/org/apache/logging/log4j"
            patch_dir="$(mktemp -d)"
            repack_dir="${patch_dir}/repack"
            overlay_dir="${patch_dir}/overlay"
            trap 'rm -rf "${patch_dir}"' EXIT
            mkdir -p "${repack_dir}" "${overlay_dir}"

            (cd "${repack_dir}" && jar xf "${apm_jar}")

            if [[ ! -f "${repack_dir}/${metadata_dir}/pom.properties" ]]; then
              echo "ERROR: org.apache.logging.log4j:log4j-api not found in $(basename "${apm_jar}")" >&2
              exit 1
            fi

            if grep -q "version=2.25.5" "${repack_dir}/${metadata_dir}/pom.properties"; then
              echo "log4j-api already at 2.25.5 in $(basename "${apm_jar}")"
              exit 0
            fi

            rm -rf "${overlay_dir:?}"/*
            (cd "${overlay_dir}" && jar xf "${source.dir}/patch/deps/log4j-api-2.25.5.jar")

            rm -rf "${repack_dir:?}/${class_dir}" "${repack_dir:?}/${metadata_dir}"
            for vdir in "${repack_dir}"/agent/META-INF/versions/*/org/apache/logging/log4j; do
              rm -rf "${vdir}"
            done

            mkdir -p "${repack_dir}/agent/org/apache/logging" "${repack_dir}/agent/META-INF/maven/org.apache.logging.log4j"
            cp -a "${overlay_dir}/org/apache/logging/log4j" "${repack_dir}/${class_dir}"
            cp -a "${overlay_dir}/META-INF/maven/org.apache.logging.log4j/log4j-api" "${repack_dir}/${metadata_dir}"
            for vdir in "${overlay_dir}"/META-INF/versions/*/org/apache/logging/log4j; do
              rel="agent/${vdir#${overlay_dir}/META-INF/}"
              mkdir -p "${repack_dir}/$(dirname "${rel}")"
              cp -a "${vdir}" "${repack_dir}/${rel}"
            done

            grep -q "version=2.25.5" "${repack_dir}/${metadata_dir}/pom.properties"

            apm_new="${patch_dir}/apm-repacked.jar"
            (cd "${repack_dir}" && jar --create --file "${apm_new}" --manifest META-INF/MANIFEST.MF .)
            mv "${apm_new}" "${apm_jar}"

            unzip -p "${apm_jar}" "${metadata_dir}/pom.properties" | grep -q "version=2.25.5"
      paths:
        - type: file
          path: ${source.dir}/patch/dhi-patch-config.json
          uid: 0
          gid: 0
          source: image/sonarqube/patch/dhi-patch-config.json
        - type: file
          path: ${source.dir}/patch/dhi-runtime-patch.sh
          uid: 0
          gid: 0
          source: image/sonarqube/patch/dhi-runtime-patch.sh
      outputs:
        - source: ${target.dir}/sonarqube
          target: /opt/sonarqube
          uid: 0
          gid: 0
        - source: ${source.dir}/entrypoint.sh
          target: /opt/sonarqube/docker/entrypoint.sh
          uid: 0
          gid: 0
          mode: "0755"
        - source: /opt/docker/sbom
          target: /opt/docker/sbom
          uid: 0
          gid: 0
    - name: locale
      contents:
        packages:
          - bash
          - locales
          - coreutils
          - gzip
      pipeline:
        - name: install locales
          runs: |
            set -eux -o pipefail

            ln -s /etc/locale.alias /usr/share/locale/locale.alias

            echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen
            locale-gen en_US.UTF-8
      outputs:
        - source: /usr/share/locale
          target: /usr/share/locale
          uid: 0
          gid: 0
        - source: /usr/lib/locale
          target: /usr/lib/locale
          uid: 0
          gid: 0
accounts:
  root: true
  run-as: root
  users:
    - name: sonarqube
      uid: 65532
      gid: 65532
    - name: _apt
      uid: 42
      gid: 65532
  groups:
    - name: sonarqube
      gid: 65532
      members:
        - sonarqube
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
work-dir: /opt/sonarqube
environment:
  DEBIAN_FRONTEND: noninteractive
  DOCKER_RUNNING: "true"
  ES_JAVA_HOME: /usr/lib/jvm/temurin-21
  JAVA_HOME: /usr/lib/jvm/temurin-21
  LANG: en_US.UTF-8
  LANGUAGE: en_US:en
  LC_ALL: en_US.UTF-8
  SONAR_VERSION: 26.8.0.126808
  SONARQUBE_HOME: /opt/sonarqube
  SQ_DATA_DIR: /opt/sonarqube/data
  SQ_EXTENSIONS_DIR: /opt/sonarqube/extensions
  SQ_LOGS_DIR: /opt/sonarqube/logs
  SQ_TEMP_DIR: /opt/sonarqube/temp
paths:
  - type: directory
    path: /opt/sonarqube/data
    uid: 65532
    gid: 65532
    mode: "0770"
  - type: directory
    path: /opt/sonarqube/extensions
    uid: 65532
    gid: 65532
    mode: "0770"
  - type: directory
    path: /opt/sonarqube/extensions/plugins
    uid: 65532
    gid: 65532
    mode: "0770"
  - type: directory
    path: /opt/sonarqube/logs
    uid: 65532
    gid: 65532
    mode: "0770"
  - type: directory
    path: /opt/sonarqube/temp
    uid: 65532
    gid: 65532
    mode: "0770"
  - type: directory
    path: /opt/sonarqube/temp/conf
    uid: 65532
    gid: 65532
    mode: "0770"
  - type: directory
    path: /opt/sonarqube/temp/conf/es
    uid: 65532
    gid: 65532
    mode: "0770"
  - type: symlink
    path: /opt/sonarqube/lib/sonarqube.jar
    uid: 0
    gid: 0
    source: /opt/sonarqube/lib/sonar-application-26.8-SNAPSHOT.jar
  - type: directory
    path: /opt/java
    uid: 0
    gid: 0
    mode: "0755"
  - type: symlink
    path: /opt/java/openjdk
    uid: 0
    gid: 0
    source: /usr/lib/jvm/temurin-21
annotations:
  org.opencontainers.image.description: A minimal SonarQube image.
  org.opencontainers.image.licenses: GNU Lesser General Public License v3.0
entrypoint:
  - gosu
  - sonarqube
  - /opt/sonarqube/docker/entrypoint.sh
ports:
  - 9000/tcp
