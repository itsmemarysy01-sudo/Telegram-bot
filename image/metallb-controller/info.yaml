metadata:
  display-name: MetalLB Controller
  short-description: MetalLB controller component manages IP address allocations for LoadBalancer services on bare metal Kubernetes clusters.
  featured: false
  home-url: https://metallb.io/
  categories:
    - networking
  included-tools:
    - name: controller
      description: MetalLB controller daemon that manages IP address allocation from configured pools and coordinates with speaker components to announce LoadBalancer service IPs.
