metadata:
  display-name: Klipper Helm
  short-description: |
    Shell entrypoint and Helm plugins used by the K3s and RKE2 built-in Helm controller job image.
  featured: false
  home-url: https://github.com/k3s-io/klipper-helm
  categories:
    - integration-and-delivery
  included-tools:
    - name: entry
      description: Upstream Klipper Helm job driver script (Helm controller entrypoint)
    - name: helm
      description: Helm 3 CLI from Docker Hardened Images, used by the entry script and plugins
