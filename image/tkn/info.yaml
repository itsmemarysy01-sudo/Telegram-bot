metadata:
  display-name: Tekton CLI
  short-description: The Tekton CLI (tkn) manages Tekton Pipelines, Tasks, and Triggers resources from the command line. It creates, starts, describes and follows the logs of pipeline and task runs against a Kubernetes cluster, and packages Tekton resources as OCI bundles.
  featured: false
  home-url: https://tekton.dev/docs/cli/
  categories:
    - developer-tools
