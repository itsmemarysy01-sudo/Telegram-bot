metadata:
  display-name: Amazon EKS Pod Identity Webhook
  short-description: The Amazon EKS Pod Identity Webhook is a Kubernetes mutating admission webhook that injects AWS IAM Roles for Service Accounts (IRSA) credentials into pods, letting workloads assume an IAM role via a projected service account token instead of long-lived credentials.
  featured: false
  home-url: https://github.com/aws/amazon-eks-pod-identity-webhook
  categories:
    - integration-and-delivery
    - security
