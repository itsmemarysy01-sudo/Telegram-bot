metadata:
  display-name: DataHub Actions
  short-description: Event-driven framework that reacts to real-time changes in DataHub's metadata graph by polling Kafka topics and invoking pluggable actions (Slack/Teams notifications, doc/tag propagation, executor for UI-triggered ingestion).
  featured: false
  home-url: https://datahub.com
  categories:
    - integration-and-delivery
