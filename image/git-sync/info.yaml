metadata:
  display-name: Git-sync
  short-description: git-sync is a sidecar that pulls a git repository into a local directory, keeping it synced to a remote.
  featured: false
  home-url: https://github.com/kubernetes/git-sync
  categories:
    - integration-and-delivery
